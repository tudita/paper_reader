{
  "schemaVersion": 2,
  "metadata": {
    "title": "DEPART: Replica Decoupling for Distributed Key-Value Storage",
    "titleTranslation": "DEPART：面向分布式键值存储的副本解耦",
    "authors": [
      "Qiang Zhang",
      "Yongkun Li",
      "Patrick P. C. Lee",
      "Yinlong Xu",
      "Si Wu"
    ],
    "year": "2022",
    "venue": "20th USENIX Conference on File and Storage Technologies (FAST '22)",
    "doi": "",
    "source": "https://www.usenix.org/conference/fast22/presentation/zhang-qiang",
    "language": "en",
    "targetLanguage": "zh-CN",
    "notes": ""
  },
  "sections": [
    {
      "id": "010-abstract",
      "title": "Abstract",
      "titleTranslation": "摘要",
      "level": 1,
      "originalMarkdown": "Modern distributed key-value (KV) stores adopt replication for fault tolerance by distributing replicas of KV pairs across nodes. However, existing distributed KV stores often manage all replicas in the same index structure, thereby leading to significant I/O costs beyond the replication redundancy. We propose a notion called replica decoupling, which decouples the storage management of the primary and redundant copies of replicas, so as to not only mitigate the I/O costs in indexing, but also provide tunable performance. In particular, we design a novel two-layer log that enables tunable ordering for the redundant copies to achieve balanced read/write performance. We implement a distributed KV store prototype, DEPART, atop Cassandra. Experiments show that DEPART outperforms Cassandra in all performance aspects under various consistency levels and parameter settings. Specifically, under the eventual consistency setting, DEPART achieves up to 1.43×, 2.43×, 2.68×, and 1.44× throughput for writes, reads, scans, and updates, respectively.",
      "translationMarkdown": "现代分布式 key-value（KV）存储通过将 KV 对的多个副本分布到不同节点上，采用复制机制实现容错。然而，现有的分布式 KV 存储通常在同一个索引结构中管理所有副本，因此除了复制本身带来的冗余之外，还会产生显著的 I/O 开销。我们提出了一个称为副本解耦（replica decoupling）的概念，将副本的主副本和冗余副本的存储管理解耦，从而不仅能降低索引过程中的 I/O 开销，还能提供可调的性能。具体而言，我们设计了一种新颖的两层日志，它能够以可调的方式对冗余副本排序，从而实现均衡的读写性能。我们在 Cassandra 之上实现了一个分布式 KV 存储原型 DEPART。实验表明，在不同的一致性级别和参数设置下，DEPART 在各项性能指标上均优于 Cassandra。具体而言，在最终一致性设置下，DEPART 的写、读、扫描和更新吞吐量最高分别达到 Cassandra 的 1.43×、2.43×、2.68× 和 1.44×。"
    },
    {
      "id": "020-introduction",
      "title": "1 Introduction",
      "titleTranslation": "1 引言",
      "level": 1,
      "originalMarkdown": "Key-value (KV) stores serve as essential building blocks in the storage infrastructure of modern data-intensive applications, such as web search [14, 31], social networking [57], photo stores [10], and cloud storage [25, 37]. To support large-scale usage, KV stores are often deployed in a distributed manner by storing the data objects (in the form of KV pairs) across multiple nodes. Examples of distributed KV stores include BigTable [14], HBase [3], Dynamo [25], HyperDex [28], Cassandra [37], TiKV [50], and Riak [54].\n\nFailures become prevalent in any large-scale deployment, so providing fault tolerance for distributed KV stores is critical. Replication remains the commonly used fault tolerance mechanism in modern distributed KV stores (including the examples listed above [3, 14, 25, 28, 37, 50, 54]). Specifically, for each KV pair issued by a user write, replication makes multiple exact copies (called replicas) and distributes the replicas across different nodes, so as to tolerate any node failure.\n\nOne subtlety is that each node internally stores all replicas in the same index structure; we call such an approach uniform indexing. For example, we have examined the codebases of various open-source distributed KV stores, including HBase [3], HyperDex [28], Cassandra [37], TiKV [50], and ScyllaDB [60], and they all adopt uniform indexing for replica management. In particular, they keep all replicas originated from different nodes in a log-structured-merged tree (LSM-tree) [48], a multi-level tree structure that supports efficient reads and writes of KV pairs and maintains sorted KV pairs in each level for efficient scans (or range queries) to consecutive ranges of KV pairs. They either build on local LSM-tree KV stores (e.g., HyperDex uses HyperLevelDB [27] and TiKV uses RocksDB [29]), or implement their own LSM-tree structures (e.g., in HBase and Cassandra).\n\nUniform indexing is simple to implement for replica management, but it also significantly degrades both the write and read performance. First, the LSM-tree performs frequent compaction operations that rewrite the currently stored KV pairs to maintain their sorted order in each level. Storing all replicas in the same LSM-tree exacerbates the write amplification beyond the replication redundancy. For example, when replication is disabled, the write amplifications of Cassandra and TiKV are 6.5× and 13.8×, respectively; however, under triple replication, the write amplifications reach 25.7× and 50.9× in Cassandra and TiKV, respectively, incurring more than three times in write amplification (§3.1). Also, as reading a KV pair needs to search multiple levels in the LSM-tree, uniform indexing amplifies the search space and exacerbates the read amplification as well. For example, under triple replication, the read amplification of Cassandra reaches 34.6× (§3.1).\n\nOur insight is that instead of putting all replicas in the same index structure, if we use different index structures for managing the storage of different types of replicas, we not only mitigate the read/write amplifications by reducing the size of the index structure for each type of replicas, but also enable flexible storage management to adapt to different design trade-offs. We make a case by proposing replica decoupling, which decouples the storage management of the replicas of each KV pair based on the primary copy (i.e., the main replica of the KV pair) and the redundant copies (i.e., the remaining replicas of the KV pair aside the primary copy). We use the LSM-tree to manage the primary copies only, so as to preserve the design features of the LSM-tree but in a more lightweight manner; meanwhile, we use simpler but tunable index structures for the redundant copies to balance the read and write performance depending on the performance requirements.\n\nIn this paper, we design replica decoupling in DEPART, a novel distributed KV store that decouples the storage management of primary and redundant copies for fault tolerance. DEPART builds on Cassandra [37]. It supports lightweight differentiation of the primary and redundant copies of replicas on the critical I/O path, while keeping the existing data organization and configurable consistency features of Cassandra. While managing the primary copies in the LSM-tree, DEPART proposes a novel two-layer log to manage the redundant copies with tunable ordering for balanced read and write performance. Its idea is to issue batched writes for the redundant copies into an append-only global log for high write performance. It further splits the global log into multiple local logs. In particular, the ordering of KV pairs in each local log is tunable by a single parameter to balance the read and write performance for the redundant copies; for example, given a high read (or write) consistency level (i.e., the number of replicas to be read (or written) in a successful operation; see §2.3), the two-layer log can be tuned to favor for high read (or write) performance. The two-layer log also improves failure recovery performance, by organizing the KV pairs by different key ranges and limiting a recovery operation to access only the relevant range of KV pairs. Our contributions are summarized as follows.\n\n- We analyze two state-of-the-art distributed KV stores, Cassandra and TiKV, and reveal their performance limitations due to uniform indexing for replicas.\n- We design DEPART, which realizes replica decoupling and has several key design features: (i) lightweight differentiation of primary and redundant copies, (ii) a two-layer log design with tunable ordering of redundant copies, and (iii) a fast failure recovery implementation via parallelization.\n- We implement DEPART atop Cassandra v3.11.4 [2]. Experiments show that DEPART outperforms Cassandra in various settings. For example, for the case of eventual consistency, DEPART achieves 1.43×, 2.43×, 2.68×, and 1.44× throughput gains over Cassandra in writes, reads, scans, and updates, respectively. DEPART also maintains its read and write performance gains under various consistency level configurations.\n\nThe source code of our DEPART prototype is available at: https://github.com/ustcadsl/depart.",
      "translationMarkdown": "Key-value（KV）存储是现代数据密集型应用存储基础设施中的关键构件，这些应用包括 Web 搜索 [14, 31]、社交网络 [57]、照片存储 [10] 和云存储 [25, 37]。为了支持大规模使用，KV 存储通常以分布式方式部署，将数据对象（以 KV 对的形式）存放在多个节点上。分布式 KV 存储的例子包括 BigTable [14]、HBase [3]、Dynamo [25]、HyperDex [28]、Cassandra [37]、TiKV [50] 和 Riak [54]。\n\n在任何大规模部署中，故障都会变得十分常见，因此，为分布式 KV 存储提供容错能力至关重要。复制仍然是现代分布式 KV 存储（包括上述例子 [3, 14, 25, 28, 37, 50, 54]）普遍采用的容错机制。具体而言，对于用户每次写入所提交的 KV 对，复制机制会生成多个完全相同的副本（称为 replicas），并将这些副本分布到不同节点上，从而能够容忍任意节点故障。\n\n一个容易被忽视的细节是，每个节点都在同一个索引结构中存储其内部的所有副本；我们将这种方法称为统一索引（uniform indexing）。例如，我们考察了多种开源分布式 KV 存储的代码库，包括 HBase [3]、HyperDex [28]、Cassandra [37]、TiKV [50] 和 ScyllaDB [60]，它们都采用统一索引来管理副本。具体而言，它们将源自不同节点的所有副本都保存在一棵 log-structured-merged tree（LSM-tree）[48] 中。LSM-tree 是一种多层树结构，支持高效地读写 KV 对，并在每一层中维护有序的 KV 对，以便对连续范围的 KV 对进行高效扫描（或范围查询）。这些系统要么构建在本地 LSM-tree KV 存储之上（例如，HyperDex 使用 HyperLevelDB [27]，TiKV 使用 RocksDB [29]），要么自行实现 LSM-tree 结构（例如 HBase 和 Cassandra）。\n\n采用统一索引实现副本管理很简单，但也会显著降低写性能和读性能。首先，LSM-tree 会频繁执行 compaction 操作，重写当前存储的 KV 对，以维持每一层中的有序性。将所有副本都存储在同一棵 LSM-tree 中，会使写放大在复制冗余之外进一步恶化。例如，当禁用复制时，Cassandra 和 TiKV 的写放大分别为 6.5× 和 13.8×；但在三副本复制下，Cassandra 和 TiKV 的写放大分别达到 25.7× 和 50.9×，使写放大增加到三倍以上（§3.1）。此外，由于读取一个 KV 对需要搜索 LSM-tree 的多个层级，统一索引也会扩大搜索空间并加剧读放大。例如，在三副本复制下，Cassandra 的读放大达到 34.6×（§3.1）。\n\n我们的洞察是：与其将所有副本放入同一个索引结构，不如使用不同的索引结构来管理不同类型副本的存储。这样不仅可以通过减小每类副本对应的索引结构规模来缓解读/写放大，还能实现灵活的存储管理，以适应不同的设计权衡。为此，我们提出副本解耦：根据主副本（即 KV 对的主要副本）和冗余副本（即除主副本之外该 KV 对的其余副本），将每个 KV 对各副本的存储管理解耦。我们仅使用 LSM-tree 管理主副本，从而以更轻量的方式保留 LSM-tree 的设计特性；与此同时，对冗余副本采用更简单但可调的索引结构，以根据性能需求平衡读写性能。\n\n在本文中，我们在一种新颖的分布式 KV 存储 DEPART 中设计了副本解耦。DEPART 对用于容错的主副本和冗余副本进行解耦存储管理。DEPART 构建于 Cassandra [37] 之上。它在关键 I/O 路径上以轻量方式区分主副本和冗余副本，同时保留 Cassandra 现有的数据组织方式和可配置的一致性特性。在使用 LSM-tree 管理主副本的同时，DEPART 提出一种新颖的两层日志，以可调的顺序管理冗余副本，从而平衡读写性能。其核心思想是：将冗余副本分批写入一个仅追加的全局日志，以获得较高的写性能；然后再将全局日志划分为多个局部日志。具体而言，每个局部日志中 KV 对的顺序可通过单个参数调节，以平衡冗余副本的读写性能。例如，当读（或写）一致性级别较高时（即一次操作成功所需读取或写入的副本数量；参见 §2.3），可以调整两层日志，使其偏向较高的读（或写）性能。两层日志还通过按不同 key range 组织 KV 对，并将恢复操作限制为仅访问相关范围内的 KV 对，来提升故障恢复性能。我们的贡献概括如下。\n\n- 我们分析了两个最先进的分布式 KV 存储 Cassandra 和 TiKV，并揭示了统一索引副本所造成的性能局限。\n- 我们设计了实现副本解耦的 DEPART，其关键设计特性包括：(i) 以轻量方式区分主副本与冗余副本；(ii) 采用两层日志设计，以可调顺序组织冗余副本；(iii) 通过并行化快速实现故障恢复。\n- 我们在 Cassandra v3.11.4 [2] 之上实现了 DEPART。实验表明，DEPART 在多种设置下均优于 Cassandra。例如，在最终一致性的情况下，DEPART 相对于 Cassandra 的写、读、扫描和更新吞吐量分别提升 1.43×、2.43×、2.68× 和 1.44×。在不同的一致性级别配置下，DEPART 也能保持其读写性能优势。\n\nDEPART 原型的源代码可从以下地址获取：https://github.com/ustcadsl/depart。"
    },
    {
      "id": "030-background",
      "title": "2 Background",
      "titleTranslation": "2 背景",
      "level": 1,
      "originalMarkdown": "We use Cassandra [37] (which serves as the baseline for our DEPART design) as an example to describe the background of a distributed KV store, including its storage architecture, I/O workflows, and consistency management.",
      "translationMarkdown": "我们以 Cassandra [37]（它也是 DEPART 设计的基线）为例，介绍分布式 KV store 的背景，包括其存储架构、I/O 工作流和一致性管理。"
    },
    {
      "id": "040-storage-architecture",
      "title": "2.1 Storage Architecture",
      "titleTranslation": "2.1 存储架构",
      "level": 2,
      "originalMarkdown": "**Data organization.** A distributed KV store partitions KV pairs across a cluster of nodes. In Cassandra, the KV pairs are partitioned based on consistent hashing [33], which has also been adopted by other production distributed KV stores [25, 41, 54, 60]. Consistent hashing associates the locations of all nodes with a hash ring and maps each KV pair deterministically to a node. Specifically, we consider a distributed KV store with $n$ physical nodes, each of which is associated with $v$ virtual nodes. It divides the hash ring into $n \\times v$ ranges, each of which covers one of the virtual nodes. For example, as shown in Figure 1, there are $n = 5$ physical nodes (i.e., $N_0$ to $N_4$) with $v = 2$ virtual nodes each. The hash ring contains $2 \\times 5 = 10$ ranges, say $(0-10)$, $(11-20)$, $\\cdots$, $(91-100)$. Each of the ranges is associated with the nearest virtual node in the clockwise direction in the hash ring and the corresponding physical node; for example, both ranges $(0-10)$ and $(51-60)$ are assigned to $N_0$. For each KV pair, consistent hashing hashes the key to a location in the hash ring (e.g., using MurmurHash [6] in Cassandra). The KV pair is then stored in the corresponding physical node that is associated with the range.\n\nReplication is commonly used in modern distributed KV stores [3, 14, 25, 28, 37, 50] for fault tolerance, by distributing the replicas of each KV pair across different nodes to protect against node failures. In Cassandra, replicas are stored in a sequence of nodes along the clockwise direction in the hash ring denoted by $N_i$, $N_{(i+1) \\bmod n}$, $N_{(i+2) \\bmod n}$, $\\cdots$, where $0 \\leq i \\leq n-1$ and $N_i$ (i.e., the first node in the node sequence) is the node to which the KV pair is hashed based on consistent hashing. We refer to the replica that is stored in $N_i$ as the primary copy, while referring to the remaining replicas that are stored in the successive physical nodes along the clockwise direction in the hash ring as the redundant copies.\n\n**Internal storage with the LSM-tree.** Each node internally manages KV pairs with some index structure. In particular, the LSM-tree [48] is one of the most commonly used index structures in distributed KV stores, including Cassandra and others [3, 20, 28, 41, 50, 60]. An LSM-tree KV store organizes KV pairs in a multi-level tree and keeps KV pairs sorted by keys in each level, so as to support efficient reads, writes, and scans. As shown in Figure 1, the LSM-tree KV store maintains a tree-based index structure with multiple levels (denoted by $L_0$, $L_1$, $\\cdots$) with an increasing capacity, in which each level stores the KV pairs in units of files called SSTables. It first appends the written KV pairs into an on-disk write-ahead log (WAL), and inserts them into an in-memory MemTable. When the MemTable is full, the LSM-tree KV store turns the MemTable to an immutable MemTable, which is flushed to the lowest level $L_0$ as an SSTable. When a lower level reaches a capacity limit, the LSM-tree KV store merges the KV pairs at the lower level into the next higher level via compaction. To keep the KV pairs in each level sorted, a compaction operation first reads the KV pairs from both levels, merges the sorted KV pairs, and writes back the sorted KV pairs. Thus, compaction incurs extra I/Os during writes, leading to write amplification. Also, since KV pairs are not sorted across different levels, reading a KV pair needs to search from the lowest level $L_0$ to the higher levels, leading to read amplification. Both write and read amplifications are shown to cause significant performance degradations in LSM-tree KV stores [12, 43, 51].\n\n*Figure 1: Storage architecture in Cassandra.*",
      "translationMarkdown": "**数据组织。** 分布式 KV store 将 KV pair 划分到一个节点集群中。在 Cassandra 中，KV pair 基于一致性哈希 [33] 进行划分；其他生产级分布式 KV store [25, 41, 54, 60] 也采用了这一方法。一致性哈希将所有节点的位置与一个哈希环关联起来，并将每个 KV pair 确定性地映射到某个节点。具体来说，考虑一个具有 $n$ 个物理节点的分布式 KV store，每个物理节点关联 $v$ 个虚拟节点。它将哈希环划分为 $n \\times v$ 个范围，每个范围覆盖一个虚拟节点。例如，如图 1 所示，共有 $n = 5$ 个物理节点（即 $N_0$ 到 $N_4$），每个物理节点具有 $v = 2$ 个虚拟节点。哈希环包含 $2 \\times 5 = 10$ 个范围，即 $(0-10)$、$(11-20)$、$\\cdots$、$(91-100)$。每个范围都与哈希环顺时针方向上距离最近的虚拟节点及其对应的物理节点相关联；例如，范围 $(0-10)$ 和 $(51-60)$ 都分配给 $N_0$。对于每个 KV pair，一致性哈希将 key 哈希到哈希环中的一个位置（例如，Cassandra 使用 MurmurHash [6]）。随后，该 KV pair 被存储到与该范围关联的相应物理节点中。\n\n现代分布式 KV store [3, 14, 25, 28, 37, 50] 通常使用复制来实现容错，即将每个 KV pair 的副本分布到不同节点上，以防止节点故障。在 Cassandra 中，副本存储在沿哈希环顺时针方向排列的一系列节点中，记为 $N_i$、$N_{(i+1) \\bmod n}$、$N_{(i+2) \\bmod n}$、$\\cdots$，其中 $0 \\leq i \\leq n-1$，而 $N_i$（即该节点序列中的第一个节点）是依据一致性哈希将该 KV pair 哈希到的节点。我们把存储在 $N_i$ 中的副本称为主副本（primary copy），把其余存储在哈希环顺时针方向后续物理节点中的副本称为冗余副本（redundant copies）。\n\n**使用 LSM-tree 的内部存储。** 每个节点在内部使用某种索引结构管理 KV pair。具体来说，LSM-tree [48] 是分布式 KV store 中最常用的索引结构之一，Cassandra 以及其他系统 [3, 20, 28, 41, 50, 60] 都采用了它。LSM-tree KV store 将 KV pair 组织成一棵多层树，并在每一层内按 key 排序 KV pair，从而支持高效的读、写和扫描。如图 1 所示，LSM-tree KV store 维护一个具有多个层级（记为 $L_0$、$L_1$、$\\cdots$）且各层容量递增的树状索引结构，每层以称为 SSTable 的文件为单位存储 KV pair。它首先将写入的 KV pair 追加到磁盘上的预写日志（WAL），并将其插入内存中的 MemTable。当 MemTable 填满时，LSM-tree KV store 将其转变为 immutable MemTable，后者会作为一个 SSTable 被 flush 到最低层 $L_0$。当较低层达到容量上限时，LSM-tree KV store 通过 compaction 将该层中的 KV pair 合并到下一个更高层。为使每层内的 KV pair 保持有序，一次 compaction 操作首先从这两个层级读取 KV pair，合并已排序的 KV pair，再写回排序后的 KV pair。因此，compaction 在写入期间产生额外 I/O，导致写放大。此外，由于不同层之间的 KV pair 并非整体有序，读取一个 KV pair 时需要从最低层 $L_0$ 向更高层搜索，导致读放大。已有研究表明，写放大和读放大都会造成 LSM-tree KV store 的显著性能下降 [12, 43, 51]。\n\n*图 1：Cassandra 的存储架构。*"
    },
    {
      "id": "050-i-o-workflows",
      "title": "2.2 I/O Workflows",
      "titleTranslation": "2.2 I/O 工作流",
      "level": 2,
      "originalMarkdown": "**Write workflow.** Writing a KV pair in Cassandra works as follows. A client first randomly selects and connects to one of the nodes, called the coordinator and sends it the KV pair. The coordinator determines the nodes in which the primary and redundant copies are stored, based on consistent hashing. It then forwards the KV pairs to the nodes.\n\n**Read workflow.** Reading a KV pair in Cassandra is similar to writing a KV pair and works as follows. The client first selects and contacts a coordinator. It issues the read request to the coordinator, which finds the nodes in which the replicas (regardless of primary and redundant copies) of the KV pair are stored. For load balancing, the coordinator prefers to read the KV pair from the nodes with low latencies, determined by the dynamic snitching module [5]. It then returns the KV pair to the client.",
      "translationMarkdown": "**写工作流。** 在 Cassandra 中写入一个 KV pair 的过程如下。客户端首先随机选择并连接到一个称为 coordinator 的节点，然后将 KV pair 发送给它。coordinator 根据一致性哈希确定存储主副本和冗余副本的节点。随后，它将 KV pair 转发给这些节点。\n\n**读工作流。** 在 Cassandra 中读取一个 KV pair 与写入 KV pair 类似，过程如下。客户端首先选择并联系一个 coordinator。它向 coordinator 发出读请求，coordinator 找到存储该 KV pair 副本（无论是主副本还是冗余副本）的节点。为了实现负载均衡，coordinator 倾向于从低延迟节点读取 KV pair，这些节点由 dynamic snitching 模块 [5] 确定。随后，它将 KV pair 返回给客户端。"
    },
    {
      "id": "060-consistency-management",
      "title": "2.3 Consistency Management",
      "titleTranslation": "2.3 一致性管理",
      "level": 2,
      "originalMarkdown": "Cassandra supports different consistency modes, e.g., strong consistency and eventual consistency. They are configured by tuning the replication factor (denoted by $k$), as well as the read consistency level (RCL) and the write consistency level (WCL). The replication factor $k$ is defined as the total number of replicas for fault tolerance. RCL and WCL are defined as the minimum numbers of replicas (regardless of primary or redundant copies) to be read and written by the coordinator to acknowledge the successful read and write operations, respectively. Both RCL and WCL are set as an integer from one to $k$. If $WCL+RCL>k$, then strong consistency is provided; if $WCL+RCL \\leq k$, then eventual consistency is provided. By default, both WCL and RCL are set to one in Cassandra.",
      "translationMarkdown": "Cassandra 支持不同的一致性模式，例如强一致性和最终一致性。通过调整复制因子（记为 $k$）、读一致性级别（RCL）和写一致性级别（WCL）来配置这些模式。复制因子 $k$ 定义为用于容错的副本总数。RCL 和 WCL 分别定义为 coordinator 为确认读操作和写操作成功而必须读取和写入的最少副本数（无论是主副本还是冗余副本）。RCL 和 WCL 都被设为一到 $k$ 之间的整数。如果 $WCL+RCL>k$，则提供强一致性；如果 $WCL+RCL \\leq k$，则提供最终一致性。在 Cassandra 中，WCL 和 RCL 默认都设为 1。"
    },
    {
      "id": "070-replica-decoupling",
      "title": "3 Replica Decoupling",
      "titleTranslation": "3 副本解耦",
      "level": 1,
      "originalMarkdown": "To motivate replica decoupling, we describe the limitations of uniform indexing for managing all replicas in internal storage management (§3.1). We also describe the naïve replica decoupling designs to motivate our DEPART design (§3.2).",
      "translationMarkdown": "为了阐明副本解耦的动机，我们描述了在内部存储管理中使用统一索引管理所有副本的局限性（§3.1）。我们还描述了朴素的副本解耦设计，以引出 DEPART 的设计（§3.2）。"
    },
    {
      "id": "080-uniform-indexing-and-its-limitations",
      "title": "3.1 Uniform Indexing and its Limitations",
      "titleTranslation": "3.1 统一索引及其局限性",
      "level": 2,
      "originalMarkdown": "Recall from §1 that existing distributed KV stores (e.g., [3, 28, 37, 50]) mainly adopt uniform indexing, in which all replicas (including all primary and redundant copies) designated for each node are managed under the same index structure. We show that uniform indexing, rather than the extra writes from replication, is the main cause of significantly exacerbating both the write and read amplifications of the LSM-tree.\n\n**Limitation #1: Write amplification aggravation.** With uniform indexing, each node treats all replicas as the regular KV pairs and stores them in the same LSM-tree without distinction (Figure 1). To show how it exacerbates the write amplification, we evaluate the write amplifications of two open-source distributed KV stores, Cassandra (v3.11.4) [2] and TiKV (release 4.0) [50]. Specifically, we deploy Cassandra and TiKV on a 5-node cluster with their default settings (detailed in §5). We configure a client machine to issue the writes of 300 M KV pairs of size 1 KiB each to the cluster that initially has empty storage. We consider no-replication ($k = 1$), double replication ($k = 2$), and triple replication ($k = 3$). Figure 2(a) shows that no-replication incurs a write amplification of 6.5× for Cassandra, due to the compaction overhead caused by the LSM-tree. However, for triple replication, the write amplification increases to 25.7×, which is around 4× the write amplification of no-replication. We also observe a similar trend for TiKV, where the write amplification increases from 13.8× to 50.9× (i.e., 3.7× increase).\n\nAlso, as the KV store size increases, the write amplification increases more significantly and shows a super-linear trend. The reason is that a larger KV store size increases the number of levels in the LSM-tree, leading to higher compaction overhead and a larger write amplification. We configure a client machine to issue the writes of 100 M, 300 M, and 600 M KV pairs of size 1 KiB each to the initially empty cluster. Here, we focus on Cassandra. Figure 2(b) shows the write amplifications of Cassandra for different KV store sizes. For a larger data store, the increase of the write amplification under triple replication compared to no replication also becomes larger. For example, triple replication has 3.4× write amplification compared to no replication under 100 M KV pairs, and becomes 4.5× under 600 M KV pairs. This super-linear trend also implies that a larger KV store size can limit the scalability of uniform indexing.\n\n*Figure 2: Write amplifications of no-replication (“No”), double replication (“double”), and triple replication (“triple”) in Cassandra and TiKV.*\n\n**Limitation #2: Read amplification aggravation.** Uniform indexing also severely exacerbates the read amplification. The main reason is that all replicas are stored in the same LSM-tree, thereby enlarging the search space of KV pairs. We evaluate the read amplifications of Cassandra and TiKV as in the above settings, while a client machine issues 30 M reads to the existing 300 M KV pairs of size 1 KiB each that have already been stored. Figure 3(a) shows that for Cassandra, the read amplification increases from 7.8× in no-replication to 34.6× in triple replication (i.e., 4.4× increase). We observe a similar trend for TiKV.\n\nIn addition, we study the impact of the KV store size on the read amplification. Here, we focus on Cassandra. We first issue the writes of 100 M, 300 M, and 600 M KV pairs of size 1 KiB each to the initially empty cluster, followed by issuing 30 M reads to the existing KV pairs. Figure 3(b) shows a super-linear increase for the read amplification as the KV store size increases for Cassandra.\n\n*Figure 3: Read amplifications of no-replication (“No”), double replication (“double”), and triple replication (“triple”) in Cassandra and TiKV.*",
      "translationMarkdown": "回顾 §1，现有分布式 KV store（例如 [3, 28, 37, 50]）主要采用统一索引，即为每个节点指定的所有副本（包括所有主副本和冗余副本）都由同一个索引结构管理。我们表明，显著加剧 LSM-tree 写放大和读放大的主要原因是统一索引，而不是复制产生的额外写入。\n\n**局限 #1：写放大加剧。** 使用统一索引时，每个节点都把所有副本当作普通 KV pair，不加区分地存储在同一棵 LSM-tree 中（图 1）。为了说明它如何加剧写放大，我们评估了两个开源分布式 KV store——Cassandra（v3.11.4）[2] 和 TiKV（release 4.0）[50]——的写放大。具体而言，我们使用默认设置将 Cassandra 和 TiKV 部署在一个 5 节点集群上（详见 §5）。我们配置一台客户端机器，向初始存储为空的集群写入 3 亿个 KV pair，每个大小为 1 KiB。我们考虑不复制（$k = 1$）、双副本（$k = 2$）和三副本（$k = 3$）。图 2(a) 表明，由于 LSM-tree 引起的 compaction 开销，Cassandra 在不复制时产生 6.5× 的写放大。然而，在三副本情况下，写放大增加到 25.7×，约为不复制时写放大的 4 倍。我们在 TiKV 中也观察到了类似趋势，其写放大从 13.8× 增加到 50.9×（即增加 3.7 倍）。\n\n此外，随着 KV store 规模增大，写放大的增长更加显著，并呈现超线性趋势。原因在于，更大的 KV store 规模会增加 LSM-tree 的层数，从而带来更高的 compaction 开销和更大的写放大。我们配置一台客户端机器，分别向初始为空的集群写入 1 亿、3 亿和 6 亿个 KV pair，每个大小为 1 KiB。此处我们聚焦于 Cassandra。图 2(b) 展示了 Cassandra 在不同 KV store 规模下的写放大。对于更大的数据存储，三副本相对于不复制所增加的写放大也更大。例如，在 1 亿个 KV pair 下，三副本的写放大是不复制的 3.4 倍；在 6 亿个 KV pair 下，这一倍数变为 4.5。这一超线性趋势也意味着，更大的 KV store 规模会限制统一索引的可扩展性。\n\n*图 2：Cassandra 和 TiKV 在不复制（“No”）、双副本（“double”）和三副本（“triple”）情况下的写放大。*\n\n**局限 #2：读放大加剧。** 统一索引也会严重加剧读放大。主要原因是所有副本都存储在同一棵 LSM-tree 中，从而扩大了 KV pair 的搜索空间。我们采用上述设置评估 Cassandra 和 TiKV 的读放大，同时由一台客户端机器针对已存储的 3 亿个、每个大小为 1 KiB 的 KV pair 发起 3000 万次读取。图 3(a) 表明，对于 Cassandra，读放大从不复制时的 7.8× 增加到三副本时的 34.6×（即增加 4.4 倍）。我们在 TiKV 中观察到了类似趋势。\n\n此外，我们还研究了 KV store 规模对读放大的影响。此处我们聚焦于 Cassandra。我们首先向初始为空的集群分别写入 1 亿、3 亿和 6 亿个 KV pair，每个大小为 1 KiB，随后针对已有 KV pair 发起 3000 万次读取。图 3(b) 表明，随着 KV store 规模增大，Cassandra 的读放大呈超线性增长。\n\n*图 3：Cassandra 和 TiKV 在不复制（“No”）、双副本（“double”）和三副本（“triple”）情况下的读放大。*"
    },
    {
      "id": "090-motivation",
      "title": "3.2 Motivation",
      "titleTranslation": "3.2 动机",
      "level": 2,
      "originalMarkdown": "Our analysis in §3.1 shows that uniform indexing exacerbates both write and read amplifications, as it is costly to manage all replicas within a single LSM-tree. This motivates us to explore the potentials of replica decoupling, which decouples the primary and redundant copies of replicas and manage them in separate index structures. We first consider two naïve replica decoupling approaches, and then motivate our design.\n\n**Naïve approaches.** A simple replica decoupling approach is to deploy two LSM-trees, one for primary copies and one for all redundant copies. However, the LSM-tree for redundant copies still has a large size (especially for a large replication factor), while not all redundant copies are accessed in each I/O operation. For example, to recover a single-node failure under triple replication, only half of the redundant copies on average are accessed. Thus, there are extra I/Os for searching the whole LSM-tree for a subset of redundant copies.\n\nAnother simple replica decoupling approach is to manage $k$ LSM-trees ($k$ is the replication factor) for $k$ replicas derived from each KV pair. For example, for Cassandra with triple replication, node $N_i$ receives the redundant copies whose corresponding primary copies are stored in nodes $N_{(i-1) \\bmod n}$ and $N_{(i-2) \\bmod n}$ (where $0 \\leq i \\leq n-1$ and $n$ is the number of physical nodes). Then we use three LSM-trees in node $N_i$, one of which stores the primary copies and the other two store the redundant copies from nodes $N_{(i-1) \\bmod n}$ and $N_{(i-2) \\bmod n}$, respectively.\n\nHowever, maintaining multiple LSM-trees incurs both significant memory and I/O overheads. Since each LSM-tree has its own MemTable and immutable MemTable, the memory overhead amplifies by $k$ times for the replication factor $k$. Specifically, if the MemTable size is $m$ MiB and the cluster size is $n$, the memory cost of Cassandra is $m \\times n$ MiB as each node maintains a single LSM-tree. However, when using $k$ LSM-trees in each node, the memory cost becomes $k \\times m \\times n$ MiB, which is $k$ times that in Cassandra. Note that if we reduce the MemTable size for each LSM-tree to limit the memory overhead, it degrades the efficiency of flushing the MemTable to disk, thereby degrading the user write performance [7, 8].\n\nAlso, each LSM-tree incurs its own compaction overhead for maintaining the fully-sorted ordering in each level. Thus, the compaction overhead is still significant and the compaction operations of multiple LSM-trees in the same node compete for the disk bandwidth, and hence the overall I/O performance is compromised. Our evaluation (Exp#1 in §5.2) shows that replica decoupling with multiple LSM-trees only brings limited performance gains over uniform indexing, even though the replicas are managed by different LSM-trees.\n\n**Our approach.** Recall that the LSM-tree always maintains the fully-sorted ordering in each level. Using a single LSM-tree for all replicas in uniform indexing, or using multiple LSM-trees for replica decoupling, may favor high read performance, but both of them incur substantial high compaction overhead that degrades write performance. In particular, different consistency levels imply different performance requirements for the reads and writes issued to the replicas, such that a high read (or write) consistency level requires high read (or write) performance for the replicas. This motivates us to design a new storage management solution that supports tunable ordering for replica decoupling, so as to balance the read and write performance.",
      "translationMarkdown": "我们在 §3.1 中的分析表明，由于在单棵 LSM-tree 中管理所有副本的成本很高，统一索引会同时加剧写放大和读放大。这促使我们探索副本解耦的潜力：将副本中的主副本与冗余副本解耦，并使用不同的索引结构管理它们。我们首先考虑两种朴素的副本解耦方法，再由此引出我们的设计。\n\n**朴素方法。** 一种简单的副本解耦方法是部署两棵 LSM-tree，一棵用于主副本，另一棵用于所有冗余副本。然而，用于冗余副本的 LSM-tree 规模仍然很大（尤其是在复制因子较大时），而每次 I/O 操作并不会访问所有冗余副本。例如，在三副本配置下恢复单节点故障时，平均只会访问一半的冗余副本。因此，为了寻找冗余副本的一个子集而搜索整棵 LSM-tree，会产生额外 I/O。\n\n另一种简单的副本解耦方法是使用 $k$ 棵 LSM-tree（$k$ 为复制因子）来管理由每个 KV pair 派生出的 $k$ 个副本。例如，对于采用三副本的 Cassandra，节点 $N_i$ 会接收相应主副本存储在节点 $N_{(i-1) \\bmod n}$ 和 $N_{(i-2) \\bmod n}$ 中的冗余副本（其中 $0 \\leq i \\leq n-1$，$n$ 为物理节点数）。随后，我们在节点 $N_i$ 中使用三棵 LSM-tree，其中一棵存储主副本，另外两棵分别存储来自节点 $N_{(i-1) \\bmod n}$ 和 $N_{(i-2) \\bmod n}$ 的冗余副本。\n\n然而，维护多棵 LSM-tree 会产生显著的内存和 I/O 开销。由于每棵 LSM-tree 都有自己的 MemTable 和 immutable MemTable，对于复制因子 $k$，内存开销会扩大到 $k$ 倍。具体来说，如果 MemTable 大小为 $m$ MiB、集群规模为 $n$，由于每个节点只维护一棵 LSM-tree，Cassandra 的内存成本为 $m \\times n$ MiB。然而，当每个节点使用 $k$ 棵 LSM-tree 时，内存成本变为 $k \\times m \\times n$ MiB，是 Cassandra 的 $k$ 倍。需要注意的是，如果通过减小每棵 LSM-tree 的 MemTable 大小来限制内存开销，就会降低将 MemTable flush 到磁盘的效率，从而降低用户写性能 [7, 8]。\n\n此外，每棵 LSM-tree 为了维持每层内的完全有序状态，都会产生自身的 compaction 开销。因此，compaction 开销仍然显著，并且同一节点内多棵 LSM-tree 的 compaction 操作会竞争磁盘带宽，进而损害整体 I/O 性能。我们的评估（§5.2 中的 Exp#1）表明，即使副本由不同的 LSM-tree 管理，采用多棵 LSM-tree 的副本解耦相对于统一索引也只能带来有限的性能提升。\n\n**我们的方法。** 回顾一下，LSM-tree 始终在每一层内维持完全有序状态。无论是在统一索引中使用单棵 LSM-tree 管理所有副本，还是使用多棵 LSM-tree 实现副本解耦，都可能有利于获得较高的读性能，但两者都会产生很高的 compaction 开销，从而降低写性能。特别是，不同的一致性级别意味着对副本所执行的读写具有不同的性能要求：较高的读（或写）一致性级别要求副本具备较高的读（或写）性能。这促使我们设计一种新的存储管理方案，为副本解耦支持可调节的有序性，从而平衡读写性能。"
    },
    {
      "id": "100-depart-design",
      "title": "4 DEPART Design",
      "titleTranslation": "4 DEPART 设计",
      "level": 1,
      "originalMarkdown": "We present DEPART, a distributed KV store that builds on Cassandra to realize replica decoupling by separating the storage management of primary and redundant copies. We introduce its architecture (§4.1) and elaborate its design techniques (§4.2-§4.5).",
      "translationMarkdown": "我们提出 DEPART，一个基于 Cassandra 构建的分布式 KV store。它通过分离 primary copy 与 redundant copy 的存储管理来实现 replica decoupling。我们先介绍其体系结构（§4.1），再详细阐述其设计技术（§4.2-§4.5）。"
    },
    {
      "id": "110-overall-architecture",
      "title": "4.1 Overall Architecture",
      "titleTranslation": "4.1 整体架构",
      "level": 2,
      "originalMarkdown": "DEPART decouples the storage management of primary and redundant copies to achieve high performance. It manages the primary copies in the LSM-tree, while managing the redundant copies in a novel two-layer log, whose ordering of redundant copies is tunable depending on the performance requirements. Keeping only the primary copies in the LSM-tree maintains the design features of the LSM-tree for reads, writes, and scans, but in a more lightweight manner as the LSM-tree size is now significantly smaller without the redundant copies. Also, the tunable ordering of the two-layer log allows balanced read and write performance for different settings of consistency levels.\n\n*Figure 4: DEPART architecture.*\n\nFigure 4 depicts the architecture of DEPART. Note that DEPART only modifies the internal storage module of each Cassandra node, but preserves the inter-node management in Cassandra (e.g., consistent hashing for data organization and consistency management). In summary, DEPART addresses several design challenges via a number of techniques.\n\n- **Lightweight replica differentiation.** DEPART differentiates the primary and redundant copies in the storage module of each node for separate management. Its replica differentiation is lightweight based on simple hash computations, and incurs limited overhead on the critical I/O path (§4.2).\n- **Two-layer log design.** DEPART manages the redundant copies with a two-layer log, so as to achieve fast writes and efficient recovery. It first appends redundant copies to a global log as sequential batched writes. It then splits the global log into multiple local logs in background (§4.3).\n- **Tunable ordering.** DEPART further provides a tunable ordering scheme for the two-layer log design to adjust the degree of ordering of the redundant copies with a single parameter, so as to balance the read and write performance for accessing the redundant copies (§4.4).\n- **Parallel recovery.** DEPART uses a parallel recovery scheme that reads and writes the primary and redundant copies in parallel during recovery, so as to achieve high recovery performance (§4.5).",
      "translationMarkdown": "DEPART 将 primary copy 与 redundant copy 的存储管理解耦，以获得高性能。它在 LSM-tree 中管理 primary copy，同时在一种新颖的 two-layer log 中管理 redundant copy；其中，redundant copy 的有序程度可以根据性能要求进行调节。LSM-tree 中仅保留 primary copy，既维持了 LSM-tree 面向读、写和扫描的设计特性，又使其更加轻量，因为移除 redundant copy 后，LSM-tree 的规模显著减小。此外，two-layer log 的可调有序性允许在不同一致性级别设置下平衡读写性能。\n\n*图 4：DEPART 架构。*\n\n图 4 展示了 DEPART 的架构。DEPART 只修改每个 Cassandra 节点的内部存储模块，而保留 Cassandra 的节点间管理机制（例如，用于数据组织的一致性哈希以及一致性管理）。概括来说，DEPART 通过以下多项技术解决若干设计挑战。\n\n- **轻量级副本区分。** DEPART 在每个节点的存储模块中区分 primary copy 和 redundant copy，以便分别管理。它的副本区分机制基于简单的哈希计算，十分轻量，并且在关键 I/O 路径上仅产生有限开销（§4.2）。\n- **Two-layer log 设计。** DEPART 使用 two-layer log 管理 redundant copy，以实现快速写入和高效恢复。它首先通过顺序批量写将 redundant copy 追加到 global log，随后在后台把 global log 拆分成多个 local log（§4.3）。\n- **可调有序性。** DEPART 进一步为 two-layer log 提供可调有序性方案，通过单个参数调整 redundant copy 的有序程度，从而平衡访问 redundant copy 时的读写性能（§4.4）。\n- **并行恢复。** DEPART 使用并行恢复方案，在恢复过程中并行读写 primary copy 和 redundant copy，从而获得较高的恢复性能（§4.5）。"
    },
    {
      "id": "120-replica-differentiation",
      "title": "4.2 Replica Differentiation",
      "titleTranslation": "4.2 副本区分",
      "level": 2,
      "originalMarkdown": "DEPART differentiates the written KV pairs in the storage module of each node as primary or redundant copies. Figure 5 depicts the replica differentiation workflow. Recall that the coordinator forwards *k* replicas of a KV pair to a sequence of *k* nodes along the clockwise direction in the hash ring, where the key of the KV pair is hashed to the first node in the node sequence (§2). When a node, say *N*, receives one of the replicas of the KV pair from the coordinator, it performs the same hash computation (i.e., MurmurHash [6] in Cassandra) on the key of the replica and determines the node to which the key is hashed. If the resulting node is the same as *N* itself, then *N* is the first node in the node sequence and we refer to the replica as a primary copy; otherwise, we refer to the replica as a redundant copy.\n\n*Figure 5: Replica differentiation in DEPART.*\n\nEach node maintains a write-ahead log (WAL) and a MemTable for the LSM-tree (for primary copies) and the two-layer log (for redundant copies). After a node differentiates whether the KV pair is a primary copy or a redundant copy, it writes the KV pair to the corresponding WAL and MemTable and acknowledges the coordinator. When the MemTable is full and becomes immutable, the node flushes the immutable MemTable to either the LSM-tree or the two-layer log.\n\nThe logic of replica differentiation is lightweight, as it requires one extra hash computation in each storage node in the critical I/O path (and *k* extra computations in total for the replication factor *k*). Our experiments show that the differentiation time is less than 0.4% of the total write time (Exp#5 in §5.2).",
      "translationMarkdown": "DEPART 在每个节点的存储模块中，将写入的 KV pair 区分为 primary copy 或 redundant copy。图 5 展示了副本区分的工作流程。回顾一下，coordinator 会沿哈希环顺时针方向，将一个 KV pair 的 *k* 个副本转发给由 *k* 个节点组成的序列，其中该 KV pair 的 key 被哈希到节点序列中的第一个节点（§2）。当某个节点（假设为 *N*）从 coordinator 接收到该 KV pair 的一个副本时，它对副本的 key 执行相同的哈希计算（即 Cassandra 中的 MurmurHash [6]），并确定该 key 被哈希到哪个节点。如果所得节点就是 *N* 本身，那么 *N* 是节点序列中的第一个节点，我们把该副本称为 primary copy；否则，把它称为 redundant copy。\n\n*图 5：DEPART 中的副本区分。*\n\n每个节点都为 LSM-tree（用于 primary copy）和 two-layer log（用于 redundant copy）分别维护一个 write-ahead log（WAL）和一个 MemTable。节点区分 KV pair 属于 primary copy 还是 redundant copy 后，会将该 KV pair 写入相应的 WAL 和 MemTable，并向 coordinator 返回确认。当 MemTable 写满并变为 immutable 时，节点会将该 immutable MemTable flush 到 LSM-tree 或 two-layer log。\n\n副本区分逻辑十分轻量，因为它在关键 I/O 路径上的每个存储节点中只需额外执行一次哈希计算（当复制因子为 *k* 时，总计额外执行 *k* 次计算）。实验表明，区分副本所用时间不足总写入时间的 0.4%（§5.2 中的 Exp#5）。"
    },
    {
      "id": "130-two-layer-log-design",
      "title": "4.3 Two-layer Log Design",
      "titleTranslation": "4.3 Two-layer Log 设计",
      "level": 2,
      "originalMarkdown": "Each node maintains a two-layer log, which is designed for the management of redundant copies with the following design features. First, it supports fast writes for the redundant copies, even though the number of redundant copies is much larger than that of primary copies and increases with the replication factor. Second, it supports tunable ordering to adapt to different consistency levels (§4.4). Third, it supports efficient parallel recovery of any failed nodes by allowing fast reads to the redundant copies in parallel.\n\nFigure 6 shows the architecture of the two-layer log in each node. Upon receiving the replicas, a node first issues sequential batched writes for the redundant copies into a global log. A background thread continuously retrieves the redundant copies from the global log and splits them into multiple local logs. We elaborate the global log and local log designs below.\n\n*Figure 6: Architecture of the two-layer log design.*",
      "translationMarkdown": "每个节点都维护一个 two-layer log，它面向 redundant copy 的管理而设计，并具有以下特征。第一，即使 redundant copy 的数量远多于 primary copy，并随复制因子的增大而增加，它仍支持对 redundant copy 的快速写入。第二，它支持可调有序性，以适应不同的一致性级别（§4.4）。第三，它允许对 redundant copy 进行快速并行读取，从而支持对任意失效节点的高效并行恢复。\n\n图 6 展示了每个节点内 two-layer log 的架构。接收到副本后，节点首先通过顺序批量写将 redundant copy 写入 global log。一个后台线程持续从 global log 中取出 redundant copy，并将它们拆分到多个 local log 中。下面详细介绍 global log 和 local log 的设计。\n\n*图 6：Two-layer log 设计的架构。*"
    },
    {
      "id": "140-append-only-global-log",
      "title": "Append-only global log",
      "titleTranslation": "仅追加的 Global Log",
      "level": 3,
      "originalMarkdown": "To enable fast writes, each node writes all redundant copies of KV pairs (flushed from the immutable MemTable) to an append-only global log. All redundant copies are grouped in units of segments and appended to the head of the global log as sequential batched writes. Note that the global log only stores all redundant copies without maintaining any extra index structure. Thus, it achieves high write performance for the redundant copies.\n\nKeeping all redundant copies in the global log achieves high write performance, but poses two issues. First, the recovery performance degrades. For the redundant copies in the global log, their corresponding primary copies may reside in different nodes. When a node failure happens, only part of the redundant copies in the global log (i.e., the redundant copies whose corresponding primary copies reside in the failed node) are needed for recovery. Thus, recovery incurs only partial access to the global log, thereby incurring lots of random I/Os. Second, the garbage collection cost increases. As new KV pairs are appended to the log head, invalid (or stale) KV pairs cannot be overwritten and hence they occupy lots of space. This incurs large storage overhead, especially in update-intensive workloads. Garbage collection can be used to reduce the storage cost by continuously reclaiming the free space of invalid KV pairs from the log tail, but it inevitably introduces large amount of extra I/Os to read segments from the log tail and write back the valid KV pairs to the log head.",
      "translationMarkdown": "为了实现快速写入，每个节点将 KV pair 的所有 redundant copy（从 immutable MemTable flush 而来）写入一个仅追加的 global log。所有 redundant copy 以 segment 为单位分组，并通过顺序批量写追加到 global log 的头部。global log 仅存储所有 redundant copy，不维护任何额外索引结构。因此，它能为 redundant copy 提供较高的写入性能。\n\n将所有 redundant copy 保存在 global log 中虽然能获得较高的写入性能，但会带来两个问题。第一，恢复性能会下降。global log 中各 redundant copy 对应的 primary copy 可能位于不同节点。当某个节点发生故障时，恢复只需要 global log 中的一部分 redundant copy（即对应 primary copy 位于失效节点中的那些 redundant copy）。因此，恢复过程只会局部访问 global log，从而产生大量随机 I/O。第二，垃圾回收成本会增加。随着新的 KV pair 被追加到日志头部，无效（或陈旧）的 KV pair 无法被覆盖，因而占用大量空间。这会造成很高的存储开销，在更新密集型工作负载下尤其如此。垃圾回收可以通过不断回收日志尾部无效 KV pair 所占的空闲空间来降低存储成本，但它不可避免地引入大量额外 I/O：需要从日志尾部读取 segment，并将其中有效的 KV pair 写回日志头部。"
    },
    {
      "id": "150-splitting-into-local-logs",
      "title": "Splitting into local logs",
      "titleTranslation": "拆分为 Local Log",
      "level": 3,
      "originalMarkdown": "To enable fast recovery, DEPART maintains a background thread to continuously split the global log into multiple local logs, each of which keeps only the redundant copies whose corresponding primary copies are stored in the same node. This allows the recovery of any failed node to access only the local log associated with the failed node. Note that each node only needs to maintain *k* − 1 local logs (recall that *k* is the replication factor), since consistent hashing distributes the replicas in a sequence of nodes along the clockwise direction in the hash ring and each node only stores a redundant copy from up to *k* − 1 nodes.\n\nThe splitting operation works as follows. It first retrieves a configurable number of segments, collectively called a split, from the tail of the global log. It then reorganizes a split of redundant copies into multiple sub-splits, each of which contains only the redundant copies whose corresponding primary copies reside in the same node. It finally writes back each sub-split into a separate local log in an append-only manner, and issues the writes to different local logs in parallel.\n\nDuring splitting, DEPART also discards any invalid KV pairs in the selected segments. Thus, it does not trigger garbage collection explicitly; instead, it realizes garbage collection in the splitting operation to save the extra I/Os.\n\nFor each redundant copy, each node needs to determine the node in which its corresponding primary copy resides. It can be feasibly done locally within a node based on replica differentiation (§4.2).",
      "translationMarkdown": "为了实现快速恢复，DEPART 维护一个后台线程，持续将 global log 拆分成多个 local log；每个 local log 只保存其对应 primary copy 存储在同一节点上的那些 redundant copy。这样，在恢复任意失效节点时，只需访问与该失效节点关联的 local log。每个节点只需维护 *k* − 1 个 local log（*k* 为复制因子），因为一致性哈希沿哈希环顺时针方向将副本分布到一个节点序列中，而每个节点最多只会存储来自 *k* − 1 个节点的 redundant copy。\n\n拆分操作如下。首先，它从 global log 尾部取出可配置数量的 segment，这些 segment 合称一个 split。随后，它将一个由 redundant copy 构成的 split 重新组织为多个 sub-split；每个 sub-split 只包含对应 primary copy 位于同一节点的那些 redundant copy。最后，它以仅追加方式将每个 sub-split 分别写回一个独立的 local log，并并行向不同 local log 发起写入。\n\n拆分期间，DEPART 还会丢弃所选 segment 中的所有无效 KV pair。因此，它不会显式触发垃圾回收，而是在拆分操作中完成垃圾回收，从而节省额外 I/O。\n\n对于每个 redundant copy，每个节点都需要确定其对应 primary copy 所在的节点。利用副本区分机制（§4.2），这一过程可以在节点本地方便地完成。"
    },
    {
      "id": "160-range-based-grouping-within-local-logs",
      "title": "Range-based grouping within local logs",
      "titleTranslation": "Local Log 内基于范围的分组",
      "level": 3,
      "originalMarkdown": "While splitting the global log into multiple local logs alleviates the recovery and garbage collection overhead, the benefit remains limited since the ranges of a hash ring stored in each node are not necessarily contiguous (e.g., in Figure 1, Node N0 stores ranges [0,10] and [51,60]). Recovering any range of KV pairs only needs to access the redundant copies for the range, so it still causes partial accesses to a local log and issues random I/Os.\n\nWe enhance each local log by managing KV pairs with range-based grouping. Figure 7 shows the idea of range-based grouping. Each local log is further divided into multiple range groups, each of which corresponds to a range in the hash ring. Note that different range groups within each local log have no overlaps in keys, so they can be managed independently. For example, for Node N2 in Figure 7, the local log LOG0 stores the redundant copies whose corresponding primary copies reside in Node N0. As Node N0 has two ranges, [0,10] and [51,60], LOG0 now contains two range groups, each of which holds the redundant copies for [0,10] and [51,60], respectively. Range-based grouping can be realized by comparing the keys (or their hashes) with the boundary of each range in the hash ring based on consistent hashing (§2.1). It still ensures that the writes to each range group in a local log are performed in an append-only, batched manner. The number of range groups in a local log, and hence the number of ranges in Cassandra, are configurable by the parameter num_tokens [2]. Range-based grouping improves the recovery performance by accessing only the KV pairs in the corresponding range groups without accessing all KV pairs in the whole local log.\n\n*Figure 7: Range-based grouping within local logs.*\n\nUnder range-based grouping, when writing KV pairs from the global log to the local logs during a splitting operation, DEPART further sorts all KV pairs by keys for each range before storing the KV pairs in a range group in the local logs; we call the sorted KV pairs for a range in a splitting operation a sorted run. Thus, each range group may store multiple sorted runs, while different sorted runs in the same range group may have overlaps in keys. Managing the range groups by sorted runs makes tunable ordering feasible (§4.4).",
      "translationMarkdown": "将 global log 拆分为多个 local log 虽能减轻恢复和垃圾回收开销，但收益仍然有限，因为每个节点所存储的哈希环范围不一定连续（例如在图 1 中，节点 N0 存储范围 [0,10] 和 [51,60]）。恢复任意一个范围的 KV pair 时，只需要访问该范围对应的 redundant copy，因此仍会局部访问某个 local log，并产生随机 I/O。\n\n我们使用基于范围的分组来管理 KV pair，从而增强每个 local log。图 7 展示了这一思想。每个 local log 被进一步划分为多个 range group，每个 range group 对应哈希环中的一个范围。每个 local log 中不同 range group 的 key 互不重叠，因此可以独立管理。例如，对于图 7 中的节点 N2，local log LOG0 存储对应 primary copy 位于节点 N0 的 redundant copy。由于节点 N0 拥有 [0,10] 和 [51,60] 两个范围，LOG0 现在包含两个 range group，分别保存 [0,10] 和 [51,60] 的 redundant copy。基于一致性哈希（§2.1），通过将 key（或其哈希值）与哈希环上各范围的边界比较，即可实现基于范围的分组。它仍保证以仅追加、批量的方式向 local log 中的每个 range group 写入。local log 中 range group 的数量，也就是 Cassandra 中范围的数量，可以通过参数 num_tokens [2] 配置。基于范围的分组只访问相应 range group 中的 KV pair，而无须访问整个 local log 中的所有 KV pair，因此能提升恢复性能。\n\n*图 7：Local log 内基于范围的分组。*\n\n采用基于范围的分组时，在拆分操作期间将 KV pair 从 global log 写入 local log 的过程中，DEPART 会先按 key 对每个范围内的所有 KV pair 进一步排序，然后再将这些 KV pair 存入 local log 的某个 range group。我们把一次拆分操作中某个范围内经过排序的 KV pair 称为一个 sorted run。因此，每个 range group 可以存储多个 sorted run，而同一 range group 中不同 sorted run 的 key 可能相互重叠。以 sorted run 管理 range group，使可调有序性成为可能（§4.4）。"
    },
    {
      "id": "170-reads-from-the-two-layer-log",
      "title": "Reads from the two-layer log",
      "titleTranslation": "从 Two-layer Log 读取",
      "level": 3,
      "originalMarkdown": "To read a KV pair from the two-layer log, DEPART first checks the segments in the global log one by one, starting with the latest one. Note that the internal structure of each segment is similar to that of SSTables in the LSM-tree, so DEPART first reads the metadata from the segment and reads the corresponding KV pair according to the offset in the metadata. If the KV pair is not found in the global log, then DEPART searches the corresponding range group, located by comparing the key with the boundary keys of the range groups. Since each range group contains multiple sorted runs and KV pairs within the sorted run are fully sorted, DEPART searches from the latest to the oldest sorted run, and uses binary search to find the key within a sorted run.",
      "translationMarkdown": "为了从 two-layer log 中读取 KV pair，DEPART 首先从最新 segment 开始，逐一检查 global log 中的 segment。每个 segment 的内部结构与 LSM-tree 中的 SSTable 相似，因此 DEPART 先从 segment 中读取 metadata，再根据 metadata 中的 offset 读取对应的 KV pair。如果在 global log 中未找到该 KV pair，DEPART 就会搜索相应的 range group；通过比较 key 与各 range group 的边界 key，可以定位这个 range group。由于每个 range group 包含多个 sorted run，而每个 sorted run 内部的 KV pair 完全有序，因此 DEPART 按从新到旧的顺序搜索 sorted run，并在每个 sorted run 内使用二分查找来寻找该 key。"
    },
    {
      "id": "180-tunable-ordering",
      "title": "4.4 Tunable Ordering",
      "titleTranslation": "4.4 可调有序性",
      "level": 2,
      "originalMarkdown": "Recall that each range group in a local log may contain multiple sorted runs, and the KV pairs across the sorted runs within a range group are not fully sorted. If a range group contains too many sorted runs, the read performance for the redundant copies will degrade, especially for high read consistency levels where both primary and redundant copies are accessed in a read operation (§2.3). Thus, we extend the two-layer log with a tunable ordering scheme, in which users can configure a single parameter to adjust the degree of ordering of each range group for different consistency requirements.\n\nDEPART adjusts the degree of ordering across multiple sorted runs with a user-configurable threshold *S*, which is a positive integer that controls the maximum number of sorted runs being allowed to exist in each range group. Figure 8 shows the idea of the tunable ordering scheme. For each new sorted run generated from a splitting operation, DEPART first checks if the existing number of sorted runs in a range group reaches the threshold. If not, it appends the new sorted run from the splitting operation directly to the range group; otherwise, it merge-sorts the new sorted run with the existing ones into a single sorted run. The merge-sort operation is similar to the compaction operation in the LSM-tree, including three steps: (i) it reads all existing sorted runs from the range group; (ii) it merges all KV pairs in the new sorted run and the existing sorted runs, and if there exist multiple KV pairs with the same key in different sorted runs, it keeps only the KV pair in the latest sorted run and discards the older ones; and (iii) it writes back all merged KV pairs into the range group. We consider two special cases for different values of *S*.\n\n*Figure 8: Tunable ordering.*\n\n- **Case 1: *S* = 1.** In this case, each range group always sorts the incoming sorted run with the currently stored sorted run, and hence all KV pairs are sorted. Thus, each local log resembles a single-level LSM-tree.\n- **Case 2: *S* approaches infinity.** In this case, DEPART always appends the new sorted run into a range group without any merge-sorting with any existing sorted runs.\n\nTo set an appropriate value of *S*, we note that *S* determines the trade-off between the read and write performance. A small *S* favors the read performance by keeping a small number of sorted runs in a range group. It also maintains the storage efficiency by discarding the invalid KV pairs in merge-sort operations. However, it incurs a large merge-sort overhead that degrades the write performance. Thus, to support a system setting with the high read consistency level under read-dominant workloads, we should set a high degree of ordering with a small *S* to benefit reads; otherwise, we should decrease the degree of ordering by increasing the value of *S* to benefit writes. We also evaluate the impact of different values of *S* via experiments, and we recommend a default setting, *S* = 20, that can effectively balance the read and write performance under different consistency configurations (see Exp#8 in §5.2).",
      "translationMarkdown": "回顾一下，local log 中的每个 range group 可能包含多个 sorted run，而一个 range group 内跨 sorted run 的 KV pair 并非完全有序。如果某个 range group 包含过多 sorted run，redundant copy 的读取性能就会下降；在较高的读一致性级别下尤其如此，因为一次读操作会同时访问 primary copy 和 redundant copy（§2.3）。因此，我们为 two-layer log 扩展了一种可调有序性方案。在该方案中，用户可以配置单个参数，针对不同的一致性需求调整每个 range group 的有序程度。\n\nDEPART 使用用户可配置的阈值 *S* 调整多个 sorted run 之间的有序程度。*S* 是一个正整数，用于控制每个 range group 中允许存在的 sorted run 的最大数量。图 8 展示了可调有序性方案的思想。对于拆分操作产生的每个新 sorted run，DEPART 首先检查 range group 中已有 sorted run 的数量是否达到阈值。如果没有达到，就把新 sorted run 直接追加到 range group；否则，就将新 sorted run 与已有 sorted run 归并排序为一个 sorted run。归并排序操作与 LSM-tree 中的 compaction 相似，包含三个步骤：(i) 从 range group 中读取所有已有 sorted run；(ii) 合并新 sorted run 与已有 sorted run 中的全部 KV pair；如果不同 sorted run 中存在多个 key 相同的 KV pair，则只保留最新 sorted run 中的 KV pair，并丢弃更旧的 KV pair；(iii) 将所有合并后的 KV pair 写回 range group。对于 *S* 的不同取值，我们考虑两种特殊情况。\n\n*图 8：可调有序性。*\n\n- **情况 1：*S* = 1。** 此时，每个 range group 总会将新到达的 sorted run 与当前存储的 sorted run 一起排序，因此所有 KV pair 都是有序的。于是，每个 local log 类似于单层 LSM-tree。\n- **情况 2：*S* 趋近于无穷大。** 此时，DEPART 总是将新 sorted run 追加到 range group，而不会与任何已有 sorted run 进行归并排序。\n\n为了设置合适的 *S* 值，需要注意 *S* 决定了读写性能之间的权衡。较小的 *S* 使 range group 中只保留较少的 sorted run，因而有利于读取性能。它还会在归并排序操作中丢弃无效 KV pair，从而维持存储效率。但是，它会产生很高的归并排序开销，导致写入性能下降。因此，对于读密集型工作负载下采用较高读一致性级别的系统设置，应使用较小的 *S* 来提高有序程度，从而有利于读取；否则，应增大 *S* 来降低有序程度，从而有利于写入。我们还通过实验评估了不同 *S* 值的影响，并推荐默认设置 *S* = 20；在不同的一致性配置下，该设置能够有效平衡读写性能（参见 §5.2 中的 Exp#8）。"
    },
    {
      "id": "190-parallel-recovery",
      "title": "4.5 Parallel Recovery",
      "titleTranslation": "4.5 并行恢复",
      "level": 2,
      "originalMarkdown": "For fast recovery of any failed node, DEPART proposes a parallel recovery scheme that exploits the benefit of decoupling the storage management of primary and redundant copies. We first review the recovery process in the current Cassandra implementation. Cassandra currently does not have a centralized node to monitor data loss and coordinate data recovery. Instead, it maintains a Merkle tree [4, 47] in each node to detect data inconsistency among multiple copies. Note that Merkle trees are also used by other consistent-hashing-based distributed KV stores, such as Dynamo [25] and Riak [54].\n\nA Merkle tree is a binary hash tree, in which each leaf node stores the hash value of a range of KV pairs, while each non-leaf node stores the hash value of its child nodes. If a KV pair is lost, the replicas of the KV pair become inconsistent as detected by the Merkle trees across the nodes that store the replicas, so Cassandra triggers a recovery process. Specifically, the recovery process has three steps: (i) building a Merkle tree for each range of KV pairs in each node; (ii) comparing the Merkle trees of the same range of KV pairs in different nodes to identify any inconsistent range of KV pairs (which implies data loss); and (iii) reconstructing any inconsistent range by retrieving the range of KV pairs from a non-failed node and sending the range of KV pairs to the recovered node.\n\nDEPART parallelizes the read and write processes for recovering multiple ranges of KV pairs for fast recovery. Its parallel recovery process is based on the recovery workflow in Cassandra, as shown in Figure 9. Suppose that we recover the lost data of a failed node at node N0. First, each node in DEPART retrieves the KV pairs from the LSM-tree and the two-layer log, and builds its own Merkle tree (note that the Merkle tree in N0 is initially empty) (Step 1). N0 compares the Merkle trees and identifies the missing KV pairs (Step 2). To recover the lost KV pairs, each surviving node (e.g., node N1 in Figure 9) issues parallel reads to the primary and redundant copies with two threads, and similarly the new node (i.e., N0) retrieves the KV pairs from other surviving nodes and issues parallel writes for the primary and redundant copies with two threads. Such multi-threading is feasible as the primary and redundant copies are stored in different index structures.\n\n*Figure 9: Parallel recovery in DEPART.*",
      "translationMarkdown": "为了快速恢复任意失效节点，DEPART 提出了一种并行恢复方案，利用了 primary copy 与 redundant copy 的存储管理相互解耦这一优势。我们首先回顾当前 Cassandra 实现中的恢复过程。Cassandra 目前没有用于监测数据丢失并协调数据恢复的中心节点。相反，它在每个节点中维护一棵 Merkle tree [4, 47]，用于检测多个副本之间的数据不一致。其他基于一致性哈希的分布式 KV store（例如 Dynamo [25] 和 Riak [54]）也使用 Merkle tree。\n\nMerkle tree 是一种二叉哈希树，其中每个叶节点存储一个 KV pair 范围的哈希值，而每个非叶节点存储其子节点的哈希值。如果某个 KV pair 丢失，存储其副本的各节点上的 Merkle tree 会检测到该 KV pair 的副本变得不一致，于是 Cassandra 触发恢复过程。具体来说，恢复过程分为三个步骤：(i) 在每个节点中，为 KV pair 的每个范围构建一棵 Merkle tree；(ii) 比较不同节点中同一 KV pair 范围的 Merkle tree，以识别所有不一致的 KV pair 范围（这意味着发生了数据丢失）；(iii) 从某个未失效节点取回该范围的 KV pair，并将该范围的 KV pair 发送到被恢复节点，从而重建每个不一致的范围。\n\n为了实现快速恢复，DEPART 将多个 KV pair 范围恢复过程中的读写并行化。如图 9 所示，它的并行恢复过程建立在 Cassandra 的恢复工作流之上。假设我们在节点 N0 上恢复某个失效节点丢失的数据。首先，DEPART 中的每个节点从 LSM-tree 和 two-layer log 中取回 KV pair，并构建自己的 Merkle tree（N0 中的 Merkle tree 最初为空）（步骤 1）。N0 比较这些 Merkle tree，并识别丢失的 KV pair（步骤 2）。为了恢复丢失的 KV pair，每个存活节点（例如图 9 中的节点 N1）使用两个线程分别对 primary copy 和 redundant copy 发起并行读取；类似地，新节点（即 N0）从其他存活节点取回 KV pair，并使用两个线程分别对 primary copy 和 redundant copy 发起并行写入。由于 primary copy 和 redundant copy 存储在不同的索引结构中，因此这种多线程处理是可行的。\n\n*图 9：DEPART 中的并行恢复。*"
    },
    {
      "id": "200-evaluation",
      "title": "5 Evaluation",
      "titleTranslation": "5 评估",
      "level": 1,
      "originalMarkdown": "DEPART builds on the codebase of Cassandra v3.11.4 [2] by implementing replica decoupling in the storage module of each node. Our DEPART prototype itself contains 6.9 K LoC, while the modification to Cassandra contains 1.9 K LoC. Note that Cassandra v3.11.4 contains about 206.2 K LoC. To demonstrate the benefits of the two-layer log design in DEPART, we also implement the naïve replica decoupling approach that simply stores replicas in multiple LSM-trees, which we refer to as mLSM (§3.2).\n\nWe conduct testbed experiments to demonstrate the efficiency of DEPART. We compare our DEPART prototype with Cassandra (v3.11.4), which performs uniform indexing for all replicas, mLSM. We address the following questions.\n\n• How is the overall performance of DEPART compared with Cassandra and mLSM under different settings, e.g., the microbenchmark performance in different types of KV operations, the performance under different consistency configurations and different replication factors, as well as the performance under YCSB core workloads [21, 22]? (Experiments 1-4)\n\n• What are the performance breakdowns of DEPART and Cassandra? (Experiment 5)\n\n• What is the performance of DEPART when a node failure occurs? (Experiments 6-7)\n\n• How does the performance of DEPART vary across parameter settings, including the ordering degree S, the store sizes, and the numbers of storage nodes? (Experiments 8-10)",
      "translationMarkdown": "DEPART 基于 Cassandra v3.11.4 [2] 的代码库构建，通过在每个节点的存储模块中实现副本解耦完成。DEPART 原型本身包含 6.9 K 行代码，而对 Cassandra 的修改包含 1.9 K 行代码。请注意，Cassandra v3.11.4 约包含 206.2 K 行代码。为了展示 DEPART 中两层日志设计的优势，我们还实现了朴素的副本解耦方法，即直接将副本存储在多个 LSM-tree 中，称为 mLSM（§3.2）。\n\n我们进行测试平台实验来展示 DEPART 的效率。我们将 DEPART 原型与对所有副本执行统一索引的 Cassandra（v3.11.4）以及 mLSM 进行比较。我们回答以下问题。\n\n• 在不同设置下，例如不同类型 KV 操作的微基准性能、不同一致性配置与复制因子下的性能，以及 YCSB 核心工作负载 [21, 22] 下的性能，DEPART 与 Cassandra 和 mLSM 相比总体表现如何？（实验 1–4）\n\n• DEPART 和 Cassandra 的性能时间分解如何？（实验 5）\n\n• 节点发生故障时，DEPART 的性能如何？（实验 6–7）\n\n• 在不同参数设置（包括有序度 S、存储规模和存储节点数量）下，DEPART 的性能如何变化？（实验 8–10）"
    },
    {
      "id": "210-setup",
      "title": "5.1 Setup",
      "titleTranslation": "5.1 实验设置",
      "level": 2,
      "originalMarkdown": "**Testbed.** We conduct all experiments on a local cluster of multiple machines, each of which has two 12-core Intel(R) Xeon(R) CPU E5-2650 v4 @ 2.20 GHz, 32 GiB RAM, and a 500 GiB Samsung 860 EVO SATA SSD. All machines are interconnected via a 10 Gb/s Ethernet switch. Each machine runs CentOS 7.6.1810, with the 64-bit Linux kernel 3.10.0 and the Ext4 file system. We use one machine to simulate multiple clients via a thread pool, while the remaining machines serve as storage nodes.\n\n**Workloads.** We generate workloads with YCSB [21,22], a general-purpose cloud system benchmark tool. By default, we focus on 1 KiB KV pairs with 24-byte keys, and generate requests based on the Zipf distribution with the default Zipfian constant 0.99. We deploy YCSB on the client machine and set the number of client threads as 50, while each client thread issues a workload from YCSB.\n\n**Default settings.** We configure five storage nodes in the cluster and triple replication to deploy Cassandra and DEPART. Before each experiment, the cluster has empty storage. By default, we set (WCL=1, RCL=1) (i.e., the default setting in Cassandra), which corresponds to eventual consistency. We also study the impact of different consistency levels (Experiments 1 and 2). Both Cassandra and DEPART use the default dynamic snitching module [5] to choose the fastest nodes for serving reads, so as to load-balance reads across different replicas. For the parameter num tokens [2], which determines the number of range groups, we use the default value 256 as in Cassandra.\n\nFor DEPART, we set the MemTable size to be the same as that of Cassandra (160 MiB by default), and the segment size in the global log to be the same as the MemTable size. Since DEPART keeps an extra MemTable for the two-layer log, we increase the row cache size of Cassandra by 160 MiB for fair comparisons. For the two-layer log, we set the data size of each split operation as 20 segments (around 3 GiB) and set S as 20 to achieve balanced read and write performance. We keep the other parameter settings in Cassandra unchanged. We plot the average results over five runs, with error bars showing the standard deviation.",
      "translationMarkdown": "**测试平台。** 我们在由多台机器组成的本地集群上进行所有实验。每台机器配有两颗 12 核 Intel(R) Xeon(R) CPU E5-2650 v4 @ 2.20 GHz、32 GiB RAM 和一块 500 GiB Samsung 860 EVO SATA SSD。所有机器通过 10 Gb/s 以太网交换机互连。每台机器运行 CentOS 7.6.1810、64 位 Linux 3.10.0 内核和 Ext4 文件系统。我们使用一台机器通过线程池模拟多个客户端，其余机器作为存储节点。\n\n**工作负载。** 我们使用通用云系统基准工具 YCSB [21,22] 生成工作负载。默认情况下，我们关注键为 24 字节的 1 KiB KV 对，并根据 Zipf 分布生成请求，默认 Zipfian 常数为 0.99。我们将 YCSB 部署在客户端机器上，将客户端线程数设为 50，每个客户端线程执行一个来自 YCSB 的工作负载。\n\n**默认设置。** 我们在集群中配置五个存储节点并采用三副本来部署 Cassandra 和 DEPART。每次实验前，集群的存储为空。默认设置为 (WCL=1, RCL=1)（即 Cassandra 的默认设置），对应最终一致性。我们也研究不同一致性级别的影响（实验 1 和 2）。Cassandra 和 DEPART 都使用默认的动态 snitching 模块 [5] 选择最快的节点来处理读请求，从而在不同副本之间均衡读负载。对于决定范围组数量的参数 num tokens [2]，我们采用 Cassandra 的默认值 256。\n\n对于 DEPART，我们将 MemTable 大小设为与 Cassandra 相同（默认为 160 MiB），并将全局日志中的 segment 大小设为与 MemTable 相同。由于 DEPART 为两层日志额外保留一个 MemTable，为公平比较，我们将 Cassandra 的 row cache 增大 160 MiB。对于两层日志，我们将每次 split 操作的数据量设为 20 个 segment（约 3 GiB），并将 S 设为 20，以取得均衡的读写性能。Cassandra 的其他参数设置保持不变。图中绘制五次运行结果的平均值，误差条表示标准差。"
    },
    {
      "id": "220-results",
      "title": "5.2 Results",
      "titleTranslation": "5.2 实验结果",
      "level": 2,
      "originalMarkdown": "**Experiment 1 (Performance in KV operations).** We first compare the performance of Cassandra, mLSM, and DEPART in different KV operations, including writes (i.e., writing new KV pairs), reads (i.e., reading existing KV pairs), scans (i.e., reading existing consecutive KV pairs), and updates (i.e., updating existing KV pairs). We configure the client machine to first randomly write 200 M KV pairs. It then issues the following requests in order: (i) 20 M reads, (ii) 2 M scans (each scan contains one seek() to locate the first key and then iterates with 100 next()’s), and (iii) 200 M updates. We also consider two settings of consistency levels: (i) (WCL=3, RCL=1) (i.e., strong consistency) and (ii) (WCL=1, RCL=1) (i.e., eventual consistency).\n\n*Figure 10: Exp#1 (Performance in KV operations).*\n\nFigure 10 shows the throughput and latency results. First, DEPART improves the overall performance over Cassandra in all cases. For (WCL=3, RCL=1), DEPART increases the throughput of writes, reads, scans, and updates to 1.42×, 2.29×, 2.22×, and 1.45×, respectively; it reduces the average write latency, average read latency, 99-th percentile write latency, and 99-th percentile read latency by 29%, 58%, 39%, and 41%, respectively. For (WCL=1, RCL=1), DEPART improves the throughput of writes, reads, scans, and updates to 1.43×, 2.43×, 2.68×, and 1.44×, respectively; it reduces the average write latency, average read latency, 99-th percentile write latency, and 99-th percentile read latency by 30%, 59%, 41%, and 48%, respectively. The latency results for scans and updates are similar and we omit the results here. The main reasons of the performance improvements of DEPART are two-fold. First, for reads, DEPART only searches the LSM-tree or the specific range group in the two-layer log within a node, thereby greatly reducing the search space. Second, for writes, DEPART mitigates the compaction overhead in the LSM-tree, which now keeps the primary copies only. The two-layer log also has limited merge-sort overhead by having a large value of the ordering degree S.\n\nSecond, mLSM notably improves the read performance, but only shows marginal improvements on writes. Specifically, compared with Cassandra, it increases the throughput of writes, reads, scans, and updates to 1.18-1.20×, 2.11-2.20×, 1.85-2.0×, and 1.15-1.20×, respectively. It also reduces the average write latency, average read latency, 99-th percentile write latency, and 99-th percentile read latency by 15-17%, 53-56%, 16-18%, and 34-41%, respectively. The reason is that mLSM still triggers frequent compaction operations, which compete for disk bandwidth and degrade the write performance. For example, the total compaction sizes of Cassandra and mLSM are 3.46 TiB and 2.72 TiB, respectively, and the total compaction and merge-sort size of DEPART is 1.65 TiB (i.e., compared with Cassandra, mLSM only reduces the total compaction size by 21%, but DEPART reduces it by 52%).\n\n*Figure 11: Exp#1 (Read and write amplifications).*\n\nWe next compare the storage and memory costs of Cassandra, mLSM, and DEPART. After the end of the update phase, the KV store sizes are 613.5 GiB for Cassandra, 611.3 GiB for mLSM, and 654.8 GiB for DEPART. DEPART incurs 6.7% additional storage overhead compared with Cassandra, since each range group allows at most S = 20 sorted runs in our default setting and contains invalid KV pairs before being merge-sorted. To measure the memory overhead, we note that the MemTable size varies over time (up to the 160 MiB limit) as the KV pairs are continuously inserted into a MemTable and flushed to disk when the MemTable is full. Thus, we measure the total memory usage of the MemTables every five seconds and obtain the average results. The total memory usage of mLSM is 335.7 MiB, which is 3.7× that of Cassandra (90.4 MiB), as each LSM-tree maintains a MemTable. However, DEPART only costs 183.9 MiB, which is 2.0× that of Cassandra, since DEPART only maintains one extra MemTable for the two-layer log. Note that the total memory usage of mLSM increases with the replication factor, while that of DEPART remains unaffected.\n\nFinally, we compare the read/write amplifications (i.e., the ratios between the amounts of system reads/writes and the amounts of the user reads/writes) of Cassandra, mLSM, and DEPART. Figure 11 shows the results. Compared with Cassandra, mLSM reduces the read and write amplifications by up to 40% and 24%, respectively, while DEPART reduces the read and write amplifications by up to 53% and 52%, respectively. Note that the performance gain of DEPART is similar under both consistency levels, so we focus on (WCL=1, RCL=1) in the following experiments (except Exp#2 and 8).\n\n**Experiment 2 (Performance under different consistency configurations).** We evaluate the performance under different consistency configurations. In particular, for strong consistency, we consider additional configurations for WCL and RCL under triple replication that satisfy the condition WCL+RCL>3, including (WCL=2, RCL=2) and (WCL=1, RCL=3).\n\n*Figure 12: Exp#2 (Performance under different consistency configurations).*\n\nFigure 12 shows the results. DEPART consistently improves the throughput of writes, reads, scans, and updates over Cassandra under different consistency configurations. Specifically, for (WCL=2, RCL=2), DEPART increases the throughput of writes, reads, scans, and updates to 1.43×, 1.70×, 1.38×, and 1.44×, respectively. For (WCL=1, RCL=3), DEPART increases the throughput of writes, reads, scans, and updates to 1.44×, 1.72×, 1.62×, 1.45×, respectively. DEPART also consistently improves the throughput of writes and updates over mLSM. Note that the write performance gains of DEPART over Cassandra stay nearly the same under different consistency configurations, since the index structures of both Cassandra and DEPART remain unchanged under triple replication.\n\nHowever, the read performance gains of DEPART over Cassandra become smaller, and DEPART’s read performance is worse than mLSM for RCL≥2. In this case, each read request needs to access at least two replicas successfully, so the redundant copies in the two-layer log must be searched. As the redundant copies in the two-layer log are not fully sorted, the performance is slower than reading the primary copies in the LSM-tree. Nevertheless, DEPART still achieves faster reads than Cassandra, as it searches for less data than Cassandra. Also, mLSM keeps redundant copies being fully sorted in each level, so it achieves higher read performance than DEPART. On the other hand, for RCL=1, each read only needs to access one replica for a successful operation. Most of the reads are routed to their primary copies, whose read latency is smaller than that of the redundant copies as determined by the dynamic snitching module (§2.2). Thus, the read performance gains under RCL=1 are higher than those under RCL≥2 in general.\n\n**Experiment 3 (Performance under different replication factors).** We evaluate the performance of DEPART by varying the replication factor k from 3 to 5. We configure the client machine to first randomly write 200 M KV pairs and then issue 20 M reads.\n\n*Figure 13: Exp#3 (Performance under different replication factors).*\n\nFigure 13 shows the throughput results of writes and reads versus the replication factor. Compared with Cassandra, DEPART increases the throughput of writes and reads to 1.43-1.59× and 2.43-3.61×, respectively. Also, DEPART achieves a higher throughput gain for a larger replication factor. The main reasons are two-fold. First, for reads, DEPART either reads primary copies from the LSM-tree or reads redundant copies from the two-layer log; for the latter, it only searches the global log and the corresponding range group in the two-layer log. Thus, the read performance of DEPART is less affected by the number of replicas. However, Cassandra stores all replicas in a single LSM-tree and its reads need to traverse the whole LSM-tree. Its read performance drops significantly as the replication factor increases. Second, for writes, DEPART implements replica decoupling and manages the redundant copies in range groups. When the number of replicas increases, the compaction cost in the LSM-tree remains unchanged and the merge-sort cost in the two-layer log increases only slightly. However, Cassandra stores all replicas in the single LSM-tree and the compaction cost increases significantly as the number of replicas increases. Combining both reasons, the performance gain of DEPART becomes larger for a higher replication factor.\n\nSimilar to DEPART, mLSM also consistently improves the throughput of writes and reads over Cassandra under different replication factors. When the replication factor increases to k = 4 and k = 5, its read performance is even better than DEPART. The main reason is that mLSM only searches the corresponding LSM-tree that is fully sorted in each level regardless of the replication factor, but DEPART keeps redundant copies in the two-layer log that is not fully sorted under the default setting. Note that we can tune the degree of ordering of the two-layer log to further increase the read performance. Furthermore, the memory usage of DEPART remains 2× that of Cassandra, but that of mLSM increases to 5× when the replication factor increases to k = 5.\n\n**Experiment 4 (YCSB performance).** We compare Cassandra, mLSM, and DEPART using the six YCSB core workloads [21,22], namely A (50% reads, 50% writes), B (95% reads, 5% writes), C (100% reads), D (95% reads, 5% writes), E (95% scans, 5% writes), and F (50% reads, 50% read-modify-writes). The client machine first randomly writes 200 M KV pairs to the cluster before running each of the six YCSB core workloads. Each workload consists of 100 M operations, except for Workload E, which contains 10 M operations with each scan involving 100 next()’s.\n\n*Figure 14: Exp#4 (YCSB performance).*\n\nFigure 14 shows the results. DEPART outperforms Cassandra under all workloads. Specifically, it increases the throughput to 1.4-2.1× under read-dominant Workloads B-D, 1.6-2.2× under write-dominant Workloads A and F, and 2.4× under scan-dominant Workload E. With replica decoupling and the two-layer log design, DEPART reduces the compaction overhead of the LSM-tree during writes and reduces the search space during reads, so it improves both read and write performance simultaneously. On the other hand, mLSM also outperforms Cassandra under all workloads due to replica decoupling. However, DEPART further improves the performance of mLSM, as the latter incurs large compaction overhead.\n\n**Experiment 5 (Time breakdown for reads/writes).** We show the time breakdown for both read and write processes in Cassandra and DEPART. We configure the client machine to first load 200 M KV pairs, followed by issuing 20 M reads. The read process comprises the reads to the MemTable, the cache (including the row cache and key cache), the index block of an SSTable (e.g., the Bloom filters and offsets), and the data block in the SSTable. Note that each segment in the two-layer log is treated as an SSTable here. The write process comprises writes to the WAL, writes to the MemTable, flushing the MemTable, the compaction of the LSM-tree, and the merge-sorts of the two-layer log (in DEPART only).\n\n*Figure 15: Exp#5 (Time breakdown for reads/writes).*\n\nFigure 15 shows the time breakdown for reads and writes. For reads, most of the read time is for reading the index blocks of SSTables in both Cassandra and DEPART, since reading a KV pair needs to check the Bloom filter in the index block in each LSM-tree level to determine if the KV pair exists, and reads the data block from the SSTable according to the offset in the index block only if it does. Overall, DEPART reduces the time costs of reading the index blocks and the data blocks of SSTables by 56% and 45%, respectively. The reasons are two-fold. First, DEPART stores only the primary copies in the LSM-tree, so the number of SSTables in the LSM-tree greatly decreases. Also, DEPART manages the redundant copies in range groups, so the number of reads for locating a KV pair decreases as well.\n\nFor writes, DEPART reduces the time costs of writes to the WAL and writes to the MemTable by 28% and 37%, respectively, as DEPART writes primary and redundant copies in parallel. DEPART also greatly reduces the compaction overhead by reducing the LSM-tree size; for example, its compaction time is only 30.7% of Cassandra’s. Furthermore, the merge-sort time of the two-layer log in DEPART is only 21.4% of the compaction time in Cassandra. Thus, the total time of compaction and merge-sorts in DEPART is only 52.1% of the compaction time in Cassandra.\n\n**Experiment 6 (Recovery performance).** We evaluate the recovery performance on recovering a failed node. We consider different write sizes, by configuring the client machine to randomly write 20 M, 50 M, and 100 M KV pairs to the cluster. We then crash one node, by killing the KV store process with the “kill -s processID” command and removing all its data with the “rm -r data” command. Finally, we restart the KV store process on the same node and call the “nodetool repair -full keyspacename” command for recovery.\n\n*Figure 16: Exp#6 (Recovery performance).*\n\nFigure 16(a) shows the total recovery time. Cassandra takes 8.9, 26.9, and 72.4 minutes to recover 20 M, 50 M, and 100 M KV pairs, respectively, while DEPART only takes 4.1, 15.4, and 45.2 minutes, respectively. Overall, DEPART reduces the recovery time of Cassandra by 38-54%. The main reason is that DEPART repairs primary and redundant copies in parallel, and scans much less data during recovery due to replica decoupling.\n\nFigure 16(b) also shows the breakdown results of the recovery time for repairing 50 M and 100 M KV pairs. We consider different steps: Build MTs (i.e., building Merkle trees for all nodes), Compare MTs (i.e., comparing all Merkle trees), Receive&Write (i.e., receiving repaired data from other nodes and writing data to disk), and Others (i.e., other operations in recovery). DEPART reduces the time costs of Build MTs and Receive&Write by nearly a half compared to Cassandra through parallelizing the read/write processes to the primary and redundant copies.\n\n**Experiment 7 (Performance when a node crashes).** We evaluate the read, write and update performance when a node crashes and before it is repaired. The client machine first randomly writes 100 M KV pairs to the cluster and we manually crash one node as in Experiment 6. We then issue 20 M reads, 100 M writes, and 100 M updates.\n\n*Figure 17: Exp#7 (Performance when a node crashes).*\n\nFigure 17(a) shows the throughput under a node failure. Compared with Cassandra, DEPART increases the throughput of reads, writes, and updates to 1.69×, 1.59×, and 1.55×, respectively. The main reason is that DEPART always searches much less data than Cassandra, even though it reads the redundant copies from the two-layer log. Also, DEPART always improves write performance under both normal and failure modes.\n\nWe also evaluate the degraded read performance in different cases: (i) the node repair is not yet triggered, (ii) the node repair is in progress, and (iii) the two-layer log for redundant copies in each node supports a higher degree of ordering by decreasing the threshold S (§4.4) from the default value 20 to 5. Figure 17(b) shows the degraded read throughput. When node repair is not triggered or is in progress, DEPART improves the throughput of degraded reads to 1.69× and 1.75×, respectively, since DEPART always searches much less data compared to uniform indexing in Cassandra. Also, when the two-layer log has a higher degree of ordering (e.g., with a smaller threshold S), the degraded read performance gains of DEPART become larger, because it is more efficient to read the KV pairs in the two-layer log that with a high degree of ordering.\n\n**Experiment 8 (Impact of the ordering degree S).** We evaluate the write and read performance under different settings of the ordering degree S in DEPART, so as to show how DEPART can balance the read and write performance gains for the redundant copies by tuning the value of S. The client machine first randomly writes 200 M KV pairs to the cluster, followed by issuing 20 M reads. Here, we use the consistency configuration (WCL=2, RCL=2), so that the redundant copies must be accessed for each successful read.\n\n| S | Write thpt (KOPS) | Read thpt (KOPS) |\n|---:|---:|---:|\n| 1 | 37.2 | 42.3 |\n| 10 | 57.2 | 31.5 |\n| 20 | 64.7 | 23.1 |\n| → ∞ | 78.4 | 7.6 |\n| Cassandra | 45.4 | 15.4 |\n\n*Table 1: Exp#8 (Impact of the ordering degree S).*\n\nTable 1 shows the results. For DEPART, if S = 1, the two-layer log reduces to a two-level LSM-tree, so it achieves the highest read throughput as the KV pairs are fully sorted, but the write throughput is the least due to the frequent merge-sorts for maintaining a single sorted run in each range group in the local logs. As we increase S (e.g., S is 10 or 20), the ordering of the two-layer log is relaxed and hence the merge-sort overhead becomes smaller, so the write throughput increases. As we set S to be a sufficiently large value, the two-layer log reduces to the append-only log, so the write throughput is the highest, but the read throughput is the least. Note that when S is 1, 10, or 20, DEPART still maintains higher throughput in both writes and reads than Cassandra, even though there exists a performance trade-off between writes and reads in DEPART.\n\n**Experiment 9 (Impact of different KV store sizes).** We now evaluate the impact of different KV store sizes. We vary the data size written by the client from 200 M to 400 M KV pairs (i.e., the total amount of primary and redundant copies increases from 600 GiB to 1200 GiB under triple replication).\n\n*Figure 18: Exp#9 (Impact of different KV store sizes).*\n\nFigure 18 shows the throughput of writes and reads under different KV store sizes. Compared with Cassandra, DEPART improves the write and read throughput by 1.43-1.52× and 2.43-2.95×, respectively. Also, the performance gains of DEPART increase as the KV store size increases, as DEPART alleviates the write and read amplifications via replica decoupling, but Cassandra aggravates the write and read amplifications via uniform indexing.\n\nTo better show the scalability of the two-layer log design under different KV store sizes, we also evaluate the compaction time of the LSM-tree and the merge-sort time of the two-layer log in DEPART, and compare them with the compaction time in Cassandra. When the KV store size increases from 200 M to 400 M KV pairs, the compaction time in Cassandra increases 2.3×, while the total time of compaction and merge-sort operations in DEPART only increases 1.9×. In particular, the ratio of the merge-sort time in DEPART to the compaction time in Cassandra drops from 21.4% to 18.7%, and the ratio of the compaction time in DEPART to the compaction time in Cassandra drops from 30.7% to 25.2%. Thus, DEPART scales well as the KV store grows.\n\n**Experiment 10 (Impact of different numbers of nodes).** We evaluate the performance of DEPART when the cluster contains more nodes. We vary the number of nodes as 5, 8, and 10. We configure the client machine to issue the writes of 200 M, 320 M, and 400 M KV pairs, respectively, so that each node contains the same amount of data. After the writes, we configure the client machine to issue 20 M, 32 M, and 40 M reads, respectively.\n\n*Figure 19: Exp#10 (Impact of different numbers of nodes).*\n\nFigure 19 shows the throughput results of write and read operations. Compared with Cassandra, DEPART increases the throughput of writes and reads to 1.35-1.43× and 2.26-2.43×, respectively. DEPART maintains its performance gains over Cassandra via replica decoupling, regardless of the cluster size. Thus, DEPART achieves good scalability as the cluster size increases.",
      "translationMarkdown": "**实验 1（KV 操作性能）。** 我们首先比较 Cassandra、mLSM 和 DEPART 在不同 KV 操作中的性能，包括写入（即写入新的 KV 对）、读取（即读取已有 KV 对）、扫描（即读取已有的连续 KV 对）和更新（即更新已有 KV 对）。我们配置客户端机器先随机写入 200 M 个 KV 对，然后依次发出以下请求：(i) 20 M 次读取；(ii) 2 M 次扫描（每次扫描包含一次 seek() 以定位第一个键，随后执行 100 次 next() 迭代）；(iii) 200 M 次更新。我们还考虑两种一致性级别设置：(i) (WCL=3, RCL=1)（即强一致性）；(ii) (WCL=1, RCL=1)（即最终一致性）。\n\n*图 10：实验 1（KV 操作性能）。*\n\n图 10 展示吞吐量和延迟结果。首先，在所有情形下，DEPART 的总体性能都优于 Cassandra。对于 (WCL=3, RCL=1)，DEPART 将写、读、扫描和更新吞吐量分别提高到 1.42×、2.29×、2.22× 和 1.45×；将平均写延迟、平均读延迟、写延迟第 99 百分位和读延迟第 99 百分位分别降低 29%、58%、39% 和 41%。对于 (WCL=1, RCL=1)，DEPART 将写、读、扫描和更新吞吐量分别提高到 1.43×、2.43×、2.68× 和 1.44×；将上述四项延迟分别降低 30%、59%、41% 和 48%。扫描和更新的延迟结果类似，因此不在此展示。DEPART 性能提升主要有两个原因。第一，对于读取，DEPART 在节点内只搜索 LSM-tree 或两层日志中的特定范围组，从而大幅缩小搜索空间。第二，对于写入，DEPART 降低了 LSM-tree 的 compaction 开销，因为其中现在只保留主副本。较大的有序度参数 S 也使两层日志的 merge-sort 开销有限。\n\n其次，mLSM 显著改善读性能，但对写入仅有小幅提升。具体而言，与 Cassandra 相比，它将写、读、扫描和更新吞吐量分别提高到 1.18–1.20×、2.11–2.20×、1.85–2.0× 和 1.15–1.20×；并将平均写延迟、平均读延迟、写延迟第 99 百分位和读延迟第 99 百分位分别降低 15–17%、53–56%、16–18% 和 34–41%。原因是 mLSM 仍会频繁触发 compaction，争夺磁盘带宽并降低写性能。例如，Cassandra 和 mLSM 的 compaction 总量分别为 3.46 TiB 和 2.72 TiB，而 DEPART 的 compaction 与 merge-sort 总量为 1.65 TiB（即相较 Cassandra，mLSM 仅将 compaction 总量降低 21%，DEPART 则降低 52%）。\n\n*图 11：实验 1（读放大和写放大）。*\n\n接下来比较 Cassandra、mLSM 和 DEPART 的存储与内存成本。更新阶段结束后，三者的 KV store 大小分别为 613.5 GiB、611.3 GiB 和 654.8 GiB。DEPART 比 Cassandra 产生 6.7% 的额外存储开销，因为默认设置下每个范围组最多允许 S = 20 个 sorted run，并且在执行 merge-sort 之前包含无效 KV 对。由于 KV 对不断插入 MemTable，并在其写满时刷盘，MemTable 大小会随时间变化（上限 160 MiB）。因此，我们每五秒测量一次所有 MemTable 的总内存用量并取平均。mLSM 的总内存用量为 335.7 MiB，是 Cassandra（90.4 MiB）的 3.7×，因为每棵 LSM-tree 都维护一个 MemTable。DEPART 仅使用 183.9 MiB，是 Cassandra 的 2.0×，因为它只为两层日志额外维护一个 MemTable。mLSM 的总内存用量随复制因子增加，而 DEPART 不受影响。\n\n最后，我们比较三者的读/写放大（即系统读/写量与用户读/写量之比）。图 11 展示结果。与 Cassandra 相比，mLSM 最多分别降低 40% 和 24% 的读放大与写放大，而 DEPART 最多分别降低 53% 和 52%。DEPART 在两种一致性级别下的性能增益相近，因此后续实验（实验 2 和 8 除外）聚焦于 (WCL=1, RCL=1)。\n\n**实验 2（不同一致性配置下的性能）。** 我们评估不同一致性配置下的性能。对于强一致性，我们还考虑三副本下满足 WCL+RCL>3 的 WCL 和 RCL 配置，包括 (WCL=2, RCL=2) 和 (WCL=1, RCL=3)。\n\n*图 12：实验 2（不同一致性配置下的性能）。*\n\n图 12 展示结果。在不同一致性配置下，DEPART 的写、读、扫描和更新吞吐量始终优于 Cassandra。对于 (WCL=2, RCL=2)，分别提高到 1.43×、1.70×、1.38× 和 1.44×；对于 (WCL=1, RCL=3)，分别提高到 1.44×、1.72×、1.62× 和 1.45×。DEPART 的写和更新吞吐量也始终优于 mLSM。由于三副本下 Cassandra 和 DEPART 的索引结构保持不变，因此不同一致性配置下 DEPART 相对 Cassandra 的写性能增益几乎相同。\n\n然而，DEPART 相对 Cassandra 的读性能增益会减小，并且在 RCL≥2 时其读性能不如 mLSM。此时每个读请求必须成功访问至少两个副本，因此必须搜索两层日志中的冗余副本。由于两层日志中的冗余副本并非完全有序，其性能慢于读取 LSM-tree 中的主副本。尽管如此，DEPART 搜索的数据比 Cassandra 少，因而读取仍然更快。mLSM 在每层中保持冗余副本完全有序，因此读性能高于 DEPART。另一方面，当 RCL=1 时，一次读取只需成功访问一个副本。大多数读取由动态 snitching 模块（§2.2）路由到主副本，其读延迟小于冗余副本。因此，一般而言 RCL=1 下的读性能增益高于 RCL≥2。\n\n**实验 3（不同复制因子下的性能）。** 我们将复制因子 k 从 3 调到 5，以评估 DEPART 的性能。客户端先随机写入 200 M 个 KV 对，再执行 20 M 次读取。\n\n*图 13：实验 3（不同复制因子下的性能）。*\n\n图 13 展示写、读吞吐量随复制因子变化的结果。与 Cassandra 相比，DEPART 将写和读吞吐量分别提高到 1.43–1.59× 和 2.43–3.61×，且复制因子越大，吞吐量增益越高。原因有二。第一，读取时 DEPART 要么从 LSM-tree 读取主副本，要么从两层日志读取冗余副本；后者只搜索全局日志及对应范围组，因此其读性能受副本数量影响较小。Cassandra 将所有副本存入一棵 LSM-tree，读取必须遍历整棵树，故读性能随复制因子增加显著下降。第二，写入时 DEPART 实现副本解耦，并在范围组中管理冗余副本。副本数量增加时，LSM-tree 的 compaction 成本不变，两层日志的 merge-sort 成本仅略有增加；Cassandra 则将所有副本存入单棵 LSM-tree，其 compaction 成本显著增加。综合两点，复制因子越高，DEPART 的性能增益越大。\n\n与 DEPART 类似，mLSM 在不同复制因子下也始终提高 Cassandra 的写、读吞吐量。当复制因子增至 k = 4 和 k = 5 时，其读性能甚至优于 DEPART。主要原因是无论复制因子如何，mLSM 只搜索对应的、每层完全有序的 LSM-tree，而默认设置下 DEPART 将冗余副本保存在并非完全有序的两层日志中。可以调整两层日志的有序度以进一步提高读性能。此外，DEPART 的内存用量保持为 Cassandra 的 2×，而 k = 5 时 mLSM 增至 5×。\n\n**实验 4（YCSB 性能）。** 我们使用六种 YCSB 核心工作负载 [21,22] 比较 Cassandra、mLSM 和 DEPART：A（50% 读、50% 写）、B（95% 读、5% 写）、C（100% 读）、D（95% 读、5% 写）、E（95% 扫描、5% 写）和 F（50% 读、50% read-modify-write）。运行每种工作负载前，客户端先向集群随机写入 200 M 个 KV 对。除工作负载 E 包含 10 M 次操作且每次扫描执行 100 次 next() 外，其余每种工作负载包含 100 M 次操作。\n\n*图 14：实验 4（YCSB 性能）。*\n\n图 14 展示结果。DEPART 在所有工作负载下均优于 Cassandra：在读密集型 B–D 下将吞吐量提高到 1.4–2.1×，在写密集型 A 和 F 下提高到 1.6–2.2×，在扫描密集型 E 下提高到 2.4×。凭借副本解耦和两层日志，DEPART 在写入时降低 LSM-tree 的 compaction 开销，在读取时缩小搜索空间，因此同时改善读写性能。mLSM 也因副本解耦而在所有负载下优于 Cassandra，但 DEPART 进一步改善了 mLSM 的性能，因为后者产生较大的 compaction 开销。\n\n**实验 5（读/写时间分解）。** 我们展示 Cassandra 和 DEPART 读写过程的时间分解。客户端先加载 200 M 个 KV 对，再执行 20 M 次读取。读过程包括读取 MemTable、cache（含 row cache 和 key cache）、SSTable 的 index block（如 Bloom filter 和 offset）以及 SSTable 的 data block。此处将两层日志中的每个 segment 视为一张 SSTable。写过程包括写 WAL、写 MemTable、flush MemTable、LSM-tree 的 compaction，以及两层日志的 merge-sort（仅 DEPART）。\n\n*图 15：实验 5（读/写时间分解）。*\n\n图 15 展示读写时间分解。对于读取，Cassandra 和 DEPART 的大部分时间都用于读取 SSTable 的 index block，因为读取 KV 对需要检查每个 LSM-tree 层级 index block 中的 Bloom filter 以判断 KV 对是否存在，只有存在时才根据 index block 中的 offset 从 SSTable 读取 data block。总体而言，DEPART 将读取 SSTable 的 index block 和 data block 的时间成本分别降低 56% 和 45%。原因有二：DEPART 的 LSM-tree 只存主副本，SSTable 数量大幅减少；同时，它用范围组管理冗余副本，定位一个 KV 对所需的读取次数也减少。\n\n对于写入，由于 DEPART 并行写入主副本和冗余副本，因此写 WAL 和写 MemTable 的时间成本分别降低 28% 和 37%。DEPART 还通过缩小 LSM-tree 大幅降低 compaction 开销，例如其 compaction 时间仅为 Cassandra 的 30.7%。此外，DEPART 两层日志的 merge-sort 时间仅为 Cassandra compaction 时间的 21.4%。因此，DEPART 的 compaction 与 merge-sort 总时间仅为 Cassandra compaction 时间的 52.1%。\n\n**实验 6（恢复性能）。** 我们评估故障节点的恢复性能。通过配置客户端向集群随机写入 20 M、50 M 和 100 M 个 KV 对，考察不同写入规模。随后用“kill -s processID”命令终止 KV store 进程，并用“rm -r data”命令删除其全部数据，使一个节点崩溃。最后在同一节点重启 KV store 进程，并调用“nodetool repair -full keyspacename”命令进行恢复。\n\n*图 16：实验 6（恢复性能）。*\n\n图 16(a) 展示总恢复时间。Cassandra 恢复 20 M、50 M 和 100 M 个 KV 对分别耗时 8.9、26.9 和 72.4 分钟，而 DEPART 分别仅需 4.1、15.4 和 45.2 分钟。总体上，DEPART 将 Cassandra 的恢复时间降低 38–54%。主要原因是 DEPART 并行修复主副本与冗余副本，并且借助副本解耦在恢复期间扫描的数据少得多。\n\n图 16(b) 还展示修复 50 M 和 100 M 个 KV 对时的恢复时间分解，包括 Build MTs（为所有节点构建 Merkle tree）、Compare MTs（比较所有 Merkle tree）、Receive&Write（从其他节点接收修复数据并写盘）以及 Others（恢复中的其他操作）。通过并行执行主副本与冗余副本的读写过程，DEPART 将 Build MTs 和 Receive&Write 的时间成本相对 Cassandra 几乎减半。\n\n**实验 7（节点崩溃时的性能）。** 我们评估节点崩溃且尚未修复时的读、写和更新性能。客户端先随机写入 100 M 个 KV 对，再像实验 6 一样手动使一个节点崩溃，随后执行 20 M 次读取、100 M 次写入和 100 M 次更新。\n\n*图 17：实验 7（节点崩溃时的性能）。*\n\n图 17(a) 展示节点故障下的吞吐量。与 Cassandra 相比，DEPART 将读、写和更新吞吐量分别提高到 1.69×、1.59× 和 1.55×。主要原因是，即使从两层日志读取冗余副本，DEPART 搜索的数据也始终远少于 Cassandra。此外，DEPART 在正常模式和故障模式下都能改善写性能。\n\n我们还评估不同情形下的降级读性能：(i) 尚未触发节点修复；(ii) 节点修复正在进行；(iii) 将阈值 S（§4.4）从默认值 20 降至 5，使每个节点用于冗余副本的两层日志支持更高有序度。图 17(b) 展示降级读吞吐量。未触发修复或修复正在进行时，DEPART 将降级读吞吐量分别提高到 1.69× 和 1.75×，因为相比 Cassandra 的统一索引，它始终搜索少得多的数据。当两层日志具有更高有序度（例如使用更小的 S）时，DEPART 的降级读性能增益更大，因为从高有序度的两层日志读取 KV 对更加高效。\n\n**实验 8（有序度 S 的影响）。** 我们评估 DEPART 在不同有序度 S 设置下的读写性能，以展示 DEPART 如何通过调整 S 来平衡冗余副本的读写性能增益。客户端先随机写入 200 M 个 KV 对，再执行 20 M 次读取。此处使用一致性配置 (WCL=2, RCL=2)，使每次成功读取都必须访问冗余副本。\n\n| S | 写吞吐量 (KOPS) | 读吞吐量 (KOPS) |\n|---:|---:|---:|\n| 1 | 37.2 | 42.3 |\n| 10 | 57.2 | 31.5 |\n| 20 | 64.7 | 23.1 |\n| → ∞ | 78.4 | 7.6 |\n| Cassandra | 45.4 | 15.4 |\n\n*表 1：实验 8（有序度 S 的影响）。*\n\n表 1 展示结果。对 DEPART 而言，S = 1 时两层日志退化为两层 LSM-tree；由于 KV 对完全有序，此时读吞吐量最高，但为在本地日志的每个范围组中维持单个 sorted run 而频繁执行 merge-sort，故写吞吐量最低。增大 S（如 10 或 20）后，两层日志的有序性放松，merge-sort 开销减小，写吞吐量上升。当 S 足够大时，两层日志退化为 append-only log，因而写吞吐量最高、读吞吐量最低。尽管 DEPART 存在读写性能权衡，但 S 为 1、10 或 20 时，其读写吞吐量仍均高于 Cassandra。\n\n**实验 9（不同 KV store 大小的影响）。** 我们评估不同 KV store 大小的影响。客户端写入的数据量从 200 M 调至 400 M 个 KV 对（即三副本下主副本与冗余副本的总量从 600 GiB 增至 1200 GiB）。\n\n*图 18：实验 9（不同 KV store 大小的影响）。*\n\n图 18 展示不同 KV store 大小下的读写吞吐量。与 Cassandra 相比，DEPART 将写和读吞吐量分别提高 1.43–1.52× 和 2.43–2.95×。随着 KV store 增大，DEPART 的性能增益也提高，因为 DEPART 通过副本解耦缓解读写放大，而 Cassandra 的统一索引会加剧读写放大。\n\n为了更好展示两层日志在不同 KV store 大小下的可扩展性，我们还评估 DEPART 中 LSM-tree 的 compaction 时间和两层日志的 merge-sort 时间，并与 Cassandra 的 compaction 时间比较。KV store 从 200 M 增至 400 M 个 KV 对时，Cassandra 的 compaction 时间增至 2.3×，而 DEPART 的 compaction 与 merge-sort 总时间仅增至 1.9×。具体而言，DEPART 的 merge-sort 时间与 Cassandra compaction 时间之比从 21.4% 降至 18.7%，DEPART 的 compaction 时间与 Cassandra compaction 时间之比从 30.7% 降至 25.2%。因此，DEPART 能随 KV store 增长良好扩展。\n\n**实验 10（不同节点数量的影响）。** 我们评估集群包含更多节点时 DEPART 的性能。节点数设为 5、8 和 10；客户端分别写入 200 M、320 M 和 400 M 个 KV 对，使每个节点包含相同数据量。写入后，客户端分别执行 20 M、32 M 和 40 M 次读取。\n\n*图 19：实验 10（不同节点数量的影响）。*\n\n图 19 展示读写操作的吞吐量结果。与 Cassandra 相比，DEPART 将写和读吞吐量分别提高到 1.35–1.43× 和 2.26–2.43×。无论集群规模如何，DEPART 都通过副本解耦保持相对 Cassandra 的性能增益。因此，随着集群规模增大，DEPART 具有良好的可扩展性。"
    },
    {
      "id": "230-related-work",
      "title": "6 Related Work",
      "titleTranslation": "6 相关工作",
      "level": 1,
      "originalMarkdown": "**Local LSM-tree KV stores.** A number of studies optimize the read and write performance of local LSM-tree KV stores that run on single machines. Read performance can be improved by Bloom filter optimization [23,38], adaptive caching [65], and scan optimization with succinct tries [68], while write performance can be improved by compaction optimization [24,32,55,56], the fragmented LSM-tree [51], KV separation [12,36,43], I/O scheduling optimization [8], memory structure optimization [9], and a mix of optimization techniques for memory-disk-log components [7]. Our work focuses on the replica management in distributed KV stores, and is compatible with the above optimization techniques for local LSM-tree KV stores in individual nodes.\n\n**Distributed KV stores.** Distributed KV stores can be classified into in-memory KV caches [42,53] and persistent stores [1–3,41,50]. Optimization efforts for in-memory KV stores include lock-free and cache-friendly designs for high concurrency and throughput [13,30,40], erasure coding designs for memory efficiency [66,67], self-tuning data placement [49], size-aware sharding for tail latency reduction [26], adaptive load balancing [16], secondary indexing [34], stretched Reed-Solomon coding [35], as well as hot spot optimization [15]. For distributed persistent KV stores, prior studies propose offline index construction for bulk loading [57], adaptive replica selection [52], multi-get scheduling [58], auto-tuning of tail latency optimization [39], load balancing [11], performance optimization via cost-benefit analysis with workload prediction [45], and optimizations of data placement and controlled migration [63, 64]. Persistent KV stores mostly adopt the LSM-tree in the storage layer to store all KV pairs. In contrast, DEPART proposes replica decoupling in distributed LSM-tree KV stores for efficient replica management.\n\n**Replica management.** Prior studies improve the replication of distributed KV stores via efficient replica placement. Early studies include chain replication [61, 62] and its extension [44], low-cost wide-area replication [17], and dynamic hierarchical replication for data grids [46]. Copyset [19] and tiered replication [18] focus on maintaining high storage reliability. Replex [59] supports efficient queries on multiple keys. Our work focuses on the replica management within each storage node, while being compatible with the upper-layer replica placement policies.",
      "translationMarkdown": "**本地 LSM-tree KV 存储。** 大量研究对运行在单机上的本地 LSM-tree KV 存储的读写性能进行了优化。读性能可以通过 Bloom filter 优化 [23,38]、自适应缓存 [65] 以及使用简洁 trie 的扫描优化 [68] 来提升；写性能则可以通过 compaction 优化 [24,32,55,56]、碎片化 LSM-tree [51]、KV 分离 [12,36,43]、I/O 调度优化 [8]、内存结构优化 [9]，以及综合优化内存、磁盘和日志组件的技术 [7] 来提升。我们的工作聚焦于分布式 KV 存储中的副本管理，并且与上述针对各节点中本地 LSM-tree KV 存储的优化技术兼容。\n\n**分布式 KV 存储。** 分布式 KV 存储可以分为内存 KV 缓存 [42,53] 和持久化存储 [1–3,41,50]。针对内存 KV 存储的优化工作包括：面向高并发和高吞吐量的无锁且缓存友好的设计 [13,30,40]、用于提升内存效率的纠删码设计 [66,67]、自调优数据放置 [49]、用于降低尾延迟的大小感知分片 [26]、自适应负载均衡 [16]、二级索引 [34]、延展式 Reed-Solomon 编码 [35]，以及热点优化 [15]。对于分布式持久化 KV 存储，先前研究提出了用于批量加载的离线索引构建 [57]、自适应副本选择 [52]、multi-get 调度 [58]、尾延迟优化的自动调优 [39]、负载均衡 [11]、结合工作负载预测并通过成本效益分析实现的性能优化 [45]，以及数据放置和受控迁移的优化 [63, 64]。持久化 KV 存储大多在存储层采用 LSM-tree 来存储所有 KV 对。相比之下，DEPART 在分布式 LSM-tree KV 存储中提出副本解耦，以实现高效的副本管理。\n\n**副本管理。** 先前研究通过高效的副本放置来改进分布式 KV 存储的复制机制。早期研究包括链式复制 [61, 62] 及其扩展 [44]、低成本广域复制 [17]，以及面向数据网格的动态分层复制 [46]。Copyset [19] 和分层复制 [18] 侧重于维持较高的存储可靠性。Replex [59] 支持对多个 key 的高效查询。我们的工作聚焦于每个存储节点内部的副本管理，同时与上层的副本放置策略兼容。"
    },
    {
      "id": "240-conclusion",
      "title": "7 Conclusion",
      "titleTranslation": "7 结论",
      "level": 1,
      "originalMarkdown": "We propose DEPART, which builds on a novel replica management scheme, replica decoupling, for distributed KV stores. DEPART uses a novel two-layer log design with tunable ordering to efficiently manage the redundant copies for different read and write performance requirements. Our DEPART prototype significantly outperforms Cassandra in different types of KV operations and maintains its performance gains for different consistency levels and parameter settings.",
      "translationMarkdown": "我们提出了 DEPART，它建立在一种面向分布式 KV 存储的新型副本管理方案——副本解耦之上。DEPART 采用一种具有可调排序方式的新型两层日志设计，从而针对不同的读写性能需求高效管理冗余副本。我们的 DEPART 原型在不同类型的 KV 操作中显著优于 Cassandra，并且在不同的一致性级别和参数设置下均能保持其性能优势。"
    }
  ],
  "terms": []
}
