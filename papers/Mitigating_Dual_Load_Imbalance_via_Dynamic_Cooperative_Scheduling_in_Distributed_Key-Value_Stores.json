{
  "schemaVersion": 2,
  "metadata": {
    "title": "Mitigating Dual Load Imbalance via Dynamic Cooperative Scheduling in Distributed Key-Value Stores",
    "titleTranslation": "通过动态协同调度缓解分布式键值存储中的双重负载不均衡",
    "authors": [
      "Jiakun Zhang",
      "Patrick P. C. Lee",
      "Wenzhe Zhu",
      "Yongkun Li",
      "Shuyi Zhang",
      "Yinlong Xu"
    ],
    "year": "2026",
    "venue": "2026 IEEE 42nd International Conference on Data Engineering (ICDE)",
    "doi": "10.1109/ICDE65706.2026.00058",
    "source": "IEEE Xplore",
    "language": "en",
    "targetLanguage": "zh-CN",
    "notes": ""
  },
  "sections": [
    {
      "id": "010-abstract",
      "title": "Abstract",
      "titleTranslation": "摘要",
      "level": 1,
      "originalMarkdown": "Distributed key-value (KV) stores are essential components of modern computing infrastructure, enabling efficient storage and management of large-scale datasets. Existing distributed KV stores often shard data by key ranges into multiple regions and distribute the regions across multiple nodes. However, such range-based sharding leads to load imbalance in two dimensions: CPU utilization and disk I/O bandwidth. Our analysis reveals that the two dimensions exhibit misaligned and dynamic behaviors. Moreover, their tight coupling, where scheduling one dimension affects the other, makes it more challenging to simultaneously achieve balance in both. To address this challenge, which we term dual load imbalance, we propose Libra, a cooperative scheduling framework that monitors the interactions of CPU and disk I/O loads and carefully migrates regions across nodes based on the critical dimension. We implement Libra atop TiKV, a production distributed KV store, and show that Libra increases throughput by up to 72.1% and reduces tail latency by up to 56.7% compared to state-of-the-art approaches.",
      "translationMarkdown": "分布式键值（KV）存储是现代计算基础设施的重要组成部分，能够高效地存储和管理大规模数据集。现有分布式 KV 存储通常按照键范围将数据分片为多个 region，并将这些 region 分布到多个节点上。然而，这种基于范围的分片会导致两个维度上的负载不均衡：CPU 利用率和磁盘 I/O 带宽。我们的分析表明，这两个维度呈现出不一致且动态变化的行为。此外，两者之间存在紧密耦合，即对一个维度进行调度会影响另一个维度，这使得同时实现两个维度的均衡更具挑战性。为应对这一挑战——我们称之为双重负载不均衡（dual load imbalance）——我们提出 Libra，这是一种协同调度框架，它监控 CPU 与磁盘 I/O 负载之间的相互影响，并根据关键维度谨慎地在节点之间迁移 region。我们在生产级分布式 KV 存储 TiKV 之上实现了 Libra，并证明与最先进的方法相比，Libra 可将吞吐量提高最多 72.1%，并将尾延迟降低最多 56.7%。"
    },
    {
      "id": "020-introduction",
      "title": "I. Introduction",
      "titleTranslation": "I. 引言",
      "level": 1,
      "originalMarkdown": "Distributed key-value (KV) stores enhance the performance, scalability, and reliability of local (single-node) KV stores by organizing data as KV pairs and distributing them across multiple nodes. They are integral to applications such as NoSQL/NewSQL databases [4], [28], [38], [55], [58], file systems [37], search engines [11], and machine learning [12]. For scalable range queries, distributed KV stores often employ range-based sharding [4], [11], [14], [52], [58], which divides an ordered key space into contiguous regions and assigns them to nodes, each of which manages multiple ranges of KV pairs. Load imbalance is a common issue in distributed KV stores [26], [36], [60], where some nodes experience disproportionately higher request traffic than others and degrade overall system performance. Under range-based sharding, load imbalance can be caused by uneven distributions of KV pairs [28], varying access patterns [5], [9], and internal storage management for index structures and regions [28], [40]. At the hardware level, our measurement (§II-B) reveals that load imbalance often manifests as bottlenecks in two dimensions, namely CPU utilization and disk I/O bandwidth, each of which exerts a different impact on system performance. We observe that loads in these two dimensions do not always align with request traffic, but instead exhibit complex, unique characteristics, which we term dual load imbalance. First, CPU and disk I/O load distributions are often misaligned across nodes; for example, a node with high CPU utilization may have low disk I/O demands, and vice versa. In addition to uneven request distributions [5], [9], this misalignment is primarily caused by internal characteristics of KV stores, such as variations in KV pair sizes [9] and differing resource demands between read and write operations [28], [40]. This complicates load balancing, as optimizing one dimension may adversely affect load balancing in the other. Second, both dimensions often show significant, simultaneous load imbalance, with distributions shifting dynamically over time. Effective load balancing in distributed KV stores requires careful scheduling of both CPU and disk I/O resources. Existing distributed KV stores commonly employ migration [8], [36], [52] to dynamically relocate regions across nodes for load balancing. However, achieving load balancing is challenging due to the complex interplay between CPU and disk I/O. Existing migration approaches struggle to effectively address the two-dimensional complexity. They adapt existing single-dimensional migration techniques to two-dimensional scenarios using either parallel and isolated execution (referred to as ISO) or weighted aggregation (referred to as WTD). ISO balances each dimension independently, leading to inter-dimensional interference; alternatively, WTD combines CPU and disk I/O loads into a single weighted score for load balancing, but relies on static weights for all nodes and regions and overlooks the misalignment of both dimensions. Our experiments (§II-C) show that even with ISO or WTD, the load on the most heavily loaded node can still exceed the cluster average by more than 50% in distributed\n\n**KV stores.** This motivates the need for a coordinated, real-time load balancing solution. We propose Libra, a cooperative scheduling framework that achieves simultaneous load balancing of both CPU and disk I/O in distributed KV stores based on range-based sharding. Libra monitors real-time CPU and disk I/O usage for each region to capture their dynamic and complex correlations. It identifies the critical dimension between the two dimensions by comparing their deviations from average usage across all nodes. It also updates node resource usage immediately after each scheduling operation, and iteratively performs scheduling operations to guide both dimensions toward a balanced state. To ensure practical efficiency of Libra, we propose three novel techniques: (i) a sufficient condition for two-dimensional resource usage per region that ensures feasible load balancing via migration, coupled with an efficient region-splitting approach for achieving the sufficient condition; (ii) a lightweight synchronization scheme that enables the centralized manager to track real-time region loads; and (iii) a lazy scheduling approach that mitigates migration overhead. Our contributions are summarized as follows.\n\n• We analyze the dual load imbalance problem and the complex correlation between CPU and disk I/O. We also analyze the limitations of three commonly used load balancing approaches in distributed\n\n**KV stores.** • We propose a two-dimensional cooperative scheduling framework that jointly manages both CPU and disk I/O loads, so as to simultaneously achieve load balancing in both dimensions.\n\n• We implement Libra atop TiKV [52], a production distributed KV store. Our evaluation shows that Libra increases throughput by up to 72.1% and reduces tail latency by up to 56.7% compared to baseline approaches. Our source code is now open sourced at https://github.com/JK1Zhang/Libra.",
      "translationMarkdown": "分布式键值（KV）存储通过将数据组织为 KV 对并分布到多个节点上，提高了本地（单节点）KV 存储的性能、可扩展性和可靠性。它们是 NoSQL/NewSQL 数据库 [4], [28], [38], [55], [58]、文件系统 [37]、搜索引擎 [11] 和机器学习 [12] 等应用不可或缺的组成部分。为了实现可扩展的范围查询，分布式 KV 存储通常采用基于范围的分片 [4], [11], [14], [52], [58]，它将有序键空间划分为连续的 region 并将其分配给节点，每个节点管理多个范围的 KV 对。负载不均衡是分布式 KV 存储中的常见问题 [26], [36], [60]，其中一些节点承受的请求流量显著高于其他节点，从而降低整体系统性能。在基于范围的分片下，KV 对分布不均 [28]、访问模式不同 [5], [9]，以及针对索引结构和 region 的内部存储管理 [28], [40] 都可能导致负载不均衡。在硬件层面，我们的测量（§II-B）表明，负载不均衡通常表现为两个维度上的瓶颈，即 CPU 利用率和磁盘 I/O 带宽，二者对系统性能的影响各不相同。我们观察到，这两个维度上的负载并不总是与请求流量一致，而是呈现出复杂且独特的特征，我们将其称为双重负载不均衡。首先，各节点之间的 CPU 与磁盘 I/O 负载分布通常并不一致；例如，CPU 利用率高的节点可能磁盘 I/O 需求较低，反之亦然。除请求分布不均 [5], [9] 外，这种不一致主要由 KV 存储的内部特征引起，例如 KV 对大小的差异 [9]，以及读写操作之间不同的资源需求 [28], [40]。这使负载均衡变得复杂，因为优化一个维度可能会对另一个维度的负载均衡产生不利影响。其次，这两个维度经常同时呈现显著的负载不均衡，并且其分布会随时间动态变化。分布式 KV 存储中的有效负载均衡需要谨慎调度 CPU 和磁盘 I/O 这两类资源。现有分布式 KV 存储通常采用迁移 [8], [36], [52]，动态地将 region 重新安置到不同节点上以实现负载均衡。然而，由于 CPU 与磁盘 I/O 之间存在复杂的相互作用，实现负载均衡颇具挑战性。现有迁移方法难以有效应对这种二维复杂性。它们通过并行且相互隔离的执行（称为 ISO）或加权聚合（称为 WTD），将现有单维迁移技术适配到二维场景。ISO 独立均衡每个维度，从而导致维度间干扰；另一种方法 WTD 则将 CPU 与磁盘 I/O 负载合并为单个加权分数进行负载均衡，但它对所有节点和 region 使用静态权重，忽略了两个维度之间的不一致。我们的实验（§II-C）表明，即便使用 ISO 或 WTD，分布式\n\n**KV 存储中**负载最高节点的负载仍可能比集群平均值高出 50% 以上。这促使我们需要一种协调的实时负载均衡解决方案。我们提出 Libra，这是一种协同调度框架，可在基于范围分片的分布式 KV 存储中同时实现 CPU 和磁盘 I/O 的负载均衡。Libra 监控每个 region 的实时 CPU 和磁盘 I/O 使用量，以捕捉二者动态且复杂的相关性。它通过比较两个维度相对于所有节点平均使用量的偏差，识别这两个维度中的关键维度。它还会在每次调度操作后立即更新节点资源使用量，并迭代执行调度操作，引导两个维度趋向均衡状态。为确保 Libra 的实际效率，我们提出了三项新技术：(i) region 二维资源使用量的一个充分条件，该条件确保可通过迁移实现可行的负载均衡，并配套一种实现该充分条件的高效 region 切分方法；(ii) 一种轻量级同步方案，使集中式管理器能够跟踪实时 region 负载；以及 (iii) 一种可减轻迁移开销的惰性调度方法。我们的贡献总结如下。\n\n• 我们分析了双重负载不均衡问题以及 CPU 与磁盘 I/O 之间的复杂相关性。我们还分析了分布式\n\n**KV 存储中**三种常用负载均衡方法的局限性。• 我们提出了一种二维协同调度框架，共同管理 CPU 和磁盘 I/O 负载，从而同时实现两个维度上的负载均衡。\n\n• 我们在生产级分布式 KV 存储 TiKV [52] 之上实现了 Libra。评估结果表明，与基线方法相比，Libra 可将吞吐量提高最多 72.1%，并将尾延迟降低最多 56.7%。我们的源代码现已开源，地址为 https://github.com/JK1Zhang/Libra。"
    },
    {
      "id": "030-background-and-motivation",
      "title": "II. Background and Motivation",
      "titleTranslation": "II. 背景与动机",
      "level": 1,
      "originalMarkdown": "We first provide an overview of the distributed KV stores considered in this paper, as shown in Figure 1.",
      "translationMarkdown": "我们首先概述本文所考虑的分布式 KV 存储，如图 1 所示。"
    },
    {
      "id": "040-distributed-kv-stores",
      "title": "A. Distributed KV Stores",
      "titleTranslation": "A. 分布式 KV 存储",
      "level": 2,
      "originalMarkdown": "**Data sharding.** Practical distributed KV stores commonly employ range-based sharding to partition KV pairs across nodes [1], [4], [11], [14], [52], [58]. It divides the entire key space into non-overlapping regions, each of which typically has a size from tens to hundreds of megabytes [4], [11], [52]. When a region reaches its capacity threshold, it is split into multiple smaller regions to maintain manageable sizes. A dedicated manager node is deployed for handling region management tasks, such as assigning region IDs, storing metadata, and determining region placement. In this paper, we consider a distributed KV store with n nodes (N1, N2, ···, Nn) and m regions (R1, R2, ···, Rm). For fault tolerance, distributed KV stores employ replication [4], [11], [13], [18], [20], [38], [52] by distributing multiple replicas of a region across nodes. They also employ a consensus protocol, such as Paxos [11] or Raft [28], to select a leader and ensure consistency among replicas. Another commonly used data sharding strategy is hash-based sharding [2], [38], [55], which maps KV pairs to nodes using hashing algorithms (e.g., consistent hashing [34]). Hash-based sharding allows clients to deterministically locate the node for a KV pair via hashing without a dedicated manager, yet it incurs high overhead in range queries [53]. In this paper, we focus on range-based sharding.\n\n**Storage engine.** Each node manages KV pairs for different regions using a unified storage engine. A mainstream choice of the storage engine is the log-structured merge tree (LSM-tree) [50], which organizes KV pairs in a hierarchical, multi-level tree structure [4], [11], [22], [25], [38], [52]. New KV pairs are appended to the lowest level, and moved from lower to higher levels via compaction. Each level (except the lowest level) maintains sorted KV pairs, with overlapping key ranges across different levels. To read a KV pair, the LSM-tree searches from the lowest to higher levels until the KV pair is located.\n\n**Request handling.** To access or insert a KV pair, the client queries the manager to identify the node that manages the relevant region for the key and sends a request to it for the KV pair. Requests are typically implemented via remote procedure calls (RPCs) and are directed to the node for the leader region among replicas to ensure consistency. For read requests, the node retrieves and returns the KV pair. For write requests, the node for the leader region synchronizes modifications across all replicas before responding to the client.\n\nFigure 1: Architecture of a distributed KV store.",
      "translationMarkdown": "**数据分片。** 实际的分布式 KV 存储通常采用基于范围的分片，在不同节点之间划分 KV 对 [1], [4], [11], [14], [52], [58]。它将整个键空间划分为互不重叠的 region，每个 region 的大小通常为数十到数百 MB [4], [11], [52]。当一个 region 达到其容量阈值时，它会被切分为多个更小的 region，以使其大小保持在便于管理的范围内。系统部署一个专用管理器节点来处理 region 管理任务，例如分配 region ID、存储元数据以及决定 region 的放置位置。本文考虑一个具有 n 个节点（N1, N2, ···, Nn）和 m 个 region（R1, R2, ···, Rm）的分布式 KV 存储。为实现容错，分布式 KV 存储采用复制 [4], [11], [13], [18], [20], [38], [52]，将一个 region 的多个副本分布在不同节点上。它们还采用 Paxos [11] 或 Raft [28] 等共识协议来选举 leader，并确保副本之间的一致性。另一种常用的数据分片策略是基于哈希的分片 [2], [38], [55]，它使用哈希算法（例如一致性哈希 [34]）将 KV 对映射到节点。基于哈希的分片使客户端无需专用管理器便可通过哈希确定性地定位某个 KV 对所在的节点，但它会给范围查询带来很高的开销 [53]。本文聚焦于基于范围的分片。\n\n**存储引擎。** 每个节点使用统一的存储引擎管理不同 region 的 KV 对。主流的存储引擎选择是日志结构合并树（LSM-tree）[50]，它以分层的多层树状结构组织 KV 对 [4], [11], [22], [25], [38], [52]。新的 KV 对被追加到最底层，并通过 compaction 从较低层移动到较高层。每一层（最底层除外）都维护有序的 KV 对，不同层之间的键范围可以重叠。读取某个 KV 对时，LSM-tree 从最底层向更高层搜索，直至找到该 KV 对。\n\n**请求处理。** 为了访问或插入一个 KV 对，客户端查询管理器，以确定哪个节点管理着该键对应的 region，并向该节点发送针对该 KV 对的请求。请求通常通过远程过程调用（RPC）实现，并被发送至各副本中 leader region 所在的节点，以确保一致性。对于读请求，该节点检索并返回 KV 对。对于写请求，leader region 所在的节点在响应客户端之前，会在所有副本之间同步修改。\n\n图 1：分布式 KV 存储的架构。"
    },
    {
      "id": "050-dual-load-imbalance",
      "title": "B. Dual Load Imbalance in CPU and Disk I/O",
      "titleTranslation": "B. CPU 与磁盘 I/O 的双重负载不均衡",
      "level": 2,
      "originalMarkdown": "Load imbalance is a critical challenge to distributed KV stores, where CPU and disk I/O are primary resource bottlenecks. Prior studies identify CPU exhaustion [39], [61], [65] and disk I/O bandwidth limitations [6], [19], [46], [54], [63] as the root causes of high access latencies in KV storage. In contrast, memory and network bandwidth resources are often abundant and will not be the bottleneck. For example, in Bigtable deployment [11] with 400 GB of data, only 1 GB of memory is used, and 8,069 busy servers have no more than 17 GB/s of peak network throughput. Thus, we focus on CPU and disk I/O to address load imbalance in distributed\n\n**KV stores.** Note that our scheduling design incurs extra network overhead. For example, we need to collect load status from various nodes, but such overhead is limited compared to Gbps-level network bandwidth. Also, hot regions generally make up only a small portion of the entire system workloads [5], so there is only limited data migrated per node for load balancing (with less than a few hundred MB). Furthermore, we design a protective solution (§III-B) to control transmission costs. We define dual load imbalance as the simultaneous load imbalance in CPU and disk I/O. To quantify the severity of dual load imbalance, we conduct experiments using production workloads. We consider a TiKV cluster (based on range-based sharding) and a Cassandra cluster (also configured with range-based sharding) with default configurations (see §V-A for testbed details). We distribute KV pairs evenly across eight nodes and disable scheduling in our evaluation. We configure a client to send a steady stream of write requests following the Mixgraph benchmark derived from Facebook’s production workloads [9]. Note that other workloads (e.g., YCSB core workloads [7]) also exhibit similar dual load imbalance. We consider two KV size distributions in our evaluation: fixed-size (1 KB in Figures 2(a) and 2(c)) and Pareto-distributed (average 1 KB in Figures 2(b) and 2(d)) [21], [43]. Fixed-size KV pairs serve as a baseline, as they are uncommon in real-world workloads [21]. We consider two load metrics per node: (i) CPU utilization, measured by the sum of per-core utilizations across eight cores (the maximum CPU utilization is 800%), and (ii) disk throughput, measured by the amount of I/Os in megabytes per second (the bandwidth of SATA SSD here is approximately 300 MB/s, which is read-write-mixed due to compaction reads). The cluster runs for 10 minutes, with metrics sampled every 15 s. Figure 2 shows the average results, with two key observations.\n\n**Observation #1: Dynamic CPU and disk I/O imbalance.** Both CPU and disk I/O loads vary significantly across nodes. Highly loaded nodes experience substantially higher load demands than lightly loaded nodes. For example, in Figure 2(b), the CPU utilization of N2 is about 4× that of N4, while the disk throughput of N6 is about 11× that of N4.\n\n**Observation #2: Misaligned CPU and disk I/O loads.** CPU and disk I/O loads are often misaligned across different nodes. High CPU utilization is not necessarily correlated with high disk throughput, and vice versa. For instance, in Figure 2(b), N2 exhibits the highest CPU utilization but relatively low disk throughput. Our further analysis reveals that this misalignment is caused by numerous small-size KV requests issued to N2 (averaging about 300 bytes). This misalignment suggests that balancing one resource dimension alone may not address load imbalance in both dimensions. Notably, other factors, such as differences in resource demands between read and write requests, also contribute to load misalignment.\n\nFigure 2: Distribution of CPU usage and disk I/O across the cluster.",
      "translationMarkdown": "负载不均衡是分布式 KV 存储面临的一项关键挑战，其中 CPU 和磁盘 I/O 是主要的资源瓶颈。以往研究将 CPU 耗尽 [39], [61], [65] 和磁盘 I/O 带宽限制 [6], [19], [46], [54], [63] 确定为 KV 存储中访问延迟较高的根本原因。相比之下，内存和网络带宽资源往往较为充足，并不会成为瓶颈。例如，在包含 400 GB 数据的 Bigtable 部署 [11] 中，仅使用了 1 GB 内存，而且 8,069 台繁忙服务器的峰值网络吞吐量不超过 17 GB/s。因此，我们聚焦于 CPU 和磁盘 I/O，以解决分布式\n\n**KV 存储中的**负载不均衡。需要注意的是，我们的调度设计会带来额外的网络开销。例如，我们需要从各个节点收集负载状态，但与 Gbps 级的网络带宽相比，这类开销很有限。此外，hot region 通常只占整个系统工作负载的一小部分 [5]，因此每个节点仅需迁移有限的数据（少于数百 MB）即可实现负载均衡。进一步地，我们还设计了一种保护方案（§III-B）来控制传输成本。我们将双重负载不均衡定义为 CPU 和磁盘 I/O 同时存在负载不均衡。为了量化双重负载不均衡的严重程度，我们使用生产工作负载开展实验。我们考虑一个 TiKV 集群（基于范围分片）和一个 Cassandra 集群（同样配置为范围分片），二者均采用默认配置（测试平台详情见 §V-A）。我们将 KV 对均匀分布到八个节点上，并在评估中禁用调度。我们配置一个客户端，按照源自 Facebook 生产工作负载的 Mixgraph 基准测试 [9]，持续发送稳定的写请求流。需要注意的是，其他工作负载（例如 YCSB 核心工作负载 [7]）同样表现出类似的双重负载不均衡。在评估中，我们考虑两种 KV 大小分布：固定大小（图 2(a) 和图 2(c) 中为 1 KB）以及 Pareto 分布（图 2(b) 和图 2(d) 中平均为 1 KB）[21], [43]。固定大小的 KV 对作为基线，因为它们在真实工作负载中并不常见 [21]。我们考虑每个节点的两个负载指标：(i) CPU 利用率，以八个核心各自利用率之和衡量（CPU 利用率最大值为 800%）；以及 (ii) 磁盘吞吐量，以每秒 MB 为单位的 I/O 量衡量（此处 SATA SSD 的带宽约为 300 MB/s；由于 compaction 读取，读写混合进行）。集群运行 10 分钟，每 15 s 采样一次指标。图 2 展示了平均结果，并得到两个关键观察。\n\n**观察 #1：动态的 CPU 和磁盘 I/O 不均衡。** 各节点之间的 CPU 和磁盘 I/O 负载差异显著。高负载节点的负载需求远高于低负载节点。例如，在图 2(b) 中，N2 的 CPU 利用率约为 N4 的 4×，而 N6 的磁盘吞吐量约为 N4 的 11×。\n\n**观察 #2：CPU 与磁盘 I/O 负载不一致。** 不同节点上的 CPU 与磁盘 I/O 负载往往并不一致。高 CPU 利用率不一定与高磁盘吞吐量相关，反之亦然。例如，在图 2(b) 中，N2 的 CPU 利用率最高，但其磁盘吞吐量相对较低。我们的进一步分析表明，这种不一致是由发送给 N2 的大量小尺寸 KV 请求（平均约 300 字节）导致的。这种不一致意味着，仅均衡一个资源维度可能无法解决两个维度上的负载不均衡。值得注意的是，读请求和写请求之间资源需求的差异等其他因素也会导致负载不一致。\n\n图 2：集群中 CPU 使用量和磁盘 I/O 的分布。"
    },
    {
      "id": "060-limitations-existing-scheduling",
      "title": "C. Limitations of Existing Scheduling Approaches",
      "titleTranslation": "C. 现有调度方法的局限性",
      "level": 2,
      "originalMarkdown": "Data migration is a common strategy for load balancing in mainstream distributed KV stores [4], [8], [14], [28], [58]. It identifies hot data on overloaded nodes and relocates it to lightly loaded nodes. Existing scheduling implementations for migration can be categorized as follows:\n\n• Single-dimensional scheduling. This approach monitors only a single dimension of nodes, either CPU utilization (referred to as “1D-CPU”) or disk I/O throughput (referred to as “1D-IO”), and migrates hot regions from the most loaded node to the least loaded node. It follows a greedy approach to select the hottest region in the most loaded node for migration.\n\n• Isolated scheduling. This approach (referred to as “2D-ISO”) is adopted by TiKV [52] and CockroachDB [58]. It monitors CPU utilization and I/O loads independently and triggers region migration based on the greedy approach when either dimension becomes highly loaded, without considering the possible influence on the other dimension.\n\n• Weighted scheduling. This approach (referred to as “2D-WTD”) is adopted by HBase [4]. It combines both CPU and I/O loads into a single score with fixed weights, such that a higher score indicates a more loaded node. It can be viewed as score-based single-dimensional scheduling.\n\n**Limitations.** Given that both CPU and I/O loads of a node are closely related to locally stored regions, migrating regions to balance the one resource dimension (e.g., CPU utilization) can interfere with the other dimension (e.g., disk I/O throughput). Such interference, together with the misaligned CPU and I/O loads, renders existing scheduling strategies inefficient. We verify this observation by conducting an evaluation on the same eight-node TiKV cluster described in §II-B. In particular, we use a 0.5:0.5 weight ratio for 2D-WTD. Notably, existing approaches typically balance loads based on software-level metrics and do not support direct scheduling at the hardware resource level. Since it is difficult to assess the hardware resource usage of individual regions (a region is only a logical unit and all regions on a node are managed collectively by the storage engine), systems typically only monitor hardware usage at the node level. To explore the effectiveness of different scheduling policies in hardware scheduling, we leverage a technique (§IV-A) that enables scheduling based on hardware metrics of regions. Figure 3 shows the CPU utilization and disk throughput across all nodes in the cluster for each scheduling strategy. We make the following observations. First, for single-dimensional scheduling, while 1D-CPU or 1D-IO balances its targeted dimension, the other dimension remains highly imbalanced due to misaligned CPU and disk I/O loads. Second, for isolated scheduling, 2D-ISO improves load balancing in both dimensions, but still fails to achieve complete load balancing due to the interference between CPU and disk I/O. Migration for addressing one dimension (e.g., reducing CPU load on N2) can inadvertently disrupt the other (e.g., increasing disk throughput on N6). Finally, for weighted scheduling, 2D-WTD slightly outperforms 2D-ISO in reducing the gap between the highest and lowest loads, but remains ineffective. The reason is that both CPU and I/O loads are assigned equal weights across all nodes in load balancing. It fails to capture the misaligned nature of the CPU and I/O loads. For example, if CPU and disk I/O loads are measured by a score from 0 to 100 with an equal weight of 0.5, a node with high CPU load (100) but low disk I/O load (50) and another node with low CPU load (50) but high disk I/O load (100) both yield a score of 75. This creates an illusion of load balancing, but the actual CPU and disk I/O loads remain significantly imbalanced.\n\nFigure 3: Impact of no scheduling (“NO-SCH”), single-dimensional scheduling based on either CPU or disk I/O (“1D-CPU” and “1D-IO”), isolated scheduling (“2D-ISO”), and weighted scheduling (“2D-WTD”). The bars in each sub-figure correspond to the loads in nodes N1–N8.",
      "translationMarkdown": "数据迁移是主流分布式 KV 存储中实现负载均衡的一种常见策略 [4], [8], [14], [28], [58]。它识别过载节点上的 hot data，并将其重新安置到低负载节点。现有用于迁移的调度实现可分为以下几类：\n\n• 单维调度。该方法仅监控节点的一个维度，即 CPU 利用率（称为“1D-CPU”）或磁盘 I/O 吞吐量（称为“1D-IO”），并将 hot region 从负载最高的节点迁移到负载最低的节点。它采用贪心方法，选择负载最高节点中最热的 region 进行迁移。\n\n• 隔离调度。TiKV [52] 和 CockroachDB [58] 采用了这种方法（称为“2D-ISO”）。它独立监控 CPU 利用率和 I/O 负载，并在任一维度进入高负载状态时，基于贪心方法触发 region 迁移，而不考虑这可能对另一个维度造成的影响。\n\n• 加权调度。HBase [4] 采用了这种方法（称为“2D-WTD”）。它使用固定权重将 CPU 和 I/O 负载合并为单个分数，分数越高表示节点负载越高。它可被视为基于分数的单维调度。\n\n**局限性。** 鉴于一个节点的 CPU 和 I/O 负载都与本地存储的 region 密切相关，通过迁移 region 来均衡一个资源维度（例如 CPU 利用率）可能会干扰另一个维度（例如磁盘 I/O 吞吐量）。这种干扰叠加 CPU 与 I/O 负载之间的不一致，使得现有调度策略效率低下。我们在 §II-B 所述的同一个八节点 TiKV 集群上进行评估，验证了这一观察。具体而言，我们对 2D-WTD 使用 0.5:0.5 的权重比。值得注意的是，现有方法通常根据软件层指标均衡负载，并不支持直接在硬件资源层面进行调度。由于难以评估单个 region 的硬件资源使用情况（region 只是逻辑单元，节点上的所有 region 由存储引擎统一管理），系统通常只在节点层面监控硬件使用量。为了探索不同调度策略在硬件调度中的有效性，我们利用了一项支持基于 region 硬件指标进行调度的技术（§IV-A）。图 3 展示了采用每种调度策略时集群中所有节点的 CPU 利用率和磁盘吞吐量。我们得到以下观察。首先，对于单维调度，虽然 1D-CPU 或 1D-IO 能够均衡其目标维度，但由于 CPU 与磁盘 I/O 负载不一致，另一个维度仍然高度不均衡。其次，对于隔离调度，2D-ISO 改善了两个维度上的负载均衡，但由于 CPU 与磁盘 I/O 之间的干扰，仍无法实现完全的负载均衡。旨在解决一个维度问题的迁移（例如降低 N2 上的 CPU 负载）可能无意中破坏另一个维度的均衡（例如增加 N6 上的磁盘吞吐量）。最后，对于加权调度，2D-WTD 在缩小最高负载与最低负载之间差距方面略优于 2D-ISO，但仍然无效。原因在于进行负载均衡时，所有节点上的 CPU 和 I/O 负载都被赋予相同权重。它无法捕捉 CPU 与 I/O 负载不一致的特性。例如，如果以 0 到 100 的分数衡量 CPU 和磁盘 I/O 负载，并赋予二者相同的 0.5 权重，那么一个 CPU 负载高（100）但磁盘 I/O 负载低（50）的节点，与另一个 CPU 负载低（50）但磁盘 I/O 负载高（100）的节点，所得分数均为 75。这会造成负载已经均衡的假象，但实际的 CPU 和磁盘 I/O 负载仍然严重不均衡。\n\n图 3：不采用调度（“NO-SCH”）、基于 CPU 或磁盘 I/O 的单维调度（“1D-CPU”和“1D-IO”）、隔离调度（“2D-ISO”）以及加权调度（“2D-WTD”）的影响。每个子图中的柱分别对应节点 N1–N8 的负载。"
    },
    {
      "id": "070-cooperative-scheduling",
      "title": "III. Cooperative Scheduling",
      "titleTranslation": "III. 协同调度",
      "level": 1,
      "originalMarkdown": "We propose a novel scheduling framework called cooperative scheduling to address dual load imbalance.",
      "translationMarkdown": "我们提出了一种名为协同调度（cooperative scheduling）的新型调度框架，以解决双重负载不均衡问题。"
    },
    {
      "id": "080-problem-formulation",
      "title": "A. Problem Formulation",
      "titleTranslation": "A. 问题形式化",
      "level": 2,
      "originalMarkdown": "We define the notation for the dual load balancing problem as follows. Consider a distributed KV store with m regions R1, R2, ···, Rm and n nodes N1, N2, ···, Nn. We represent the load of each region Ri as a two-dimensional load vector (αi,βi), in which αi and βi represent the CPU utilization and disk I/O bandwidth of Ri, respectively. Similarly, we represent the load of each node Nj as a two-dimensional load vector (A j,B j), where Aj = ∑αi and Bj = ∑βi aggregate the CPU utilization and disk I/O bandwidth of all regions in node Nj, respectively. To allow comparisons across CPU and disk I/O loads, we normalize the load vectors as follows:\n\n$$A'_j=A_j/A,\\quad B'_j=B_j/B,\\quad j\\in[1,n]. \\tag{1}$$\n\n$$\\alpha'_i=\\alpha_i/A,\\quad \\beta'_i=\\beta_i/B,\\quad i\\in[1,m]. \\tag{2}$$\n\nwhere A and B denote the average CPU utilization and disk I/O across all n nodes, respectively. Based on normalization, we can directly compare the CPU and disk I/O loads of a region or a node. For example, if α′ i exceeds β ′ i for region Ri, then Ri imposes higher normalized CPU load than normalized disk I/O load, indicating that it is now CPU-intensive. We quantify the deviation of each node’s resource utilization with respect to the system average over all nodes.\n\n$$\\delta_j=\\max\\left(\\frac{A_j-A}{A},\\frac{B_j-B}{B}\\right)=\\max(A'_j-1,B'_j-1),\\quad j\\in[1,n]. \\tag{3}$$\n\nWe further define the load imbalance degree ∆, which specifies the highest resource utilization deviation of all nodes.\n\n$$\\Delta=\\max(\\delta_j),\\quad j\\in[1,n]. \\tag{4}$$\n\nA smaller ∆ indicates better load balance; ideally, we aim for ∆= 0, where the loads of all nodes have no deviation from the system average in both dimensions. Our scheduling objective is to constrain ∆ below a predefined threshold λ, where λ indicates the maximum acceptable deviation from the system average. The unified λ assumes all nodes are homogeneous and cannot adapt to heterogeneous nodes. λ is a configurable parameter. In general, a small value of λ is preferred to ensure low latency and meet SLA requirements. However, in practice, λ cannot be arbitrarily small, as its precision is limited by load estimation discrepancy (§IV-A) and fluctuations. Also, as λ decreases, the scheduling overhead increases due to an increasing number of migrations and splitting operations and extra CPU costs of managing numerous smaller regions (see details in §V-F).",
      "translationMarkdown": "我们对双重负载均衡问题所使用的符号作如下定义。考虑一个具有 m 个 region $R_1, R_2, ···, R_m$ 和 n 个节点 $N_1, N_2, ···, N_n$ 的分布式 KV 存储。我们将每个 region $R_i$ 的负载表示为二维负载向量 $(α_i,β_i)$，其中 $α_i$ 和 $β_i$ 分别表示 $R_i$ 的 CPU 利用率和磁盘 I/O 带宽。类似地，我们将每个节点 $N_j$ 的负载表示为二维负载向量 $(A_j,B_j)$，其中 $A_j = ∑α_i$ 和 $B_j = ∑β_i$ 分别汇总了节点 $N_j$ 中所有 region 的 CPU 利用率和磁盘 I/O 带宽。为了能够比较 CPU 负载与磁盘 I/O 负载，我们对负载向量进行如下归一化：\n\n$$A'_j=A_j/A,\\quad B'_j=B_j/B,\\quad j\\in[1,n]. \\tag{1}$$\n\n$$\\alpha'_i=\\alpha_i/A,\\quad \\beta'_i=\\beta_i/B,\\quad i\\in[1,m]. \\tag{2}$$\n\n其中，$A$ 和 $B$ 分别表示全部 n 个节点的平均 CPU 利用率和平均磁盘 I/O。基于归一化结果，我们可以直接比较一个 region 或一个节点的 CPU 负载与磁盘 I/O 负载。例如，如果 region $R_i$ 的 $α'_i$ 超过 $β'_i$，那么 $R_i$ 施加的归一化 CPU 负载高于归一化磁盘 I/O 负载，表明它此时是 CPU 密集型的。我们量化每个节点的资源利用率相对于所有节点的系统平均值的偏差。\n\n$$\\delta_j=\\max\\left(\\frac{A_j-A}{A},\\frac{B_j-B}{B}\\right)=\\max(A'_j-1,B'_j-1),\\quad j\\in[1,n]. \\tag{3}$$\n\n我们进一步定义负载不均衡度 $\\Delta$，它表示所有节点中最大的资源利用率偏差。\n\n$$\\Delta=\\max(\\delta_j),\\quad j\\in[1,n]. \\tag{4}$$\n\n$\\Delta$ 越小，表示负载均衡效果越好；理想情况下，我们希望 $\\Delta=0$，即所有节点在两个维度上的负载均不偏离系统平均值。我们的调度目标是将 $\\Delta$ 限制在预定义阈值 $\\lambda$ 以下，其中 $\\lambda$ 表示相对于系统平均值可接受的最大偏差。统一的 $\\lambda$ 假设所有节点都是同构的，因此无法适应异构节点。$\\lambda$ 是一个可配置参数。一般而言，为确保低延迟并满足 SLA 要求，倾向于采用较小的 $\\lambda$ 值。然而在实践中，$\\lambda$ 不能任意小，因为其精度受负载估计误差（§IV-A）和负载波动的限制。此外，随着 $\\lambda$ 减小，由于迁移和分裂操作的数量增加，以及管理大量更小 region 所需的额外 CPU 成本，调度开销也会增加（详见 §V-F）。"
    },
    {
      "id": "090-cooperative-scheduling-framework",
      "title": "B. Cooperative Scheduling Framework",
      "titleTranslation": "B. 协同调度框架",
      "level": 2,
      "originalMarkdown": "Cooperative scheduling is activated when ∆ exceeds λ. It then triggers migrations to reduce ∆ to an acceptable level. Unlike isolated or weighted scheduling approaches, which are ineffective due to load misalignment (§II-C), cooperative scheduling iteratively evaluates the system load state and executes migration based on the most recent load information in periodic statistics and measured upload performance from data nodes, so as to ensure adaptive and effective load balancing. Figure 4 depicts the idea of cooperative scheduling, which iteratively performs two key steps: (i) selecting the critical dimension and (ii) selecting a region for migration.\n\n**Selecting the critical dimension.** In each iteration, the cooperative scheduling framework selects the critical dimension, either CPU utilization load or disk I/O load, that contributes most significantly to the current load imbalance. Intuitively, performing load balancing on the critical dimension has a higher load balancing gain than the other dimension. Specifically, the framework identifies the node, Nc, with the highest deviation δj across all nodes Nj, where j = 1,2,...,n. Given Nc, we select the critical dimension by comparing the normalized CPU load and the normalized disk I/O and check which dimension dictates δc: if A′ c > B′ c, the CPU utilization load is the critical dimension; otherwise, the disk I/O load is the critical dimension. The approach is intuitively sound by directly comparing A′ c and B′ c. It prioritizes the dimension with the global peak load (i.e., the highest load of all nodes across both dimensions), thereby maximizing load balancing gains while minimizing interference with the non-critical dimension. Also, it simplifies region selection, as we can focus on identifying the highly loaded regions only in the most overloaded node.\n\n**Selecting the region for migration.** Based on the critical dimension, the cooperative scheduling framework selects a region for migration. For clarity, we assume that in the current iteration, the critical dimension is the CPU load; the discussion is analogous for disk I/O load, with the CPU and I/O load parameters swapped. We first select the source node, Nsrc, with the highest CPU load, from which a region will be chosen for migration. We also select the destination node, Ndst, with the lowest CPU load, to which the region will be migrated. We select an appropriate region from Nsrc for migration. We sort the regions in Nsrc by their CPU load in descending order. For each region Ri, we determine if Ri is selected based on the following three conditions:\n\n• We check the load constraint by determining whether A′ dst + α′ i < 1+λ. This ensures that if Ri is migrated to Ndst, the CPU load of Ndst will not exceed the average load of all nodes plus a tolerable deviation λ ∗A.\n\n• We check the interference constraint by determining whether β ′ i < α′ i. This condition limits the interference to the non-critical I/O dimension by ensuring that the I/O load of Ri is proportionally lower than its CPU load. For example, in the ideal situation where β ′ i = 0, migrating Ri will not affect the I/O load of nodes. Thus, there is no interference between CPU and I/O load dimensions.\n\n• We check the benefit threshold by determining whether α′ i > 1/500, an empirically measured threshold, to ensure that the gain of migrating Ri outweighs the overhead. A larger α′ i implies less extra network overhead incurred by scheduling.\n\nIf all conditions are met, Ri is selected for migration; otherwise, we continue to search for another appropriate region. If no region satisfies the conditions after all regions in Nsrc have been examined, the current round of selection terminates, and the framework proceeds with either of the following options: splitting the current region, or moving on to the next node. The whole scheduling process terminates when either load balance is reached or no suitable region can be found.\n\nFigure 4: Cooperative scheduling framework.",
      "translationMarkdown": "当 $\\Delta$ 超过 $\\lambda$ 时，协同调度会被激活。随后，它会触发迁移，将 $\\Delta$ 降低到可接受的水平。孤立调度或加权调度方法会因负载错位（§II-C）而失效；与这些方法不同，协同调度会迭代评估系统负载状态，并基于周期性统计中的最新负载信息以及数据节点实测的上报性能来执行迁移，从而确保自适应且有效的负载均衡。图 4 展示了协同调度的思想，它迭代执行两个关键步骤：(i) 选择关键维度；(ii) 选择一个用于迁移的 region。\n\n**选择关键维度。** 在每次迭代中，协同调度框架选择对当前负载不均衡贡献最大的关键维度，即 CPU 利用率负载或磁盘 I/O 负载。直观而言，对关键维度执行负载均衡比对另一个维度执行负载均衡能获得更高的负载均衡收益。具体来说，框架在所有节点 $N_j$（其中 $j = 1,2,...,n$）中识别出偏差 $\\delta_j$ 最高的节点 $N_c$。给定 $N_c$ 后，我们通过比较归一化 CPU 负载与归一化磁盘 I/O 负载，并检查哪个维度决定了 $\\delta_c$ 来选择关键维度：如果 $A'_c > B'_c$，则 CPU 利用率负载为关键维度；否则，磁盘 I/O 负载为关键维度。通过直接比较 $A'_c$ 与 $B'_c$，这种方法在直觉上是合理的。它优先处理具有全局负载峰值的维度（即两个维度上所有节点中的最高负载），从而在尽量减少对非关键维度干扰的同时，最大化负载均衡收益。此外，它还简化了 region 选择，因为我们只需在过载最严重的节点中识别高负载 region。\n\n**选择用于迁移的 region。** 协同调度框架根据关键维度选择一个用于迁移的 region。为清楚起见，我们假设当前迭代中的关键维度是 CPU 负载；对于磁盘 I/O 负载，讨论方式与之类似，只需交换 CPU 和 I/O 负载参数。我们首先选择 CPU 负载最高的源节点 $N_{src}$，并从中选取一个 region 进行迁移。我们还选择 CPU 负载最低的目标节点 $N_{dst}$，该 region 将迁移到此节点。我们从 $N_{src}$ 中选择一个合适的 region 进行迁移。我们按照 CPU 负载的降序对 $N_{src}$ 中的 region 进行排序。对于每个 region $R_i$，我们依据以下三个条件判断是否选择 $R_i$：\n\n• 我们通过判断 $A'_{dst} + α'_i < 1+\\lambda$ 是否成立来检查负载约束。这保证了如果将 $R_i$ 迁移到 $N_{dst}$，$N_{dst}$ 的 CPU 负载不会超过所有节点的平均负载加上可容忍偏差 $\\lambda * A$。\n\n• 我们通过判断 $β'_i < α'_i$ 是否成立来检查干扰约束。该条件通过确保 $R_i$ 的 I/O 负载在比例上低于其 CPU 负载，限制对非关键 I/O 维度的干扰。例如，在 $β'_i = 0$ 的理想情况下，迁移 $R_i$ 不会影响节点的 I/O 负载。因此，CPU 与 I/O 负载维度之间不存在干扰。\n\n• 我们通过判断 $α'_i > 1/500$ 是否成立来检查收益阈值；这是一个通过经验测量得到的阈值，用于确保迁移 $R_i$ 的收益大于其开销。$α'_i$ 越大，表示调度引入的额外网络开销越少。\n\n如果所有条件均满足，则选择 $R_i$ 进行迁移；否则，我们继续搜索另一个合适的 region。如果检查完 $N_{src}$ 中的所有 region 后，仍没有 region 满足这些条件，则当前轮次的选择终止，框架随后执行以下选项之一：分裂当前 region，或继续处理下一个节点。当达到负载均衡或找不到合适的 region 时，整个调度过程终止。\n\n图 4：协同调度框架。"
    },
    {
      "id": "100-design-issues",
      "title": "C. Design Issues",
      "titleTranslation": "C. 设计问题",
      "level": 2,
      "originalMarkdown": "Integrating the cooperative scheduling framework into a distributed KV store poses three key challenges.\n\n**Issue #1: System load monitoring.** Accurately attributing resource loads to individual regions is difficult, as load monitoring typically occurs at the node or process level. In particular, cooperative scheduling requires precise, timely load data, but frequent monitoring increases communication overhead and can overwhelm the manager, which is responsible for collecting load information. A lightweight monitoring module is needed to capture region-specific loads efficiently.\n\n**Issue #2: Precise region splitting.** The cooperative scheduling framework selects regions for migration based on the three conditions described in §III-B, but large regions are coarse-grained and may fail to meet these criteria. This causes the algorithm to terminate easily, failing to achieve load balancing. Splitting large regions is a potential solution, but dividing them into precise load sizes is complicated due to the varying popularity of KV pairs. Also, excessively small regions incur significant management overhead. A region-splitting module should support cooperative scheduling by creating regions with an appropriate load size.\n\n**Issue #3: Scheduling plan optimization.** Cooperative scheduling is an iterative algorithm that executes migrations sequentially, but its efficiency can be improved by queuing actions for collective optimization. Analyzing collectively multiple migration actions enables the elimination of redundant migrations and splits, thereby reducing CPU cycles, data transfers, and disk I/O overhead associated with region management.",
      "translationMarkdown": "将协同调度框架集成到分布式 KV 存储中会带来三个关键挑战。\n\n**问题 #1：系统负载监控。** 准确地将资源负载归因于单个 region 十分困难，因为负载监控通常在节点或进程级别进行。尤其是，协同调度需要精确且及时的负载数据，但频繁监控会增加通信开销，并可能使负责收集负载信息的 manager 不堪重负。因此，需要一个轻量级监控模块来高效捕获 region 特定的负载。\n\n**问题 #2：精确的 region 分裂。** 协同调度框架依据 §III-B 中描述的三个条件选择用于迁移的 region，但大型 region 的粒度较粗，可能无法满足这些条件。这会导致算法很容易终止，从而无法实现负载均衡。分裂大型 region 是一种潜在解决方案，但由于 KV 对的热度各不相同，将它们划分为具有精确负载大小的 region 十分复杂。此外，过小的 region 会产生显著的管理开销。region 分裂模块应通过创建具有适当负载大小的 region 来支持协同调度。\n\n**问题 #3：调度计划优化。** 协同调度是一种按顺序执行迁移的迭代算法，但可以通过将操作排入队列并进行整体优化来提升其效率。对多个迁移操作进行整体分析，可以消除冗余的迁移和分裂，从而减少与 region 管理相关的 CPU 周期、数据传输和磁盘 I/O 开销。"
    },
    {
      "id": "110-libra-design",
      "title": "IV. Libra Design",
      "titleTranslation": "IV. Libra 设计",
      "level": 1,
      "originalMarkdown": "Libra addresses the challenges in §III-C via three core techniques. First, it integrates a lightweight system monitoring approach (§IV-A) to track resource usage for each region, using a metric mapping technique that mixes high- and low-precision statistics to monitor the system state without overloading the centralized manager. Second, it develops a precise region-splitting approach (§IV-B) to calculate an appropriate upper bound for region loads and determine the optimal split thresholds for regions. Finally, it uses a lazy scheduling strategy (§IV-C) to defer execution of splitting and migration until necessary by grouping and analyzing multiple actions in a pending queue to mitigate scheduling overhead.",
      "translationMarkdown": "Libra 通过三项核心技术解决 §III-C 中的挑战。首先，它集成了一种轻量级系统监控方法（§IV-A），用于跟踪每个 region 的资源使用情况；该方法采用一种混合高精度统计与低精度统计的指标映射技术，在不使集中式 manager 过载的情况下监控系统状态。其次，它开发了一种精确的 region 分裂方法（§IV-B），用于计算 region 负载的适当上界，并确定 region 的最优分裂阈值。最后，它采用惰性调度策略（§IV-C），通过在 pending queue 中对多个操作进行分组和分析，将分裂与迁移的执行推迟到必要时，以降低调度开销。"
    },
    {
      "id": "120-system-load-monitoring",
      "title": "A. System Load Monitoring",
      "titleTranslation": "A. 系统负载监控",
      "level": 2,
      "originalMarkdown": "Libra employs two lightweight techniques to efficiently monitor hardware resource usage: a metric mapping scheme for estimating region-level loads of the two dimensions and a two-layer information gathering framework for collecting load information with limited communication overhead.\n\n**Load estimation by metric mapping.** Measuring CPU and disk I/O loads directly for regions is challenging. Libra adopts a metric mapping scheme to correlate software-level metrics, such as operations per second (OPS), with hardware-level resource usage, so as to enable accurate load estimation with minimal overhead. It introduces a statistics module in each node to collect software-level metrics, which are mapped to hardware-level metrics based on the established relationships. We first study the correlation between OPS and CPU usage. Through our analysis in §II-B, we identify four major components for CPU usage: (i) Remote Procedure Call (RPC) handling of requests, (ii) Raft message processing, (iii) data persistence, and (iv) KV indexing. We measure the CPU usage of the threads associated with each component. Figure 5 shows the results for the TiKV cluster (the Cassandra cluster shows similar behaviors). Figure 5(a) shows a positive correlation between OPS and CPU usage for write operations (note that writes in the KV engine incur no indexing cost). The CPU load of region Ri on node Nj can be roughly estimated as:\n\n$$\\alpha_i=A_j\\times\\frac{\\mathrm{OPS}_{R_i}}{\\mathrm{OPS}_{N_j}}.$$\n\nwhere Aj is the measured CPU utilization. However, this estimation does not address both write and read operations and their differences in resource usage. Figure 5(b) shows that write requests consume 63% more CPU than read requests at 32 KOPS. To account for this difference in CPU usage, we separate the throughput of read and write operations and denote them by ROPS and WOPS, respectively. We introduce a correction factor c to represent the ratio of CPU usage between write and read operations; from our empirical measurement, we set c = 1.63. Thus, the refined CPU load of Ri on Nj is estimated as:\n\n$$\\alpha_i=A_j\\times\\frac{\\mathrm{ROPS}_{R_i}+c\\times\\mathrm{WOPS}_{R_i}}{\\mathrm{ROPS}_{N_j}+c\\times\\mathrm{WOPS}_{N_j}}.$$\n\nIn long-running systems, the correction factor remains relatively stable, as it is primarily influenced by the underlying hardware. In our experiments, the fluctuations of the value of c are always within 10%. The correction factor can be measured via offline profiling, either before system startup or when substantial changes occur (e.g., significant changes in the number of nodes, total data volume, or hardware configuration). We can also establish a similar relationship between disk I/O and user I/O (i.e., the amount of user-requested data per second). As in CPU estimation, it is necessary to distinguish between reads and writes. Read requests often leverage caching to absorb disk I/O, while write requests incur significantly higher disk I/O due to LSM-tree compaction and replica synchronization [5], [9]. Thus, we directly measure region loads and take into account the impact of replication for accurate I/O estimation.\n\n**Two-layer information gathering.** To accurately characterize and partition a region’s internal load, detailed request information is required beyond basic statistics. Centralized data collection would incur significant communication overhead (§IV-B). Libra employs a two-layer information gathering framework, as shown in Figure 6, which mitigates communication overhead between the manager and cluster nodes. It divides the information gathering process into two layers: each node locally collects detailed request information, while the manager processes aggregated summaries to reduce data transmission. Each node in the cluster computes two load metrics: region-level (load_inforegion) and node-level (load_infonode). These metrics are batched into lightweight heartbeats that are of a few bytes each, and are periodically sent to the manager. The manager runs an imbalance checker to monitor load_infonode for any load imbalance. If imbalance is detected and region splitting is required, the manager selects a large region based on load_inforegion and sends the corresponding node (say N) a split operation request, including the region ID and split ratio. Node N then collects detailed request information for the specified region over a defined period and executes the split operation (§IV-B). Once splitting is complete, the manager generates a scheduling plan using updated heartbeats (load_info’ in Figure 6) and sends it to N. Finally, N schedules the region for migration based on the region ID and destination node ID. By delegating split decisions to individual nodes, the two-layer framework significantly reduces communication overhead. Given the high request volume in distributed KV stores (e.g., up to hundreds of thousands of requests per second), centralized decision-making would substantially increase the number of heartbeats. The two-layer framework allows independent heartbeat rates for load_infonode and load_inforegion, enabling trade-offs between load balancing timeliness and information gathering overhead. Note that high-frequency heartbeats are not necessarily more efficient, as they are susceptible to temporary load changes. Based on empirical measurement, we set the default interval to 10 s to filter short-term fluctuations. If significant fluctuations are detected in recent heartbeats (when the load gap between consecutive intervals exceeds a 20% threshold), Libra temporarily suspends scheduling to prevent erroneous decisions and unnecessary data migration. It resumes scheduling only when the load gap drops below the threshold.\n\nFigure 5: Relationship between requests and CPU usage.\n\nFigure 6: Two-layer information gathering.",
      "translationMarkdown": "Libra 采用两种轻量级技术来高效监控硬件资源使用情况：一种指标映射方案，用于估算 region 在两个维度上的负载；以及一种两层信息收集框架，用于以有限的通信开销收集负载信息。\n\n**通过指标映射进行负载估算。** 直接测量 region 的 CPU 和磁盘 I/O 负载颇具挑战。Libra 采用指标映射方案，将每秒操作数（OPS）等软件层指标与硬件层资源使用情况关联起来，从而以最小的开销实现准确的负载估算。它在每个节点中引入一个统计模块，用于收集软件层指标，并根据已建立的关系将其映射为硬件层指标。我们首先研究 OPS 与 CPU 使用率之间的相关性。通过 §II-B 中的分析，我们确定了 CPU 使用的四个主要组成部分：(i) 请求的远程过程调用（RPC）处理，(ii) Raft 消息处理，(iii) 数据持久化，以及 (iv) KV 索引。我们测量与各组成部分相关联的线程的 CPU 使用率。图 5 展示了 TiKV 集群的结果（Cassandra 集群表现出类似的行为）。图 5(a) 表明，对于写操作，OPS 与 CPU 使用率呈正相关（注意，KV 引擎中的写入不会产生索引开销）。节点 Nj 上 region Ri 的 CPU 负载可以粗略估算为：\n\n$$\\alpha_i=A_j\\times\\frac{\\mathrm{OPS}_{R_i}}{\\mathrm{OPS}_{N_j}}.$$\n\n其中，Aj 是测得的 CPU 利用率。然而，这一估算没有同时处理写操作和读操作，也没有考虑二者在资源使用方面的差异。图 5(b) 表明，在 32 KOPS 时，写请求消耗的 CPU 比读请求多 63%。为考虑这种 CPU 使用差异，我们将读操作与写操作的吞吐量分开，并分别记为 ROPS 和 WOPS。我们引入校正因子 c 来表示写操作与读操作之间的 CPU 使用率之比；根据经验测量，我们将 c 设为 1.63。因此，节点 Nj 上 Ri 的修正后 CPU 负载估算为：\n\n$$\\alpha_i=A_j\\times\\frac{\\mathrm{ROPS}_{R_i}+c\\times\\mathrm{WOPS}_{R_i}}{\\mathrm{ROPS}_{N_j}+c\\times\\mathrm{WOPS}_{N_j}}.$$\n\n在长期运行的系统中，校正因子保持相对稳定，因为它主要受底层硬件影响。在我们的实验中，c 值的波动始终处于 10% 以内。校正因子可以通过离线 profiling 来测量，测量既可以在系统启动前进行，也可以在发生重大变化时进行（例如，节点数量、总数据量或硬件配置发生显著变化）。我们还可以在磁盘 I/O 与用户 I/O（即每秒由用户请求的数据量）之间建立类似的关系。与 CPU 估算一样，有必要区分读和写。读请求通常利用缓存来吸收磁盘 I/O，而写请求则会因 LSM-tree compaction 和副本同步而产生显著更高的磁盘 I/O [5], [9]。因此，为了准确估算 I/O，我们直接测量 region 负载，并将复制的影响考虑在内。\n\n**两层信息收集。** 为了准确刻画并划分一个 region 的内部负载，除了基本统计信息外，还需要详细的请求信息。集中式数据收集会产生显著的通信开销（§IV-B）。如图 6 所示，Libra 采用两层信息收集框架，降低 manager 与集群节点之间的通信开销。该框架将信息收集过程分为两层：每个节点在本地收集详细的请求信息，而 manager 则处理聚合后的摘要，以减少数据传输。集群中的每个节点计算两个负载指标：region 级（load_inforegion）和节点级（load_infonode）。这些指标会被批量封装进轻量级心跳中，每个指标仅占几个字节，并周期性地发送给 manager。manager 运行不均衡检查器，监控 load_infonode 中是否存在负载不均衡。如果检测到不均衡且需要拆分 region，manager 会根据 load_inforegion 选择一个大 region，并向相应节点（记为 N）发送拆分操作请求，其中包括 region ID 和拆分比例。随后，节点 N 在规定的时间段内收集指定 region 的详细请求信息，并执行拆分操作（§IV-B）。拆分完成后，manager 使用更新后的心跳（图 6 中的 load_info’）生成调度计划，并将其发送给 N。最后，N 根据 region ID 和目标节点 ID 调度该 region 进行迁移。通过将拆分决策委托给各个节点，两层框架显著降低了通信开销。鉴于分布式 KV 存储中的请求量很高（例如，每秒多达数十万个请求），集中式决策会大幅增加心跳数量。两层框架允许为 load_infonode 和 load_inforegion 设置彼此独立的心跳频率，从而可以在负载均衡的及时性与信息收集开销之间进行权衡。需要注意的是，高频心跳未必更高效，因为它们容易受到临时负载变化的影响。根据经验测量，我们将默认间隔设为 10 s，以滤除短期波动。如果在近期心跳中检测到显著波动（即相邻两个间隔之间的负载差距超过 20% 的阈值），Libra 会暂时中止调度，以防止错误决策和不必要的数据迁移。只有当负载差距降至该阈值以下时，它才恢复调度。\n\n图 5：请求与 CPU 使用率之间的关系。\n\n图 6：两层信息收集。"
    },
    {
      "id": "130-load-based-splitting",
      "title": "B. Load-based Splitting",
      "titleTranslation": "B. 基于负载的分裂",
      "level": 2,
      "originalMarkdown": "Libra supports load-based splitting, a technique that splits regions based on a specified load ratio, independent of their internal load distribution. Unlike size-based splitting, which splits regions based on the region sizes and is sensitive to load skewness, load-based splitting ensures balanced load distribution. Figure 7 depicts the idea of load-based splitting. When a region requires splitting, the node begins to sample requests for the region over a period of time. It identifies the optimal load split point, and splits the region accordingly. The load-based splitting approach comprises three main components.\n\n**Upper bound of region load.** As stated in Issue #2 (§III-C), excessively large regions can lead to inefficient scheduling, while overly small regions result in additional overhead. To address this issue, we prove that Libra should cap region loads at an upper bound λ/2 (recall that λ represents the upper bound of the load imbalance degree; see §III-A), so as to ensure the balanced state defined in §III-A.\n\nTheorem 1. If the normalized loads α′ i and β ′ i of each region Ri do not exceed λ/2, the system can achieve the load balance state (i.e., ∆≤λ) by scheduling.\n\nProof. Suppose that the both region loads α′ i and β ′ i of each region Ri are no greater than λ/2. We classify regions as A-class if α′ i > β ′ i (i.e., CPU-dominant) and B-class if α ′ i ≤β ′ i (i.e., I/O-dominant). For an empty node Nj, we start by placing an A-class region, followed by placing B-class regions on Nj until Nj’s disk I/O load B′ j exceeds Nj’s CPU load A′ j. We next place A-class regions on Nj until A′ j exceeds B′ j. We repeat this cycle until both A′ j and B′ j exceed one. We repeat the process for other nodes. We argue that Libra achieves a balanced state based on the above placement strategy. Note that each time we add a region to a node, both CPU and I/O loads of the node will increase, but by no more than λ/2 based on our assumption. Thus, A′ j and B′ j always differ by no more than λ/2. When both A′ j and B′ j exceed one, the lower load reaches at most 1+λ/2, while the higher load reaches up to 1+ λ. Thus, we have δj ≤λ, and hence ∆≤λ.\n\nBased on Theorem 1, we split a region only when either of its normalized CPU or disk I/O loads exceeds λ/2. When the loads of the resulting regions are below λ/2, there will be no further splitting. Note that this condition is sufficient but not necessary, and Libra performs splitting only when no lighter-loaded regions meet the migration criteria in §III-B.\n\n**Analysis of convergence.** Based on the above constraints, we analyze the convergence of Libra’s cooperative scheduling (notation follows §III). If ∆> λ, there exists at least one node with δ j > λ (i.e., max(A′ j,B′ j) > 1+λ). We classify such nodes based on the difference dj = |A′ j −B′ j|.\n\n• Case 1: dj < λ/2. If a node has d j < λ/2 and δj > λ, it is selected as a source node. The migrated region has loads α′ i and β ′ i with |α′ i −β ′ i | ≤λ/2, where the critical dimension (higher load) dominates. After migration, dj remains less than λ/2, and the node’s load decreases by at most λ/2. If the initial load is L > 1+λ, the new load satisfies:\n\n$$L-\\alpha'_i\\ge L-\\lambda/2>1+\\lambda-\\lambda/2=1+\\lambda/2. \\tag{5}$$\n\nThus, the node’s load remains above 1 + λ/2. Through repeated migrations, the load eventually decreases to max(A′ j,B′ j) ≤1+λ, achieving balance and no longer participating in the scheduling process.\n\n• Case 2: dj ≥λ/2. If a node has dj ≥λ/2 and δj > λ, it may act as a source or target node. In either case, dj decreases. Since |α′ i −β ′ i | ≥x (where x is the lower bound of |α′ i −β ′ i | across all regions with active workloads), migrating this region reduces dj by at least x, until the node transitions to Case 1. The node’s load remains above 1 + λ/2 until balanced. Other nodes may remain with loads below 1+λ/2 or transition to Case 2 after migration. All nodes eventually reach a balanced state. Let Y = ∆−1 be the maximum load excess above 1 for any node. For Case 1, the number of migrations to reduce the load to 1+λ is at most Y/x. For Case 2, the number of migrations to reduce d j to below λ/2 is at most dj/x. Thus, the algorithm can converge to a balanced state in a finite number of steps.\n\n**Load distribution estimation.** Accurate load-based splitting requires the knowledge of the load distribution of a region. Libra estimates the load distribution by sampling requests over a pre-specified period (e.g., 10 s based on our empirical measurement), which enables us to capture sufficient requests for hot regions based on the temporal locality of load distribution. As shown in Figure 7, when splitting a region, the node records the details of all processed requests for the region over a pre-specified period, including the start_key, end_key, and data volume. For each given key, Libra finds the cumulative load of requests whose keys are smaller than the given key and divides it by the total load to obtain a load ratio, so as to establish a functional relationship between keys and the load distribution. This enables precise load-based splitting. Note that splitting is a one-time operation, and the collected information can be removed after splitting.\n\n**Bound-constrained splitting.** Load-based splitting divides a region to achieve a target load ratio, so as to ensure that new regions meet load requirements (i.e., loads of partitioned regions are at most λ/2). Based on the ordering of KV pairs, Libra can accumulate request loads in key order until the desired load ratio is reached, and mark the current key as the split point. However, for requests with overlapping keys, finding the exact split point becomes complicated and potentially leads to inaccurate load splitting.\n\nLibra employs a heuristic using upper and lower bounds to find an approximate split point. Figure 7 depicts the idea, in which a two-way arrow represents the scope of a request in the key space from the start_key to the end_key. When the split ratio is, say τ, Libra sorts requests by start_key in ascending order and accumulates request loads until the cumulative load reaches τ. The start_key of the next request defines the minimum split point (lower bound), so that the left-side load is at most τ. Next, Libra sorts requests by end_key in descending order and accumulates loads until reaching 1−τ. The end_key of the current request defines the maximum split point (upper bound), with the left-side load at least τ. Finally, the midpoint of the lower and upper bounds serves as the split point. This heuristic works for both overlapping and non-overlapping requests. For overlapping requests, it approximates the split point. As the overlap diminishes, the upper and lower bounds converge, leading to a more accurate estimate. Without overlaps, the heuristic provides accurate results.\n\nFigure 7: Load-based splitting.",
      "translationMarkdown": "Libra 支持基于负载的拆分，这是一种根据指定负载比例拆分 region 的技术，与 region 的内部负载分布无关。基于大小的拆分根据 region 大小来拆分 region，容易受到负载倾斜的影响；与之不同，基于负载的拆分能够确保负载分布均衡。图 7 展示了基于负载的拆分思想。当某个 region 需要拆分时，节点会在一段时间内开始对该 region 的请求进行采样。它确定最优的负载拆分点，并据此拆分该 region。基于负载的拆分方法由三个主要组成部分构成。\n\n**Region 负载的上界。** 如问题 #2（§III-C）所述，过大的 region 会导致调度效率低下，而过小的 region 则会带来额外开销。为解决这一问题，我们证明，为确保达到 §III-A 所定义的均衡状态，Libra 应当将 region 负载限制在上界 λ/2 以内（回顾一下，λ 表示负载不均衡程度的上界；见 §III-A）。\n\n定理 1. 如果每个 region Ri 的归一化负载 α′ i 和 β ′ i 均不超过 λ/2，则系统可以通过调度达到负载均衡状态（即 ∆≤λ）。\n\n证明. 假设每个 region Ri 的两种 region 负载 α′ i 和 β ′ i 均不大于 λ/2。如果 α′ i > β ′ i（即 CPU 主导），我们将该 region 归为 A 类；如果 α ′ i ≤β ′ i（即 I/O 主导），则将其归为 B 类。对于一个空节点 Nj，我们首先放置一个 A 类 region，然后在 Nj 上放置 B 类 region，直到 Nj 的磁盘 I/O 负载 B′ j 超过其 CPU 负载 A′ j。接下来，我们在 Nj 上放置 A 类 region，直到 A′ j 超过 B′ j。我们重复这一循环，直到 A′ j 和 B′ j 均超过 1。对于其他节点，我们重复这一过程。我们认为，基于上述放置策略，Libra 能够达到均衡状态。注意，每次向节点添加一个 region 时，该节点的 CPU 和 I/O 负载都会增加，但根据我们的假设，增量均不会超过 λ/2。因此，A′ j 与 B′ j 之间的差值始终不超过 λ/2。当 A′ j 和 B′ j 均超过 1 时，较低的负载至多达到 1+λ/2，而较高的负载至多达到 1+ λ。因此，我们有 δj ≤λ，进而有 ∆≤λ。\n\n根据定理 1，只有当某个 region 的归一化 CPU 负载或磁盘 I/O 负载超过 λ/2 时，我们才拆分该 region。当拆分所得 region 的负载低于 λ/2 时，将不再进一步拆分。注意，该条件是充分条件而非必要条件，并且只有在不存在满足 §III-B 中迁移标准的负载更轻的 region 时，Libra 才执行拆分。\n\n**收敛性分析。** 基于上述约束，我们分析 Libra 协同调度的收敛性（符号沿用 §III）。如果 ∆> λ，则至少存在一个 δ j > λ 的节点（即 max(A′ j,B′ j) > 1+λ）。我们根据差值 dj = |A′ j −B′ j| 对这类节点进行分类。\n\n• 情形 1：dj < λ/2。如果某个节点满足 d j < λ/2 且 δj > λ，则它会被选为源节点。迁移的 region 具有负载 α′ i 和 β ′ i，满足 |α′ i −β ′ i | ≤λ/2，并且关键维度（负载较高的维度）占主导。迁移后，dj 仍小于 λ/2，且该节点的负载最多降低 λ/2。如果初始负载为 L > 1+λ，则新的负载满足：\n\n$$L-\\alpha'_i\\ge L-\\lambda/2>1+\\lambda-\\lambda/2=1+\\lambda/2. \\tag{5}$$\n\n因此，该节点的负载仍高于 1 + λ/2。经过反复迁移，负载最终会降至 max(A′ j,B′ j) ≤1+λ，从而达到均衡，并且不再参与调度过程。\n\n• 情形 2：dj ≥λ/2。如果某个节点满足 dj ≥λ/2 且 δj > λ，则它可能充当源节点或目标节点。无论哪种情况，dj 都会减小。由于 |α′ i −β ′ i | ≥x（其中 x 是所有具有活跃工作负载的 region 的 |α′ i −β ′ i | 下界），迁移该 region 会使 dj 至少减小 x，直到该节点转入情形 1。在达到均衡之前，该节点的负载始终高于 1 + λ/2。其他节点的负载可能保持低于 1+λ/2，也可能在迁移后转入情形 2。所有节点最终都会达到均衡状态。令 Y = ∆−1 表示任意节点超过 1 的最大负载超额量。对于情形 1，将负载降至 1+λ 所需的迁移次数至多为 Y/x。对于情形 2，将 d j 降至 λ/2 以下所需的迁移次数至多为 dj/x。因此，该算法能够在有限步内收敛至均衡状态。\n\n**负载分布估算。** 准确的基于负载的拆分需要了解 region 的负载分布。Libra 通过在预先指定的时间段内（例如，根据我们的经验测量设为 10 s）对请求进行采样来估算负载分布；基于负载分布的时间局部性，这使我们能够为热点 region 捕获足够多的请求。如图 7 所示，在拆分一个 region 时，节点会在预先指定的时间段内记录为该 region 处理的所有请求的详细信息，包括 start_key、end_key 和数据量。对于每个给定的 key，Libra 找出 key 小于该给定 key 的请求的累计负载，再将其除以总负载以获得负载比例，从而建立 key 与负载分布之间的函数关系。这使得精确的基于负载的拆分成为可能。注意，拆分是一次性操作，所收集的信息可以在拆分后删除。\n\n**受边界约束的拆分。** 基于负载的拆分按照目标负载比例划分一个 region，以确保新的 region 满足负载要求（即划分后 region 的负载至多为 λ/2）。根据 KV 对的顺序，Libra 可以按 key 顺序累加请求负载，直到达到所需的负载比例，并将当前 key 标记为拆分点。然而，对于 key 重叠的请求，寻找精确拆分点会变得复杂，并可能导致不准确的负载拆分。\n\nLibra 采用一种利用上界和下界的启发式方法来寻找近似拆分点。图 7 展示了这一思想，其中双向箭头表示请求在 key 空间中从 start_key 到 end_key 的范围。假设拆分比例为 τ，Libra 按 start_key 升序排列请求，并累加请求负载，直到累计负载达到 τ。下一个请求的 start_key 定义最小拆分点（下界），从而使左侧负载至多为 τ。接下来，Libra 按 end_key 降序排列请求，并累加负载，直到达到 1−τ。当前请求的 end_key 定义最大拆分点（上界），此时左侧负载至少为 τ。最后，以上界和下界的中点作为拆分点。该启发式方法既适用于重叠请求，也适用于不重叠请求。对于重叠请求，它给出拆分点的近似值。随着重叠程度降低，上界与下界逐渐收敛，从而得到更准确的估计。在没有重叠的情况下，该启发式方法能够给出准确结果。\n\n图 7：基于负载的拆分。"
    },
    {
      "id": "140-scheduling-plan-optimization",
      "title": "C. Scheduling Plan Optimization",
      "titleTranslation": "C. 调度计划优化",
      "level": 2,
      "originalMarkdown": "To optimize scheduling, Libra adopts a lazy scheduling strategy by deferring region splitting and migration until necessary and batching operations to mitigate overhead. This avoids unnecessary migration. As shown in Figure 8, the process can be decomposed into three stages.\n\n• Stage 1: Queue maintenance. The manager maintains a dedicated region queue for each node and stores region load information.\n\n• Stage 2: Candidate selection. When Libra triggers scheduling for a hot node, it sorts the regions of the hot node by the critical dimension’s load in descending order. Libra evaluates each region sequentially. If the region load exceeds λ/2 and no suitable destination node is found, it is added to the split candidate queue. Otherwise, it is added to the schedule candidate queue if the region load is smaller than λ/2 or the destination node satisfies: the load after migration will not exceed 1+λ, and (ii) the load difference between two dimensions does not increase.\n\n• Stage 3: Execution. There are two possible cases. In Case 1 (Stage 3-1), if scheduling requirements are not met but the accumulated load in the split candidate queue is significant, the schedule candidate queue is cleared, and splitting proceeds. In Case 2 (Stage 3-2), if the regions in the schedule candidate queue reduce the node’s load below one, Libra clears the split candidate queue and proceeds with scheduling and performs migration. All cleared operations are not executed.\n\n**Optimization analysis.** Libra’s lazy scheduling involves three optimizations. First, Libra adopts deferred splitting, in which regions not involved in scheduling or involved in the schedule candidate queue are not split, even if their load exceeds λ. This avoids unnecessary splits when the resulting regions can be scheduled on the same node or when balance can be achieved by scheduling other unsplit regions. Second, Libra adopts efficient migration, which prevents inefficient migrations of small-load regions in the schedule candidate queue and prioritizes splits for better load distribution. Finally, Libra avoids generating too many regions by merging small, cold regions in the background at a low rate as in production [4], [8], [52], to minimize the impact on performance.\n\nFigure 8: Lazy scheduling.",
      "translationMarkdown": "为优化调度，Libra 采用惰性调度策略，将 region 拆分和迁移推迟到确有必要时再执行，并通过批量执行操作来降低开销。这避免了不必要的迁移。如图 8 所示，该过程可以分解为三个阶段。\n\n• 阶段 1：队列维护。manager 为每个节点维护一个专用的 region 队列，并存储 region 负载信息。\n\n• 阶段 2：候选项选择。当 Libra 触发对热点节点的调度时，它按照关键维度的负载对该热点节点的 region 进行降序排序。Libra 依次评估每个 region。如果 region 负载超过 λ/2 且找不到合适的目标节点，则将其加入拆分候选队列。否则，如果 region 负载小于 λ/2，或者目标节点满足以下条件，则将其加入调度候选队列：迁移后的负载不会超过 1+λ，并且 (ii) 两个维度之间的负载差异不会增大。\n\n• 阶段 3：执行。存在两种可能的情形。在情形 1（阶段 3-1）中，如果不满足调度要求，但拆分候选队列中的累计负载较大，则清空调度候选队列，并继续执行拆分。在情形 2（阶段 3-2）中，如果调度候选队列中的 region 能够将该节点的负载降至 1 以下，Libra 则清空拆分候选队列，继续执行调度并进行迁移。所有被清除的操作均不会执行。\n\n**优化分析。** Libra 的惰性调度包含三项优化。首先，Libra 采用延迟拆分：对于不参与调度或进入调度候选队列的 region，即使其负载超过 λ，也不会进行拆分。当拆分所得 region 可以被调度到同一节点，或通过调度其他未拆分 region 即可实现均衡时，这可以避免不必要的拆分。其次，Libra 采用高效迁移，它会阻止调度候选队列中低负载 region 的低效迁移，并优先执行拆分以获得更好的负载分布。最后，Libra 通过以生产系统中的做法 [4], [8], [52] 在后台以较低速率合并小型冷 region，避免生成过多 region，从而最大限度地减小对性能的影响。\n\n图 8：惰性调度。"
    },
    {
      "id": "150-evaluation",
      "title": "V. Evaluation",
      "titleTranslation": "V. 评估",
      "level": 1,
      "originalMarkdown": "We implement Libra on TiKV [52] and compare it with multiple scheduling strategies. We evaluate the load-balancing effectiveness (§V-B), microbenchmark performance (§V-C), and performance under YCSB workloads [7], as well as the impact of Libra’s components (§V-E) and configurations (§V-F).",
      "translationMarkdown": "我们在 TiKV [52] 上实现了 Libra，并将其与多种调度策略进行比较。我们评估了负载均衡的有效性（§V-B）、微基准测试性能（§V-C）和 YCSB 工作负载 [7] 下的性能，以及 Libra 各组件（§V-E）和配置（§V-F）的影响。"
    },
    {
      "id": "160-experiment-setup",
      "title": "A. Experiment Setup",
      "titleTranslation": "A. 实验设置",
      "level": 2,
      "originalMarkdown": "**Testbed.** We conduct experiments on an in-house cluster comprising five physical machines, each equipped with a 20-core 2.1 GHz Intel Xeon Gold 5218R CPU, 32 GB of RAM, and a 1 TB SATA SSD, running Ubuntu 22.04.5 LTS. All machines are interconnected by a 10 Gbps network. We set up nine nodes: one manager node and eight data nodes. One machine hosts the manager node, and each of the remaining four machines hosts two data nodes, with eight cores allocated per data node. We use TiKV [52] (v5.4.0), a production KV store using range-based sharding [11], [14], [58], as the codebase to ensure fair comparisons across various scheduling policies.\n\n**Workloads.** We use the extended YCSB benchmark (based on go-YCSB [51] v1.0.1), with the Mixgraph hotness distribution model [9] to better simulate KV access patterns in real-world production environments (except for the experiments in §V-D, which use the native YCSB workload). Also, we follow the common practice of configuring the KV pair sizes to follow a generalized Pareto distribution [27]. We generate two types of workloads: (i) discrete workloads, which include discrete operations (e.g., reads, writes, and scans) for microbenchmarks; and (ii) mixed workloads, which use YCSB’s core workloads A-F for evaluation in practical settings.\n\n**Baselines.** We compare Libra against three representative scheduling strategies, namely single-dimensional (1D-CPU and 1D-IO), two-dimensional isolated (2D-ISO), and two-dimensional weighted scheduling (2D-WTD), as detailed in §II-C. The weight for 2D-WTD is set to a ratio of 0.5:0.5 (see §V-F for details). All strategies are implemented within TiKV to ensure a fair comparison and are extended to support the metric mapping in §IV-A for CPU and disk I/O scheduling.\n\n**Configurations.** To capture hotness skewness, the hotness distribution in Mixgraph follows a probability density function f(x) = aebx +cedx [21]. To reflect real workload characteristics observed in Facebook’s production clusters, we follow the default parameter setting (a = 14.18, b = −2.917, c = 0.0164, and d = −0.08082) [21]. We set λ = 0.05 (see the rationale explained in §V-F), meaning that the scheduling will be triggered when the maximum load exceeds 1.05× the average load across all nodes. Each region has three replicas for fault tolerance. For each experiment, we show the average results over five runs, with error bars indicating the standard deviation.",
      "translationMarkdown": "**测试平台。** 我们在一个由五台物理机器组成的内部集群上开展实验，每台机器均配备一颗 20 核 2.1 GHz Intel Xeon Gold 5218R CPU、32 GB 内存和一块 1 TB SATA SSD，并运行 Ubuntu 22.04.5 LTS。所有机器通过 10 Gbps 网络互连。我们搭建了九个节点：一个管理节点和八个数据节点。其中一台机器承载管理节点，其余四台机器各承载两个数据节点，每个数据节点分配八个核心。我们使用 TiKV [52]（v5.4.0）作为代码库，以确保不同调度策略之间的公平比较；TiKV 是一种采用基于范围的分片 [11]、[14]、[58] 的生产级 KV 存储。\n\n**工作负载。** 我们使用扩展的 YCSB 基准测试（基于 go-YCSB [51] v1.0.1），并采用 Mixgraph 热度分布模型 [9]，以更好地模拟真实生产环境中的 KV 访问模式（§V-D 中使用原生 YCSB 工作负载的实验除外）。此外，我们遵循常见做法，将 KV 对的大小配置为服从广义帕累托分布 [27]。我们生成两类工作负载：(i) 离散工作负载，其中包括用于微基准测试的离散操作（例如读、写和扫描）；(ii) 混合工作负载，使用 YCSB 的核心工作负载 A-F 在实际设置下进行评估。\n\n**基线。** 我们将 Libra 与 §II-C 中详述的三类代表性调度策略进行比较，即单维调度（1D-CPU 和 1D-IO）、二维隔离调度（2D-ISO）以及二维加权调度（2D-WTD）。2D-WTD 的权重比例设为 0.5:0.5（详见 §V-F）。所有策略均在 TiKV 内实现，以确保公平比较，并被扩展为支持 §IV-A 中用于 CPU 和磁盘 I/O 调度的指标映射。\n\n**配置。** 为了刻画热度偏斜，Mixgraph 中的热度分布遵循概率密度函数 f(x) = aebx +cedx [21]。为了反映在 Facebook 生产集群中观察到的真实工作负载特征，我们采用默认参数设置（a = 14.18、b = −2.917、c = 0.0164、d = −0.08082）[21]。我们设置 λ = 0.05（其依据见 §V-F），这意味着当最大负载超过所有节点平均负载的 1.05× 时将触发调度。每个 region 有三个副本以实现容错。对于每项实验，我们给出五次运行结果的平均值，误差条表示标准差。"
    },
    {
      "id": "170-load-balancing-effectiveness",
      "title": "B. Load Balancing Effectiveness",
      "titleTranslation": "B. 负载均衡效果",
      "level": 2,
      "originalMarkdown": "**Load distribution.** Figures 9 and 10 depict the load distribution among servers. The red portion represents the load of the most heavily loaded node (Max), while the yellow portion indicates the average load across all nodes (Mean). Figure 9 presents the OPS corresponding to CPU load under different scheduling policies, while Figure 10 shows the user I/O bandwidth usage, reflecting the consumption of disk I/O bandwidth. We activate all scheduling policies at the 4th minute. Until the 16th minute, all policies have completed their scheduling tasks, and the load remains stable. We quantify the difference between Max and Mean (calculated as (MAX −Mean)/Mean). A smaller value indicates a higher degree of balance. By comparing the results in Figures 9 and 10, we find that Libra achieves balanced performance in both dimensions simultaneously and attains a significantly smaller Max-Mean gap than all baselines. As shown in Figures 9(a) and 10(a), 1D-CPU effectively balances the load in the CPU dimension, but fails to balance the other; 1D-IO exhibits a similar pattern. For the two-dimensional schedulers (i.e., 2D-ISO and 2D-WTD) (see Figures 9(c), 9(d), 10(c), and 10(d)), although they partially mitigate imbalances in both CPU and disk I/O loads, they exhibit a notable Max-Mean gap, indicating that their load balancing effectiveness remains limited.\n\n**Load balance metric.** To quantify the effectiveness of Libra on load balancing, we use the Max/Mean ratio (the maximum load divided by the average load) to measure the degree of load balance. A lower ratio implies a higher degree of balance, with the ideal value being one (i.e., all nodes have the same load). For brevity, we present results under a write-only workload, while results under other workloads show similar trends. We fix the request rate for all scheduling strategies for fair comparisons. To obtain the CPU and disk I/O utilizations, we first perform scheduling until all migration operations have been completed. We then run the system for another 10 minutes and sample the CPU and disk I/O utilizations every 15 seconds to obtain average results. This provides sufficient sampling points to ensure accuracy of the results. Figure 11 shows that Libra consistently achieves the best performance in balancing both CPU and disk I/O loads among all scheduling policies. Compared to 1D-CPU and 1D-IO, Libra reduces the Max/Mean ratio by 4.1% and 47.0% in CPU, and 52.7% and 5.0% in disk I/O, respectively. While either 1D-CPU or 1D-IO achieves performance similar to Libra in CPU or disk I/O, they fail to balance both CPU and disk I/O simultaneously. Compared to 2D-ISO and 2D-WTD, Libra reduces the Max/Mean ratio by 34.1% and 32.2% in CPU, and by 26.5% and 24.5% in disk I/O, respectively. The performance advantage of Libra comes from the innovation in scheduling principle. While 2D-WTD and 2D-ISO consider the CPU and disk I/O dimensions in isolation or at the expense of compromising the accuracy of load status, Libra cooperatively considers both dimensions based on precise load information.\n\nFigure 9: Throughput distribution after deploying different approaches.\n\nFigure 10: User I/O distribution after deploying different approaches.\n\nFigure 11: The degree of balance in two dimensions.",
      "translationMarkdown": "**负载分布。** 图 9 和图 10 描绘了服务器之间的负载分布。红色部分表示负载最重节点的负载（Max），黄色部分表示所有节点的平均负载（Mean）。图 9 展示了不同调度策略下与 CPU 负载相对应的 OPS，而图 10 展示了用户 I/O 带宽使用量，反映磁盘 I/O 带宽的消耗情况。我们在第 4 分钟启用所有调度策略。到第 16 分钟，所有策略都已完成其调度任务，负载保持稳定。我们量化 Max 与 Mean 之间的差异（计算方式为 (MAX −Mean)/Mean）。该值越小，表示均衡程度越高。通过比较图 9 和图 10 中的结果，我们发现 Libra 能够同时在两个维度上实现均衡，并且其 Max-Mean 差距显著小于所有基线。如图 9(a) 和图 10(a) 所示，1D-CPU 能够有效均衡 CPU 维度的负载，但无法均衡另一个维度；1D-IO 呈现出类似模式。对于二维调度器（即 2D-ISO 和 2D-WTD）（见图 9(c)、9(d)、10(c) 和 10(d)），尽管它们在一定程度上缓解了 CPU 和磁盘 I/O 的负载不均衡，但仍表现出显著的 Max-Mean 差距，这表明其负载均衡效果仍然有限。\n\n**负载均衡指标。** 为了量化 Libra 在负载均衡方面的有效性，我们使用 Max/Mean 比率（最大负载除以平均负载）来衡量负载均衡程度。比率越低意味着均衡程度越高，理想值为 1（即所有节点具有相同负载）。为简洁起见，我们给出了纯写工作负载下的结果，而其他工作负载下的结果呈现类似趋势。为进行公平比较，我们对所有调度策略固定请求速率。为了获得 CPU 和磁盘 I/O 利用率，我们首先执行调度，直至所有迁移操作完成。随后，我们让系统继续运行 10 分钟，并每隔 15 秒采样一次 CPU 和磁盘 I/O 利用率，从而得到平均结果。这提供了足够的采样点，以确保结果的准确性。图 11 表明，在所有调度策略中，Libra 始终在均衡 CPU 和磁盘 I/O 负载方面取得最佳性能。与 1D-CPU 和 1D-IO 相比，Libra 在 CPU 维度上分别将 Max/Mean 比率降低了 4.1% 和 47.0%，在磁盘 I/O 维度上分别降低了 52.7% 和 5.0%。虽然 1D-CPU 或 1D-IO 在 CPU 或磁盘 I/O 维度上取得了与 Libra 相近的性能，但它们无法同时均衡 CPU 和磁盘 I/O。与 2D-ISO 和 2D-WTD 相比，Libra 在 CPU 维度上分别将 Max/Mean 比率降低了 34.1% 和 32.2%，在磁盘 I/O 维度上分别降低了 26.5% 和 24.5%。Libra 的性能优势源于调度原则上的创新。2D-WTD 和 2D-ISO 要么孤立地考虑 CPU 和磁盘 I/O 维度，要么以牺牲负载状态的准确性为代价，而 Libra 则基于精确的负载信息协同考虑两个维度。\n\n图 9：部署不同方法后的吞吐量分布。\n\n图 10：部署不同方法后的用户 I/O 分布。\n\n图 11：两个维度上的均衡程度。"
    },
    {
      "id": "180-microbenchmarks",
      "title": "C. Microbenchmarks",
      "titleTranslation": "C. 微基准测试",
      "level": 2,
      "originalMarkdown": "We evaluate write, read, and scan operations, and report average latency, 99th-percentile (P99) latency, and throughput for different scheduling strategies. For each run, we send 10 million requests to ensure statistical accuracy.\n\n**Write performance.** Figure 12 shows the write performance. For latency, compared to 1D-CPU, 1D-IO, 2D-ISO, and 2D-WTD, Libra reduces the average latency by 41.6%, 45.8%, 30.4%, and 28.4%, and reduces the P99 tail latency by 62.1%, 63.2%, 56.7%, and 55.1%, respectively. Thus, Libra responds to upper-layer applications much more promptly and improves the responsiveness of KV storage. For throughput (Figure 12(c)), Libra achieves 32.4%-72.1% higher throughput.\n\n**Read performance.** Figure 13 shows the read results. Compared to 1D-CPU, 1D-IO, 2D-ISO, and 2D-WTD, Libra reduces the average latency by 27.7%, 33.9%, 20.2%, and 18.9%, and the P99 tail latency by 49.0%, 51.3%, 44.4%, and 42.7%, respectively. It also achieves 23.3%-51.3% higher read throughput.\n\n**Scan performance.** Figure 14 shows the scan results. Compared to 1D-CPU, 1D-IO, 2D-ISO, and 2D-WTD, Libra reduces average latency by 32.1%, 35.6%, 18.7%, and 16.8%, and the P99 tail latency by 51.9%, 53.0%, 36.6%, and 35.1%, respectively. It also achieves 20.2%-55.3% higher throughput.\n\n**Summary.** Libra shows significant performance improvements for all operations. Notably, it reduces tail latency by 35.1%-63.2%, effectively avoiding CPU and disk I/O bottlenecks.\n\nFigure 12: Write performance.\n\nFigure 13: Read performance.\n\nFigure 14: Scan performance.",
      "translationMarkdown": "我们评估了写、读和扫描操作，并报告不同调度策略的平均延迟、第 99 百分位（P99）延迟和吞吐量。每次运行中，我们发送 1000 万个请求，以确保统计准确性。\n\n**写性能。** 图 12 展示了写性能。在延迟方面，与 1D-CPU、1D-IO、2D-ISO 和 2D-WTD 相比，Libra 分别将平均延迟降低了 41.6%、45.8%、30.4% 和 28.4%，并分别将 P99 尾延迟降低了 62.1%、63.2%、56.7% 和 55.1%。因此，Libra 能够更加迅速地响应上层应用，并提升 KV 存储的响应能力。在吞吐量方面（图 12(c)），Libra 实现了 32.4%-72.1% 的吞吐量提升。\n\n**读性能。** 图 13 展示了读操作的结果。与 1D-CPU、1D-IO、2D-ISO 和 2D-WTD 相比，Libra 分别将平均延迟降低了 27.7%、33.9%、20.2% 和 18.9%，并分别将 P99 尾延迟降低了 49.0%、51.3%、44.4% 和 42.7%。它还实现了 23.3%-51.3% 的读吞吐量提升。\n\n**扫描性能。** 图 14 展示了扫描操作的结果。与 1D-CPU、1D-IO、2D-ISO 和 2D-WTD 相比，Libra 分别将平均延迟降低了 32.1%、35.6%、18.7% 和 16.8%，并分别将 P99 尾延迟降低了 51.9%、53.0%、36.6% 和 35.1%。它还实现了 20.2%-55.3% 的吞吐量提升。\n\n**总结。** Libra 对所有操作均展现出显著的性能提升。值得注意的是，它将尾延迟降低了 35.1%-63.2%，有效避免了 CPU 和磁盘 I/O 瓶颈。\n\n图 12：写性能。\n\n图 13：读性能。\n\n图 14：扫描性能。"
    },
    {
      "id": "190-ycsb-evaluation",
      "title": "D. YCSB Evaluation",
      "titleTranslation": "D. YCSB 评估",
      "level": 2,
      "originalMarkdown": "YCSB is the standard for evaluating KV stores and comprises six workloads, each representing a typical real-world scenario. Thus, we evaluate Libra using the six core workloads: A (50% reads, 50% updates), B (95% reads, 5% updates), C (100% reads), D (95% reads, 5% inserts), E (95% scans, 5% inserts), and F (50% reads, 50% read-modify-writes). In each workload, we send 10 million operations, a sufficient number of operations to ensure stable experimental results. The only exception is Workload E, which is limited to one million operations, as Workload E is scan-dominated. We reduce the number of operations to control the total experiment time. Figure 15 presents the results, with all values normalized to 2D-ISO. The results demonstrate that Libra is effective across diverse workloads. In Figure 15(a), Libra shows an average latency reduction of 26.0%, 17.3%, 22.8%, 18.3%, 17.7%, and 26.4% for workloads A to F, respectively. In Figure 15(b), Libra shows a 35.7%-52.7% reduction in the P99 tail latency for all workloads. In Figure 15(c), Libra achieves 22.1%-33.6% improvements in throughput. In Figure 15(d), Libra demonstrates significant improvements in load balancing, achieving a 20.9%-35.8% reduction in the Max/Mean ratio.\n\nFigure 15: YCSB performance. The numbers above the bars represent the values of the corresponding indicators of 1D-ISO.",
      "translationMarkdown": "YCSB 是评估 KV 存储的标准，由六种工作负载组成，每种工作负载代表一种典型的真实场景。因此，我们使用六种核心工作负载评估 Libra：A（50% 读、50% 更新）、B（95% 读、5% 更新）、C（100% 读）、D（95% 读、5% 插入）、E（95% 扫描、5% 插入）以及 F（50% 读、50% 读-修改-写）。在每种工作负载中，我们发送 1000 万次操作，这一操作数量足以确保实验结果稳定。唯一的例外是工作负载 E，由于工作负载 E 以扫描为主，因此限制为 100 万次操作。我们减少操作数量，以控制实验总时长。图 15 给出了结果，所有值均归一化到 2D-ISO。结果表明，Libra 在不同工作负载下均有效。在图 15(a) 中，对于工作负载 A 至 F，Libra 分别将平均延迟降低了 26.0%、17.3%、22.8%、18.3%、17.7% 和 26.4%。在图 15(b) 中，对于所有工作负载，Libra 将 P99 尾延迟降低了 35.7%-52.7%。在图 15(c) 中，Libra 实现了 22.1%-33.6% 的吞吐量提升。在图 15(d) 中，Libra 在负载均衡方面表现出显著改进，将 Max/Mean 比率降低了 20.9%-35.8%。\n\n图 15：YCSB 性能。柱形上方的数字表示 1D-ISO 相应指标的值。"
    },
    {
      "id": "200-performance-breakdown",
      "title": "E. Performance Breakdown",
      "titleTranslation": "E. 性能分解",
      "level": 2,
      "originalMarkdown": "**Load monitoring.** To evaluate the overhead of load monitoring, we measure both types of heartbeat messages and present the results in Table I. Both types cause less than 10% additional CPU usage, demonstrating the framework’s lightweight feature.\n\n**Load-based splitting.** To evaluate the accuracy of load-based splitting (L-split), we randomly select 500 regions for splitting and compare the results against size-based splitting (S-split). Figure 16 presents the average results. The x-axis represents the target load ratio for the newly split region, while the y-axis shows the error rate, calculated by dividing the difference between the target and actual loads by the target load. In Figure 16(a), L-split consistently achieves significantly higher accuracy for CPU utilization, with an error rate of only about 10%. In contrast, S-split exhibits a high error rate of more than 40%. Figure 16(b) shows a similar pattern for disk I/O, indicating that L-split consistently achieves higher accuracy. To evaluate the impact of L-split on system performance, we compare the performance of Libra under three splitting strategies: no-split (no splitting), S-split, and L-split. Figure 17 illustrates the variations in latency and the Max/Mean ratio under these three policies during scheduling. L-split achieves the highest degree of load balance with the fastest scheduling speed, as it accurately partitions regions that require scheduling. Although S-split also attains load balancing, it requires more splitting and scheduling, taking 20 minutes longer than L-split to reach a balanced state. Without splitting, the system fails to achieve load balancing due to overloaded regions.\n\n**Lazy scheduling.** To evaluate the effectiveness of lazy scheduling, we compare the performance of Libra with and without this mechanism. To examine its long-term behavior, we conduct multi-round tests, each under a different hotspot distribution. We record both the number of regions and the average CPU overhead from inter-node communication, as shown in Figure 18. The x-axis represents the number of rounds, each corresponding to the scheduling process for a hotspot. Lazy scheduling effectively reduces both the number of regions and the CPU overhead resulting from communication across numerous small regions. In contrast, without lazy scheduling, the number of regions increases rapidly, resulting in significantly higher CPU overhead (an increase of 105.9% after seven rounds).\n\nFigure 16: Load splitting accuracy.\n\nFigure 17: Impact of the splitting policy.\n\nFigure 18: Impact of lazy scheduling.\n\nTable I: Rate and CPU usage of heartbeats.\n\n| Type | Rate (per node) | CPU usage |\n|---|---:|---:|\n| Store heartbeat | 6 / min | <1% |\n| Region heartbeat | 100–200 / s | 5–10% |",
      "translationMarkdown": "**负载监控。** 为了评估负载监控的开销，我们测量了两类心跳消息，并将结果列于表 I 中。两类心跳造成的额外 CPU 使用率都低于 10%，证明了该框架的轻量级特性。\n\n**基于负载的拆分。** 为了评估基于负载的拆分（L-split）的准确性，我们随机选择 500 个 region 进行拆分，并将结果与基于大小的拆分（S-split）进行比较。图 16 给出了平均结果。x 轴表示新拆分 region 的目标负载比率，y 轴表示误差率，其计算方法是用目标负载与实际负载之差除以目标负载。在图 16(a) 中，L-split 对 CPU 利用率始终实现了显著更高的准确性，误差率仅约为 10%。相比之下，S-split 的误差率很高，超过 40%。图 16(b) 对磁盘 I/O 展示了类似模式，表明 L-split 始终实现了更高的准确性。为了评估 L-split 对系统性能的影响，我们比较 Libra 在三种拆分策略下的性能：no-split（不拆分）、S-split 和 L-split。图 17 展示了调度过程中这三种策略下延迟和 Max/Mean 比率的变化。L-split 实现了最高的负载均衡程度和最快的调度速度，因为它能够准确地划分需要调度的 region。虽然 S-split 也能实现负载均衡，但它需要进行更多的拆分和调度，比 L-split 多花 20 分钟才能达到均衡状态。在不进行拆分的情况下，由于存在过载 region，系统无法实现负载均衡。\n\n**惰性调度。** 为了评估惰性调度的有效性，我们比较了启用和不启用该机制时 Libra 的性能。为了考察其长期行为，我们进行了多轮测试，每轮采用不同的热点分布。我们记录了 region 数量以及节点间通信产生的平均 CPU 开销，如图 18 所示。x 轴表示轮数，每一轮对应针对一个热点的调度过程。惰性调度有效减少了 region 数量，并降低了跨大量小 region 通信所产生的 CPU 开销。相比之下，在不使用惰性调度时，region 数量迅速增加，导致 CPU 开销显著升高（七轮后增加 105.9%）。\n\n图 16：负载拆分准确性。\n\n图 17：拆分策略的影响。\n\n图 18：惰性调度的影响。\n\n表 I：心跳的速率和 CPU 使用率。\n\n| 类型 | 速率（每节点） | CPU 使用率 |\n|---|---:|---:|\n| Store 心跳 | 6 / min | <1% |\n| Region 心跳 | 100–200 / s | 5–10% |"
    },
    {
      "id": "210-impact-configurations",
      "title": "F. Impact of Configurations",
      "titleTranslation": "F. 配置的影响",
      "level": 2,
      "originalMarkdown": "We study the impact of several key configuration parameters in this section. For brevity, we focus on the write-only workload, as other workloads show qualitatively similar results.\n\n**Impact of number of nodes.** To evaluate the scalability of Libra, we test its performance across clusters of 8, 12, and 16 nodes, while keeping the per-node configuration unchanged. Figure 19(a) shows that Libra’s throughput scales linearly with the number of nodes while latency remains stable, which can be attributed to its robust load balancing across all nodes irrespective of the cluster size. Figure 19(b) shows the CPU overhead of the manager node. The CPU utilization for processing requests grows linearly, scaling with the overall throughput. The overhead of region management, including heartbeat handling and scheduling decisions, increases only marginally, indicating that Libra’s scheduling framework imposes minimal overhead and does not hinder the system’s inherent scalability. We also develop and open-source a load-balancing simulator that models the algorithm’s computation and scheduling costs at any scale, producing results that corroborate our experimental findings.\n\n**Impact of load traffic.** We evaluate performance by varying the client request rate with different load sizes. Before each test, we form a baseline by running the system without scheduling and record the throughput as load traffic 1. Figure 20 shows the average and tail latency, with the x-axis showing the ratio of each request rate compared to the baseline. Under light loads (i.e., ratio < 1), bottlenecks are rare, resulting in low latency. However, under high loads, 2D-ISO and 2D-WTD have sharp latency increases, while Libra consistently maintains low latency. Compared to 2D-ISO and 2D-WTD, Libra reduces tail latency by 41.5%-56.7% for load ratios ≥1. Libra prevents nodes from reaching CPU and disk I/O limits, thereby reducing latency under heavy loads.\n\n**Impact of hotspot duration.** Figure 21 shows the performance of 2D-ISO, 2D-WTD, and Libra under different hotspot durations. The tail latency and Max/Mean ratio remain largely unchanged for durations under 5 minutes, as migration requires time to complete. After the 5-minute mark, Libra improves steadily and outperforms the others. By the 30-minute mark, Libra achieves load balance, whereas 2D-ISO and 2D-WTD remain engaged in the balancing process. The results indicate that Libra schedules more efficiently than other baselines.\n\n**Impact of parameter λ.** We evaluate Libra with different values of λ. As shown in Figure 22(a), a smaller λ, which reflects lower tolerance for imbalance, leads to a lower Max/Mean ratio. The improvements in load balance become negligible beyond λ = 0.05 due to estimation errors and system noise. Additionally, tail latency increases due to more splitting and overhead from managing many small regions. Figure 22(b) shows that once λ exceeds 0.05, the number of splits rises sharply and scan throughput drops by 23.1%, mainly because cross-region scanning overhead outweighs load balancing benefits. Thus, we choose λ = 0.05 as the default in Libra.\n\n**Impact of weight setting in 2D-WTD.** To appropriately configure the weights in the 2D-WTD, we evaluate various weight configurations for 2D-WTD to determine their impact, and the results are summarized in Table II. In our experiments, the algorithm performs best at a 0.5:0.5 weight ratio, as both CPU and disk I/O represent critical bottlenecks that require balanced scheduling. The optimal weight may shift in scenarios where hardware configuration changes. For instance, if the CPU is superior while disk I/O remains a bottleneck, assigning a higher weight to I/O can enhance system performance. This highlights a key limitation of 2D-WTD: selecting an effective weight is challenging and the weight lacks adaptability.\n\nFigure 19: Impact of number of nodes.\n\nFigure 20: Impact of load traffic.\n\nFigure 21: Impact of hot spot duration.\n\nFigure 22: Impact of parameter λ.\n\nTable II: 2D-WTD with different weights.\n\n| CPU weight | Disk I/O weight | Max/mean | Tail latency (ms) |\n|---:|---:|---:|---:|\n| 0 | 1 | 2.19 | 153.32 |\n| 0.25 | 0.75 | 2.04 | 139.45 |\n| 0.5 | 0.5 | 1.71 | 125.84 |\n| 0.75 | 0.25 | 2.11 | 133.22 |\n| 1 | 0 | 2.41 | 149.10 |",
      "translationMarkdown": "本节研究几个关键配置参数的影响。为简洁起见，我们重点关注纯写工作负载，因为其他工作负载呈现出定性相似的结果。\n\n**节点数量的影响。** 为了评估 Libra 的可扩展性，我们在由 8、12 和 16 个节点组成的集群上测试其性能，同时保持每个节点的配置不变。图 19(a) 显示，Libra 的吞吐量随节点数量线性扩展，而延迟保持稳定，这可归因于无论集群规模如何，它都能在所有节点之间实现稳健的负载均衡。图 19(b) 展示了 manager 节点的 CPU 开销。用于处理请求的 CPU 利用率随整体吞吐量线性增长。region 管理（包括心跳处理和调度决策）的开销仅略有增加，这表明 Libra 的调度框架只会产生极小的开销，并不会妨碍系统固有的可扩展性。我们还开发并开源了一个负载均衡模拟器，它能够对任意规模下算法的计算与调度成本进行建模，所得结果印证了我们的实验发现。\n\n**负载流量的影响。** 我们通过改变不同负载规模下的客户端请求速率来评估性能。每次测试前，我们在不进行调度的情况下运行系统以形成基线，并将此时的吞吐量记为负载流量 1。图 20 展示了平均延迟和尾延迟，其中 x 轴表示每个请求速率相对于基线的比值。在轻负载下（即比值 < 1），瓶颈很少出现，因此延迟较低。然而，在高负载下，2D-ISO 和 2D-WTD 的延迟急剧上升，而 Libra 始终保持低延迟。与 2D-ISO 和 2D-WTD 相比，当负载比值 ≥1 时，Libra 将尾延迟降低了 41.5%-56.7%。Libra 可防止节点达到 CPU 和磁盘 I/O 的极限，从而降低重负载下的延迟。\n\n**热点持续时间的影响。** 图 21 展示了 2D-ISO、2D-WTD 和 Libra 在不同热点持续时间下的性能。当持续时间不足 5 分钟时，尾延迟和 Max/Mean 比值基本保持不变，因为迁移需要一定时间才能完成。超过 5 分钟后，Libra 的表现稳步改善，并优于其他方法。到 30 分钟时，Libra 已实现负载均衡，而 2D-ISO 和 2D-WTD 仍处于均衡过程中。结果表明，Libra 的调度效率高于其他基线。\n\n**参数 λ 的影响。** 我们评估了采用不同 λ 值时 Libra 的表现。如图 22(a) 所示，较小的 λ 表示对不均衡的容忍度较低，因此会带来较低的 Max/Mean 比值。由于估算误差和系统噪声，当 λ 超过 0.05 后，负载均衡方面的改善变得微不足道。此外，由于进行了更多拆分，且管理大量小 region 会产生额外开销，尾延迟随之增加。图 22(b) 显示，一旦 λ 超过 0.05，拆分次数便会急剧增加，scan 吞吐量下降 23.1%，其主要原因是跨 region 扫描开销超过了负载均衡带来的收益。因此，我们选择 λ = 0.05 作为 Libra 的默认值。\n\n**2D-WTD 中权重设置的影响。** 为了合理配置 2D-WTD 中的权重，我们针对 2D-WTD 评估了多种权重配置以确定其影响，结果汇总于表 II。在我们的实验中，该算法在权重比为 0.5:0.5 时表现最佳，因为 CPU 和磁盘 I/O 都是关键瓶颈，需要通过均衡调度来应对。在硬件配置发生变化的场景中，最优权重可能会改变。例如，如果 CPU 性能更强，而磁盘 I/O 仍是瓶颈，则为 I/O 分配更高权重可以提升系统性能。这凸显了 2D-WTD 的一个关键局限：有效权重难以选择，且权重缺乏自适应能力。\n\n图 19：节点数量的影响。\n\n图 20：负载流量的影响。\n\n图 21：热点持续时间的影响。\n\n图 22：参数 λ 的影响。\n\n表 II：采用不同权重的 2D-WTD。\n\n| CPU 权重 | 磁盘 I/O 权重 | Max/mean | 尾延迟（ms） |\n|---:|---:|---:|---:|\n| 0 | 1 | 2.19 | 153.32 |\n| 0.25 | 0.75 | 2.04 | 139.45 |\n| 0.5 | 0.5 | 1.71 | 125.84 |\n| 0.75 | 0.25 | 2.11 | 133.22 |\n| 1 | 0 | 2.41 | 149.10 |"
    },
    {
      "id": "220-related-work",
      "title": "VI. Related Work",
      "titleTranslation": "VI. 相关工作",
      "level": 1,
      "originalMarkdown": "**KV stores.** Many KV stores employ the LSM-tree as the storage engine [3], [22], [25], [45], [58]. To improve LSMtree management, various optimizations have been proposed, including compaction optimization [17], [24], [56], [57], KV separation [10], [37], [45], Bloom filter management [16], [44], learned indexes [15], parameter tuning [16], [17], [62], etc. Recent work also explores LSM-tree designs for new storage devices, such as persistent memory [31], [33], [35]. Distributed KV stores combine multiple KV store instances over a network for high performance and data reliability [3], [18], [48], [52], [58]. A key challenge in such systems is load imbalance. Libra addresses this issue in LSM-tree-based KV stores while remaining compatible with existing LSM-tree optimizations.\n\n**Load balancing.** Load balancing of distributed systems has been extensively studied. Stateless systems [49] can distribute tasks dynamically without moving data, whereas stateful systems, such as distributed KV stores, depend on data placement. A common approach is random static placement via consistent hashing [2], [34], [38], [55]. However, consistent hashing is unsuitable for systems based on range sharding and cannot handle dynamic load effectively [23]. Some studies leverage caching to support load balancing [23], [30], [42], while others use replication to distribute requests among replicas [41], [47], [64]. However, they are effective only for read-heavy workloads. Libra adopts migration by dynamically adjusting the data distribution, offloading hot data from overloaded nodes to underutilized ones [4], [36], [52], [58]. Libra can also work alongside caching and replication.\n\n**Migration.** Existing distributed KV stores implement migration by moving regions or data within specific ranges, based on software-level metrics [4], [52], [58], [59]. Libra improves load balancing by carefully balancing both CPU utilization and disk I/O. Some studies enable effective hotspot detection for migration [29], [66]; however, they lack the ability to capture region-level hardware resource statistics, a core capability of Libra. Other works focus on optimizing the efficiency of data movement during migration [32], [67], while Libra focuses on the migration decision-making.",
      "translationMarkdown": "**KV store。** 许多 KV store 采用 LSM-tree 作为存储引擎 [3]、[22]、[25]、[45]、[58]。为了改进 LSM-tree 管理，人们提出了多种优化方法，包括 compaction 优化 [17]、[24]、[56]、[57]，KV 分离 [10]、[37]、[45]，Bloom filter 管理 [16]、[44]，learned index [15]，参数调优 [16]、[17]、[62] 等。近期工作还探索了面向新型存储设备（如持久内存）的 LSM-tree 设计 [31]、[33]、[35]。分布式 KV store 通过网络组合多个 KV store 实例，以实现高性能和数据可靠性 [3]、[18]、[48]、[52]、[58]。这类系统中的一个关键挑战是负载不均衡。Libra 在保持与现有 LSM-tree 优化兼容的同时，解决了基于 LSM-tree 的 KV store 中的这一问题。\n\n**负载均衡。** 分布式系统的负载均衡已得到广泛研究。无状态系统 [49] 无需移动数据即可动态分配任务，而分布式 KV store 等有状态系统则依赖于数据放置。一种常见方法是通过一致性哈希进行随机静态放置 [2]、[34]、[38]、[55]。然而，一致性哈希不适用于基于范围分片的系统，也无法有效处理动态负载 [23]。一些研究利用缓存来支持负载均衡 [23]、[30]、[42]，另一些研究则利用复制在副本之间分配请求 [41]、[47]、[64]。然而，这些方法仅对读密集型工作负载有效。Libra 采用迁移，通过动态调整数据分布，将热点数据从过载节点卸载到利用率较低的节点 [4]、[36]、[52]、[58]。Libra 也可以与缓存和复制协同工作。\n\n**迁移。** 现有分布式 KV store 基于软件层指标，通过移动 region 或特定范围内的数据来实现迁移 [4]、[52]、[58]、[59]。Libra 通过谨慎地同时均衡 CPU 利用率和磁盘 I/O 来改善负载均衡。一些研究能够为迁移提供有效的热点检测 [29]、[66]；然而，它们无法捕获 region 级硬件资源统计信息，而这是 Libra 的一项核心能力。其他工作侧重于优化迁移期间数据移动的效率 [32]、[67]，而 Libra 则侧重于迁移决策。"
    },
    {
      "id": "230-conclusion",
      "title": "VII. Conclusion",
      "titleTranslation": "VII. 结论",
      "level": 1,
      "originalMarkdown": "We identify and analyze the dual load imbalance in distributed KV stores and propose Libra, a cooperative scheduling framework that balances CPU and disk I/O loads simultaneously and dynamically. Experiments confirm that Libra achieves significant performance enhancements. In future work, we will automate parameter tuning to reduce manual effort. Although Libra is theoretically transferable to heterogeneous environments, we plan to implement the necessary development to make it practical. Finally, we will extend the framework to non-KV systems to demonstrate its general applicability.",
      "translationMarkdown": "我们识别并分析了分布式 KV store 中的双重负载不均衡，并提出 Libra——一个能够同时且动态地均衡 CPU 和磁盘 I/O 负载的协同调度框架。实验确认 Libra 能够带来显著的性能提升。在未来工作中，我们将实现参数调优的自动化，以减少人工工作。尽管理论上 Libra 可迁移到异构环境，但我们计划完成必要的开发工作，使其能够在实践中应用。最后，我们将把该框架扩展到非 KV 系统，以证明其通用适用性。"
    }
  ],
  "terms": []
}